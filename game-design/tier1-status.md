# Wildhaven — Tier 1 Status

> Authoritative, per-row **build state** for the fifteen Tier-1 systems rows defined in
> [gdd.md](gdd.md) → **Scope: the floor and the depth** — how far each has gotten through
> [systems-pipeline.md](systems-pipeline.md)'s five steps (Scope the thin form → Declare
> constants → Implement thin → Verify headless → Human gate), who owns it, which constants
> are still awaiting a ruling, and what it actually cost. This doc is the *record*;
> systems-pipeline.md is the *procedure*. The content counterpart is
> [content-pipeline-status.md](content-pipeline-status.md).
>
> **What this file is not:** it does not restate the design (that's gdd.md), the field-level
> contract (that's [spec.md](spec.md)), or per-species/terrain/building state (that's
> content-pipeline-status.md). `thin_form` quotes gdd.md's table; it never extends it.
>
> **Who writes here:** the human seeds `thin_form`, `invariants`, `owner_agent`, `est_hours`,
> and later fills `human_gate`, `actual_hours`, and `depth` decisions;
> [gameplay-engineer](../.claude/agents/gameplay-engineer.md) and
> [ui-engineer](../.claude/agents/ui-engineer.md) write `implementation_location` (**before**
> building, not after) and propose `constants`;
> [qa-engineer](../.claude/agents/qa-engineer.md) writes `validation_status` and nothing
> else. **Every field has exactly one write-owner.** `status` is recomputed by whoever last
> changed a field, with one hard rule: it can only show ✅ once `human_gate` is recorded.
>
> **Seeded against actual repo state on 2026-07-27, not against the narrative.** Several
> rows read worse here than gdd.md implies. That is deliberate — the content tracker earned
> its trust the same way. Notably: row 6 has three undecided constants *and* two schema
> fields that do not exist in code; row 12's News Report copy is written and verified but
> has nowhere to live.
>
> **Updated 2026-07-27 (Step 1, → D-26).** The content data layer landed: `TerrainDefinition`,
> `PlaceableDefinition` and `HarvestableTileDefinition`, with six terrain entries, a
> harvestable, a house, and grey-box scenes for all seven. Suite: 29 → 34 green.
>
> **Updated 2026-07-27 (Step 2 — the vertical slice).** Rows **2, 3, 4, 5, 7** are built thin
> and validated. **Row 6 is NOT at its thin form** — see the correction below. The causal path runs end to end: paint rock beside grass →
> `rabbit` capacity rises above 0 → an arrival enqueues → the resident spawns → the fact card
> fires. **Row 4's USP proof is real** — a villager moves into a House only once a cultivated
> field beside it lifts capacity. `human.tres` was authored from roster.md's decided row.
> Suite: 34 → 43 green, 367 new assertions.
>
> **Correction, same day — row 6 was over-reported.** Its thin form in gdd.md names
> **waypoint wander, the home prop, and the live neighborhood preview**; all three were
> excluded from the Step 2 dispatch brief and row 6 was nonetheless summarised as "built
> thin." It is not. The causality spine is real and validated; the row's floor is not met.
> Found by the human running the game and noticing a villager standing motionless inside the
> house it had moved into — which is what playing a build catches and a headless suite
> cannot.
>
> **Updated 2026-07-28 (row 6 correction, part 1 of 2).** **Waypoint wander and the home prop
> are built and verified headless.** Residents now pick a waypoint inside their home site's
> radius, walk to it playing their Walk clip facing the direction of travel, pause on Idle, and
> go again; a den appears at a wild home site and never beside a House. The motion is
> presentation-layer and lives in its own node — `evaluations_run` measured **0** across 120
> simulated seconds of a rabbit walking, so the zero the Performance argument rests on is
> intact. **The third clause of row 6's thin form — the live neighborhood preview — is still
> missing** and is ui-engineer's. Suite: 43/43, unchanged (no new suite; wander was proven with
> a scratch script, since QA owns step 4). Also landed: `WorldRoot.resident_at()`, a
> live-position resident hit-test that replaces `ui/resident_index.gd`'s arrival-time list.
>
> **Updated 2026-07-28 (row 6 correction, part 2 of 2).** **The live neighborhood preview is
> built, and row 6's thin form is now met in full.** While the cursor is over land in Terraform
> or Build, one line reads the neighbourhood **qualitatively** — no counts, no fractions, no
> bars — and the numeric form is unreachable by construction, since `NeighborhoodPreview.read()`
> returns a band and carries no number out. Its cost is measured rather than promised: every
> query originates at the cursor tile, a still cursor costs nothing, and 40 reads moved
> `evaluations_run` by 0. **What it cannot say:** gdd.md has the preview read the qualification
> system's near-miss summary, which is row 12's and does not exist, so there is no "getting
> closer" band — only wild and welcoming, either side of qualification. **`ui/resident_index.gd`
> is deleted**, so there is one resident hit-test in the build and one copy of its constants;
> the priority rule is asserted against a resident that has walked away from where it arrived.
> Suite: 43/43, unchanged (no new suite; the two existing UI suites absorbed the new assertions
> and the preview was proven with a scratch script, since QA owns step 4).
>
> **Updated 2026-07-28 (rows 3 + 10 — Gentle Displacement and the removal path).** **Both
> missing pillar invariants now exist.** The settlement rule is built as its own node
> (`GentleDisplacement`) driving a per-neighbourhood restartable window (`SettlementWindow`):
> capacity is still re-evaluated **immediately** on every edit — unchanged — and what the
> window gates is only the irreversible half. **Nothing about the pre-edit world is recorded
> anywhere**, so "reverting within the window means the displacement never happened" is a
> property of the arithmetic rather than bookkeeping, and it covers free terrain. Arrivals
> were left outside the window and were re-proved to land while a gesture is pending. Row 3's
> `remove_at()` closes #16's build half. **The zero survives:** displacement's own tick returns
> on its first line when nothing is pending, and an edit on land nobody lives on opens no
> gesture at all, so a world with no residents never runs a settlement timer in its life.
> Proven headlessly with a scratch script (120 assertions, since deleted — QA owns step 4);
> suite 45/47, with the two reds owned entirely by the parallel content dispatch that landed
> `human.tres`'s real `fact_text` and flipped two inverted "still PLACEHOLDER" assertions.
> One QA-owned file was edited out of necessity: `test_economy_rules.gd` pinned `WorldRoot`'s
> signal surface at exactly three, and row 10 adds three by design — the pin was widened, its
> intent ("none of them is an error/failure channel") deliberately unchanged.
>
> **What that does and does not mean.** Five rows are at **step 4** (verified headless); **none
> has passed step 5**, the human gate, so no row is ✅. Still entirely unbuilt: rows 1, 11,
> 13, 14. *(Superseded by the 2026-07-28 entry above, and kept so the record reads honestly: this
> paragraph also named two missing **pillar invariants** — Gentle Displacement (row 10) and
> removal/undo with its grace window (row 3, #16). Both were true when written; both are now
> built thin and unblocked for the kid playtest, awaiting only QA validation and the human
> gate.)*
>
> **Updated 2026-07-28 (row 10 — Gentle Displacement).** Rows **3 and 10** are built thin and
> validated, closing the two pillar invariants that were missing after Step 2: the displacement
> guarantee, and removal/undo with its grace window (#16) — which is what makes Pillar 1's word
> *reversible* literally true rather than resource-deep only. Open Question **#31** closed in the
> same wave: the villager fact card is written and source-verified, so no shipped card reads
> `PLACEHOLDER`. Suite: 47 → 50 green, 1550 assertions.
>
> **First parallel dispatch.** Four agents in two concurrent pairs, disjoint by directory. No
> collisions — the claims table below earned its keep on first real use.
>
> **Updated 2026-07-28 (D-27 rulings 1–3 implemented — rows 2 and 6).** `capacity_radius` and
> `max_individuals` are real `AnimalDefinition` fields; `CapacityEvaluator` reads both from the
> species and counts tags over `capacity_radius`, and its module constant is gone. The shipped
> divisors are roster.md's decided Human 1 / Fox 5 / Rabbit 4. The resident tap radius is capped
> at half a tile, so an animal's hitbox is one tile wide instead of 1.6 and the priority rule no
> longer reaches onto the neighbouring tile — which closes the pillar conflict named in the
> paragraph below. **Suite 41/50.** The two content-owned reds are unchanged; the other seven are
> stale pins that these rulings mechanically invalidated (`test_capacity_formula` references the
> deleted constant by name; six suites pin either the 12 divisor or the one-rabbit population it
> produced). They are re-points, not defects, and they are qa-engineer's — the exact line for each
> is in the build report. **One thing the retune surfaced** is recorded in row 6's `constants`: a
> home site can now finish over capacity when a neighbour's arrival takes tiles it was nearer to,
> and nothing consumes that fall.
>
> **Still open before a playtest.** Row 10's *presentation* half is unvalidated (the widgets that
> render the warning; the payload contract they consume is pinned). And a live conflict between
> two pillar invariants: the animal-wins-the-tap priority rule makes the floor's likeliest
> displacement — clearing the field beside an occupied House — unreachable by tap while the
> villager is home (162.8 px hitbox against a 44 px floor). Intermittent, because residents roam.
> Needs a human decision, not a patch.

> **Updated 2026-08-01 (row 1 — Start & persist, built thin).** `SaveStore` (disk — list/read/
> atomic write/name-slugging), `WorldSnapshot` (schema — `capture`/`apply`/`can_apply`/
> `_migrate`), `Autosave` (the four-reason vocabulary and the interval) and `GameSession` (the
> static menu→world hand-off) landed in `project/scripts/save/`, with the one v1 preset as data
> (`WorldPreset` + `project/data/presets/meadow_start.tres`) and a front end
> (`NewGameScreen`/`LoadGameScreen`/`LeaveOverlay`, `project/scripts/menu/` +
> `project/scenes/menu/`). Restoring residents reuses rows 6/10's own spawn and registration
> paths through two new seam methods rather than a second copy of them — see the claims table.
> **Measured (qa-engineer's step 4): 57 suites, 57 passed**, up from a baseline of 51; six new
> suites (`test_save_store`, `test_world_preset`, `test_restore_seams`, `test_world_snapshot`,
> `test_save_round_trip`, `test_autosave_triggers`). The local count includes one untracked
> suite belonging to another workstream (`test_avoids_distance_keeping.gd`); a clean checkout
> of this branch runs 56.
>
> **The floor is not fully met, stated plainly rather than glossed:** `mist_reveal` is verified
> **by contract, not by a live caller** — `test_autosave_triggers.gd` proves every reason in
> `Autosave.REASONS` writes a file, but nothing shipped yet calls `request("mist_reveal")`; row
> 13 (Mist, unbuilt) adds the one call site. And the two menu-flow tasks (New Game/Load Game,
> the Leave overlay) are **unverified headlessly by construction** — the harness cannot deliver
> input to `_unhandled_input`, so both shipped with a human click-through checklist in their
> task reports instead of a suite. `human_gate` is not recorded; `status` stays 🚧.

> **Updated 2026-08-02 (row 1 — the human played it, and the playtest earned its keep).** The
> human walked a real build: New Game → name → in, a faithful reload, Load correctly disabled
> with no saves, the same name twice giving two worlds, a **pending arrival surviving a quit**
> (D-31's fix, confirmed outside the harness), a resident's roamed position surviving, and
> **a rabbit actually moving in** — the causal chain closed in a real build for the first time.
> `human_gate` is recorded as **partial**; `status` stays 🚧, because `LeaveOverlay` and a
> displacement pending across a reload are still unwalked, and `mist_reveal` still has no caller.
>
> **What the playtest found that no suite would have.** The human's first attempt at a rabbit —
> "grass and rock next to each other" — produced **capacity 0**, and below the threshold no
> amount of waiting can ever produce an animal. Measured on this build, each patch cleared
> between readings (a radius of 8 makes closer patches contaminate each other): **1–3 tiles of
> each → 0; 4 of each → 1.** Rabbit's `tiles_per_individual = 4` against `open_grass` **and**
> `cover` costs **eight tiles**; the villager's divisor of 1 costs **two**. So the starter animal
> is four times harder to attract than the villager, the natural first gesture yields nothing
> permanently, and the only thing that says so is the qualitative hover line. Every suite passed
> throughout — the number is correct by its own definition, which is exactly why a suite could
> not catch it. gdd.md's **≤2 min time-to-first-move-in** and the First 60 Seconds both hang on
> it. Recorded in `human_gate` as open; the value is D-27 #2's and remains the human's.

> **Mechanics, read before editing:**
> - Field names are exact and used identically in every row block. Other documents
>   (systems-pipeline.md, the agent definitions) reference these same names.
> - **One field per row, always** — even a not-started field gets its own row saying "not
>   started." A later update touches one field; a combined row has to be split to do that.
> - **The scan table and the row's own `status` must always agree.** Updating one without
>   the other is an incomplete edit.
> - `constants` entries carry `PROPOSED (YYYY-MM-DD) —` until the human deletes the marker.
>   A marker is the open gate; deleting it is the decision.

## Status Glyphs

| Glyph | Meaning |
|---|---|
| ✅ | **Done** — thin form implemented, suite green, human gate recorded |
| 🚧 | **In progress** — some real work exists; not through step 5 |
| 📋 | **Specced** — steps 1–2 complete (thin form scoped, constants decided), nothing built |
| ⬜ | **Not started** |
| ⛔ | **Blocked** — cannot proceed; the blocker is named in the row |

`depth` is tracked separately (`not started` / `thin` / `deepened`) and deliberately does
**not** get its own glyph — encoding two dimensions in one symbol is the mistake the content
tracker correctly avoids.

## Fields

| Field | Pipeline step | Meaning |
|---|---|---|
| `thin_form` | 1 | the row's floor, quoted from gdd.md's fifteen-row table |
| `invariants` | 1 | pillar obligations that ship whole with this row — never blank; says "none" explicitly if so |
| `constants` | 2 | every tuning number the row touches, each mapped to a spec.md constant or `#NN`; `PROPOSED`-marked until the human rules |
| `implementation_location` | 3 | directory under `project/` claimed by this row — **declared before building**, so directory-disjointness is checkable |
| `validation_status` | 4 | `run-tests.sh` result + which suites — **owned by qa-engineer** |
| `human_gate` | 5 | who/when, or what is still blocking it |
| `depth` | — | `not started` \| `thin` \| `deepened` |
| `est_hours` | — | from gdd.md's first-cut hours ledger |
| `actual_hours` | — | measured human-attention hours; feeds Open Question **#30** at the velocity review |
| `status` | computed | single glyph, see legend |

---

## Scan table

| # | System | Phase | Owner | Depth | Est h | Status |
|---|---|---|---|---|---|---|
| 1 | Start & persist | 1, 5 | gameplay-engineer | thin | 2–3 | 🚧 |
| 2 | Camera & modes | 1 | ui-engineer | not started | 3–5 | 🚧 |
| 3 | Terraform | 1 | gameplay-engineer | not started | 2–3 | ✅ |
| 4 | Build | 2 | gameplay-engineer | not started | 1.5–2 | ✅ |
| 5 | Economy | 2 | gameplay-engineer | not started | 0.5–1 | 🚧 |
| 6 | Habitat & move-in | 3, 4 | gameplay-engineer | not started | 4–6 | 🚧 |
| 7 | Fact cards | 5 | ui-engineer | not started | 1.5–2 | 🚧 |
| 8 | The decided roster | 4 | content pipeline | not started | 1.5–2 | 🚧 |
| 9 | Minimal Avoids | 4 | gameplay-engineer | not started | 1 | 🚧 |
| 10 | Gentle displacement | 5 | gameplay-engineer | thin | 2–3 | 🚧 |
| 11 | Species status | 6 | ui-engineer | done (D-34) | 1 | ✅ |
| 12 | Pointers | 6 | ui-engineer | not started | 1 | 🚧 |
| 13 | Mist | 6 | gameplay-engineer | not started | 1.5–2 | ⬜ |
| 14 | A thin audio slice | 7 | tech-art | not started | 0.75–1 | ⬜ |
| 15 | Settings & Credits | 7 | ui-engineer | not started | 0.75–1 | 🚧 |

**Floor total: ≈ 29–40 h** including the unpriced lines in gdd.md's ledger (render
validation ~1 h; integration, debugging and playtest logistics 3–5 h). Rows 11–12 and
14–15 are each half of a paired ledger line.

**Directory claims** (Technical Strategy #6 — parallel dispatch is unsafe unless
directories are disjoint). **Created** where marked; the rest still reserved:

| Directory | Rows |
|---|---|
| `project/scripts/world/` | 3, 13, **6 (resident presentation — declared 2026-07-28)**, **9 (avoids roam bias — declared 2026-08-01; roam is presentation, same file as row 6's wander, not `simulation/`)**, **1 (`world_root.gd` — declared 2026-08-01; `_ready()`'s session branch, save-identity fields, `MIN_SAVED_WORLD_TILES`/`MAX_SAVED_WORLD_TILES`)**, **11 (`world_root.gd` — declared 2026-08-07; one wrapper, `population_of(species_id)`, plus a doc-comment line — additive, no existing method touched)** — **created 2026-07-27** (rows 3/4/5/6 dispatch) |
| `project/scripts/simulation/` | 6 (**`gentle_displacement.gd` extended 2026-08-01 with the arrival-check fix, D-29 #5 — still row 6, not row 9**), 10, **1 (seam — `habitat_simulation.gd` gains `restore_site(position, species_id, resident_positions)` and `home_site_registry.gd` gains `restore_hosted(ids)`, both declared 2026-08-01, both reusing rows 6/10's existing spawn/registration paths rather than duplicating them)**, **11 (seam, declared 2026-08-07 — `home_site_registry.gd` gains `population_of(species_id)`, additive, no existing method body touched; Village Population needs a per-species sum across every site and `total_residents()` cannot give it one)** — **created 2026-07-27**. Row 9 does not use this directory; its roam-bias build is `project/scripts/world/`, above |
| `project/scripts/economy/` | **3 (removal/refund — declared 2026-07-28)**, 4, 5 — **created 2026-07-27** |
| `project/scripts/save/` | 1 — **created 2026-08-01** |
| `project/scripts/menu/` + `project/scenes/menu/` | 1 — **created 2026-08-01** |
| `project/data/presets/` | 1 — **created 2026-08-01** |
| `project/scripts/ui/` + `project/scenes/ui/` | 2, 7, 11, 12, 15 — **created 2026-07-27** (rows 2/7 dispatch) |
| `project/scripts/audio/` | 14 |
| `project/scripts/definitions/` | **3, 4, 5, 6, 8 — SHARED, not disjoint**, plus **1 (`world_preset.gd`, declared 2026-08-01 — a fifth schema in the already-shared directory)** |
| `project/data/terrain/` | 3, 5 |
| `project/data/buildings/` | 4 |
| `project/data/animals/` | 6, 8 |
| `project/assets/placeholder/` | 3, 4 |

**Consequence of row 1's seams into `project/scripts/simulation/` and `project/scripts/world/`:
row 1 is not parallel-safe with rows 3, 6, 9, 10 or 13.** The `simulation/` seam methods
(`HabitatSimulation.restore_site()`, `HomeSiteRegistry.restore_hosted()`) are additive — new
public methods, no existing method body touched — and the `world_root.gd` change is a modified
existing file in a directory already shared by rows 3, 6, 9 and 13. Neither directory is
row-1-exclusive any more: any future dispatch that reopens `habitat_simulation.gd`,
`home_site_registry.gd`, or anything under `project/scripts/world/` (including `world_root.gd`
itself) alongside row 1 work must be sequenced, not parallelized.

**`project/scripts/definitions/` is the one directory the disjointness precondition does
not cover** (declared 2026-07-27, when rows 3/4/5 landed `terrain_definition.gd`,
`placeable_definition.gd` and `harvestable_tile_definition.gd` alongside row 6/8's
existing `animal_definition.gd`). The four schemas are separate files, so ordinary edits
do not collide — but they share the ten-tag habitat vocabulary, which lives once in
`AnimalDefinition.HABITAT_TAGS` and is *referenced* by the other three specifically so it
cannot drift. **Consequence: rows 3, 4, 5 and 6 must not be dispatched in parallel while
any schema is in flux**, and any change to the vocabulary is a system-wide human decision
touching all four rows at once (terrain.md → Tag emission). `project/data/terrain/` is
shared by rows 3 and 5 for the same structural reason: `forest.tres` (row 3's terrain)
holds the reference to `forest_harvest.tres` (row 5's harvestable).

---

## Row 1 — Start & persist

| Field | Value |
|---|---|
| `thin_form` | Fixed preset → name → in; Load a plain named list; autosave on interval + move-in + mist reveal + exit-to-menu. "Save a game" means *autosave works and Load restores faithfully* — no save button. |
| `invariants` | No save button, ever (Pillar 1 — the game never asks the player to manage saving). Autosave must cover the two events the completion test hangs on — a move-in and a mist reveal — because interval-only cadence loses them to a hard quit. |
| `constants` | `DECIDED 2026-08-09 (→ D-35) — Autosave.INTERVAL_SECONDS = 90.0` (spec.md → Pacing Constants, "~1–2 min", midpoint) · `DECIDED 2026-08-09 (→ D-35) — WorldSnapshot.SAVE_VERSION = 1` (the initial value this row's tracker names) · `DECIDED 2026-08-09 (→ D-35) — preset display_name "Meadow Start"` (`project/data/presets/meadow_start.tres`; **#10** owns the real preset list) · `DECIDED 2026-08-09 (→ D-35) — DEFAULT_WORLD_NAME "Wildhaven"` (`new_game_screen.gd`; pre-filled so a kid can press *Let's go!* without typing; content-writer's checklist applies to the string itself) · `DECIDED 2026-08-09 (→ D-35) — MIN_SAVED_WORLD_TILES / MAX_SAVED_WORLD_TILES = 1 / 128` (`world_root.gd`; the bound a loaded save's `width`/`depth` must fall inside before it is trusted to build a grid, vs Open Question **#18**'s ~128×128 cap — same number row 13's world cap rules in D-38; outside the bound the load falls back to the preset's own dimensions rather than clamping the file's number) |
| `implementation_location` | **New:** `project/scripts/save/` — `save_store.gd` (disk: list/read/atomic write/name→slug), `world_snapshot.gd` (schema: `capture`/`apply`/`can_apply`/`_migrate`), `autosave.gd` (the four-reason vocabulary + interval), `game_session.gd` (static menu→world hand-off). `project/scripts/definitions/world_preset.gd` + `project/data/presets/meadow_start.tres` (the one v1 preset). `project/scripts/menu/` + `project/scenes/menu/` — `new_game_screen`, `load_game_screen`, `leave_overlay` (`.gd` + `.tscn` each). **Modified:** `project/scripts/world/world_root.gd` (`_ready()`'s session branch, save-identity fields, the dimension bounds above), `project/scripts/simulation/habitat_simulation.gd` and `home_site_registry.gd` (restore seams — see the claims table), `project/scripts/title_screen.gd` (Load Game button), `project/scenes/TitleScreen.tscn`, `project/scenes/Main.tscn` (the `LeaveOverlay` instance), `project/tests/test_mode_tap_model.gd` (click-swallow scan widened to the whole `Main.tscn` tree), `scripts/run-tests.sh` (now fails a suite that prints `SCRIPT ERROR`, infra work surfaced during this row, not scoped to it alone). |
| `validation_status` | **qa-engineer's field; recorded here on their behalf as the measured result of this run, not a re-ruling.** `bash scripts/run-tests.sh`: **57 suites, 57 passed** (baseline before this row was 51). New suites: `test_save_store`, `test_world_preset`, `test_restore_seams`, `test_world_snapshot`, `test_save_round_trip`, `test_autosave_triggers`. The local count includes one untracked suite belonging to another workstream (`test_avoids_distance_keeping.gd`); a clean checkout of this branch runs 56. |
| `human_gate` | **2026-08-09 — full gate, everything approved. Supersedes the 2026-08-02 partial gate.** The two items that gate was waiting on both landed and both passed on a real build: **`LeaveOverlay`** — the save writes before the confirmation prompt appears, **Stay** returns to a world that is still simulating, **Yes** returns to the title screen — **approved**. **A displacement left pending across a reload** (D-32's fix) — survives the reload and resolves correctly afterward — **approved**. Everything the 2026-08-02 partial gate already covered stands: New Game → preset → name → in; faithful reload; Load correctly disabled with no saves; two typed-twice names producing two worlds; a pending arrival surviving a quit (D-31); a roamed position surviving a reload; a rabbit genuinely moving in. **Both open items the 2026-08-02 playtest surfaced are now ruled, not just disclosed (→ D-39):** the rabbit's `tiles_per_individual = 4` (D-27 #2) stands **unchanged, no revisit** — the human's call, having seen the ~4× harder attraction threshold in a real build. The `_ever_hosted` save-corruption defect (a hand-edited save naming a nonexistent species permanently pollutes the Species Hosted counter) is **triaged and ruled non-blocking** — real but low-severity (only reachable via a corrupt or hand-edited file, never normal play) — **deferred to a post-Tier-1 fix**, not required before this row closes. **One disclosure still stands, unchanged, and does not block this gate:** `mist_reveal` has no call site — mist (row 13) is unbuilt, so that autosave reason is verified by contract (`test_autosave_triggers.gd`) rather than by a live caller; row 13 adds the one missing call site when it builds. |
| `depth` | thin |
| `est_hours` | 2–3 |
| `actual_hours` | — |
| `status` | ✅ — **the floor is closed: built, headless-validated, and played by a human end to end (2026-08-09, full gate — see `human_gate`).** New game → preset → name → in; Load reads a plain named list; autosave fires on `interval` (at attach, then every 90 s), `move_in`, and `exit_to_menu`, all measured in `test_autosave_triggers.gd`; `mist_reveal` is contract-verified only, pending row 13. Save/load round-trips the terrain grid, buildings, Wood, home sites, live resident positions, and `species_hosted` (`test_save_round_trip.gd`). No save button anywhere (Pillar 1 holds). `LeaveOverlay` and a displacement pending across a reload — the floor's last two unwalked behaviours — are now human-verified. The rabbit attraction-threshold question and the `_ever_hosted` defect are both ruled (D-39), not merely disclosed. |

## Row 2 — Camera & modes

> **REOPENED 2026-08-14 (→ D-41), REVERSING D-33.** Everything below this note, down to and
> including the "REOPENED 2026-08-03 (→ D-33)" note, describes the first-person walk camera —
> **it no longer describes the shipping camera**, kept as-is rather than deleted because it
> genuinely happened and the record reads honestly. D-41 replaces first-person outright with a
> fixed orthographic camera at `yaw = 45°`, `pitch ≈ -26.565°` (`IsoCameraFraming`), matching
> the project's original 2D mockups' isometric convention rather than either camera tried
> before it. Movement reverts to D-13's pan/zoom model; nothing walks. Full rationale,
> including the measured finding that camera angle alone does not fix the tree-occlusion
> problem D-33 was built to solve (a headless AABB-ray probe found the new angle occludes
> about as much of the ring adjacent to a forest block as the original problem, ~11-12%,
> once the canopy is back to full size — the canopy squash was doing the real work, not the
> pitch), is in D-41.
>
> **`implementation_location`:** `project/scripts/ui/camera_rig.gd` (full rewrite — the
> first-person camera is fully replaced, not extended), `project/scripts/world/
> iso_camera_framing.gd` (new — the shared orthographic framing math both the live camera and
> the save-thumbnail capture use), `project/scripts/world/occlusion_fader.gd` (new — the
> bounded, per-resident selective transparency fade that replaces camera angle as the
> occlusion fix), `project/scripts/ui/tap_router.gd` (mouse-capture gating and the
> `MAX_INTERACTION_RANGE` "walk closer" gate both removed — cursor-position targeting,
> unconditional, the same D-13 model), `project/scripts/ui/game_hud.gd` (the hotbar's own
> mouse-capture gate removed, same reason). **Deleted:** `project/scripts/world/
> overview_camera.gd` (`OverviewCameraRig` — its Home-peek role folds into the live camera
> itself, since it is now the same fixed-heading orthographic type the peek needs; its
> save-thumbnail role is superseded by a plain, script-less `Camera3D` configured fresh by
> `IsoCameraFraming` on every capture), `project/tests/test_crosshair_targeting.gd` (its whole
> premise — a bounded interaction range plus a `Crosshair` reticle rendering valid/invalid — is
> structurally gone: the range is deleted and the reticle self-suppresses forever, since it
> gates on `Input.mouse_mode == MOUSE_MODE_CAPTURED`, which the new camera never sets).
> **Kept, not touched, though D-33 added them:** `TapRouter.attach()`'s `Crosshair` parameter
> and `is_crosshair_valid()`/`_refresh_crosshair_state()` — still consumed by a real test
> (`test_tap_router_cursor.gd`); only the reticle's own visibility is permanently dark, not the
> validity computation feeding it. `TerrainView.BOUNDARY_WALL_THICKNESS` — D-33's movement-
> blocking walls that used to consume it are gone (nothing walks), but `mist_boundary.gd`
> reads the same constant externally to size its own, unrelated visual curtain panels.
>
> **`constants`:** `IsoCameraFraming.YAW_DEGREES = 45.0`, `PITCH_DEGREES = -26.565` — derived,
> not chosen, from the mockup generator's own 2:1 tile-diamond ratio (`archive/images/mockups/
> generate.py`'s `TILE_W=96`/`TILE_H=48`), the standard pixel-art isometric convention.
> `CameraRig.ZOOM_MIN_TILES = 4.0`, `ZOOM_DEFAULT_TILES = 14.0`, `ZOOM_STEP = 1.15`,
> `KEY_PAN_TILES_PER_SECOND = 16.0`, `OVERVIEW_MARGIN = 1.06`, `RECENTRE_BEGIN = 0.60` — all
> carried over unchanged from D-29's already-playtested values as a starting point, **not
> re-validated under orthographic projection** (a different zoom mechanic — frustum `size`,
> not distance+FOV) — flagged for a real playtest, same as D-29's own numbers once were.
> `OcclusionFader.RESIDENT_CHECK_RADIUS_TILES = 3`, `FADE_HYSTERESIS_FRAMES = 6`,
> `fade_alpha = 0.9` — the human's own stated starting point for the alpha value specifically,
> explicitly flagged as needing further tuning against real play, not a settled number.
>
> **`validation_status`:** `bash scripts/run-tests.sh` — 68/70 suites passing (one fewer total
> than the prior first-person count: `test_crosshair_targeting.gd` deleted, its whole premise
> gone). The two remaining failures are both pre-existing and unrelated, confirmed
> independently across multiple task reviews during this rebuild: `test_placeable_schema`
> (a content-pipeline gap — `house.tres`'s `fact_text` still placeholder-prefixed, nothing to
> do with the camera) and `test_save_thumbnail` (this sandbox has no real rendering backend —
> headless dummy driver, confirmed three independent ways during the original spike this
> decision grew out of — so a captured save-thumbnail `Image` cannot be asserted non-blank
> here; needs a real display/GPU environment to fully verify, not a code defect).
>
> **`human_gate`:** not yet run. The zoom-range re-tuning, the occlusion-fade scope/tuning, and
> the tree-canopy asset fix are all named above as open, human-owned calls this decision
> deliberately did not make — this row's gate should cover all three, not just "does the new
> camera work."
>
> **`status`: 🚧 — reopened, not closed.** Built and headless-validated against the plan above;
> not yet human-played. The prior ✅/🚧 history below this note is superseded, not deleted.
>
> **BUILT AGAINST D-33, 2026-08-03 — dispatched to gameplay-engineer, not this row's usual
> `owner_agent` (ui-engineer, below).** Flagged rather than silently overwritten: the human
> routed this specific rebuild here because it spans row 2's camera/tap files AND row 3's
> `terrain_view.gd` (the new walking-collision layer), which is gameplay-engineer's own
> already-claimed file — the human should reconcile `owner_agent` for future row-2 work, this
> entry does not change it unilaterally.
>
> **`implementation_location`, declared 2026-08-03:** `project/scripts/ui/camera_rig.gd`
> (rewritten — the OLD safety-rail camera's behaviour is fully replaced by D-33's first-person
> walk camera, not extended); `project/scripts/ui/tap_router.gd` (look-and-press crosshair
> targeting replaces cursor-position targeting; `handle_tap()`/`refresh_preview()`'s own
> position-driven signatures are unchanged); `project/scripts/ui/game_hud.gd` /
> `project/scripts/ui/game_ui.gd` (Home button changed from a single `pressed` signal to
> `button_down`/`button_up`, so gdd.md's "Home key, held (and a HUD button)" gets real hold
> semantics on the button too, not just the key). **Added:** `project/scripts/world/
> overview_camera.gd` (`OverviewCameraRig`, a from-scratch file but NOT new logic — it ports the
> OLD `camera_rig.gd`'s rail-2 frustum search verbatim rather than re-deriving it, per D-33's
> explicit instruction; instantiated twice in `scenes/Main.tscn`, once visible for the Home-key
> map peek and once hidden inside a `SubViewport` for the save-thumbnail capability).
> **Cross-claim into row 3's directory (named, not silent, same pattern this row's own
> 2026-08-02 entry used):** `project/scripts/world/terrain_view.gd` gained the whole-tile
> movement-blocking collision layer D-33 asked for (`PICKING_COLLISION_LAYER` /
> `MOVEMENT_BLOCKING_COLLISION_LAYER`, `IMPASSABLE_TERRAIN_IDS`) — first-person walking's
> collision needs, not a Terraform-mechanics change; `terrain_view.gd`'s own tile-visual/tag
> logic is untouched. **Deleted:** `project/scripts/world/camera_angle_spike.gd` (+ `.uid`) and
> its `CameraAngleSpike` node in `scenes/Main.tscn` — the spike this row's rebuild came from,
> now fully promoted or (Spike A) discarded per the task brief.
>
> **`constants`, DECIDED 2026-08-09 (→ D-36) — every value below approved as proposed, carried
> over from the spike's already-flagged first-guess values, not newly invented:**
> `CameraRig.EYE_HEIGHT = 0.9` (gdd.md's "a villager stands ~1 tile tall" at
> `WorldGrid.TILE_SIZE = 1.0`, eye height a fraction under full height — not a measured
> eye-to-crown ratio) · `CameraRig.MOVE_UNITS_PER_SECOND = 4.0` (a walking pace, judgment call) ·
> `CameraRig.MOUSE_SENSITIVITY_DEG = 0.15` (degrees per pixel, judgment call) ·
> `CameraRig.PITCH_LIMIT_DEGREES = 80.0` (spike's brief: "something sane, e.g. ±80") ·
> `CameraRig.BODY_CAPSULE_RADIUS = 0.3` / `BODY_CAPSULE_HEIGHT = 0.8` (the player's own collision
> capsule, sized to roughly fit under `EYE_HEIGHT`, no GDD number) ·
> `TerrainView.IMPASSABLE_TERRAIN_IDS = ["forest", "rock", "water"]` (the VALUES are D-33's own
> stated scope, not a proposal; the MECHANISM — a hardcoded id list in view code rather than a
> `TerrainDefinition.blocks_movement` field — **ruled D-36: stays a hardcoded list**, ships
> fastest; revisit only if more impassable terrain types are added) ·
> `TerrainView.MOVEMENT_BLOCK_HEIGHT = 2.5` (collision-box height for a blocking tile, generous
> first-pass judgment call, not a GDD number) · `ThumbnailViewport` size `(480, 270)` in
> `scenes/Main.tscn` (save-thumbnail render resolution, no GDD number, placeholder). Every OLD
> constant this note's own reopening block already listed as superseded stays superseded; none
> are revived.
>
> **`validation_status` addendum, 2026-08-03:** `bash scripts/run-tests.sh` — **53/57 suites
> green, same 4 failing as expected and for the reason expected: every one depends on the OLD
> camera's API surface** (`CameraRig.initialize()`/`.zoom_tiles()`/`.PITCH_DEGREES` etc., or a
> tap driven at a screen position computed against the old top-down framing). `test_camera_rails`
> (wholesale — every assertion is an OLD-camera invariant, e.g. `PITCH_DEGREES == -45`,
> `ZOOM_MIN_TILES`, the no-rotation check, the pan/zoom clamps: none apply to a first-person
> camera and this suite needs a full replacement, not a patch) · `test_mode_tap_model.gd`
> (`_check_priority_rule_in_all_three_modes` drives taps at resident screen positions computed
> under the old camera's default framing, and calls the removed `.initialize()`/`.zoom_tiles()`
> contract) · `test_resident_lookup.gd` (hard `SCRIPT ERROR: Nonexistent function 'zoom_tiles'`
> at `_land_a_wandering_rabbit`, `test_resident_lookup.gd:264` — stops that helper outright) ·
> `test_fact_card.gd` (`TAP-TO-REPLAY` drives an Inspect tap at a resident's OLD-camera screen
> projection, which now resolves to a different tile/miss under the new camera's starting
> position and orientation). **Not fixed here per the task brief** — a qa-engineer pass rewrites
> these against the new camera; `handle_tap()`/`refresh_preview()`'s own position-driven public
> signatures are unchanged, so once a test computes the RIGHT screen position for the new
> camera, the rest of each suite's plumbing should still apply. Collision (whole-tile blocking,
> dynamic terrain-change updates), the map peek's camera swap, and the thumbnail camera's
> independent per-viewport framing were all sanity-checked with throwaway headless `SceneTree`
> scripts (deleted after) rather than added to the permanent suite, per the brief's "hand-written
> throwaway... delete them after."
>
> **REOPENED 2026-08-03 (→ D-33).** Everything below this note describes the OLD fixed-pitch
> pan/zoom camera and its 2026-08-02 full human gate — kept as-is, not deleted, because it
> genuinely happened and the record reads honestly. **It no longer describes the shipping
> camera.** D-33 reverses D-13 outright: the camera becomes a true first-person walk camera
> (free mouse-look, WASD ground-level walking), driven by real tree assets occluding sightlines
> at the old 45° pitch and the human's own read that the target age (kids who already play
> Minecraft) can handle the added complexity. A same-day spike comparison (`camera_angle_spike.gd`
> — low-angle pitch vs. true first-person) settled it in first-person's favor.
> `constants` below (`ZOOM_MIN_TILES`, `ZOOM_STEP`, FOV, etc.) are superseded per D-33 — none
> describe the new camera. See D-33 for the proposed Home-key map-peek ("never lost" replacement)
> and look-and-press tap targeting.
>
> **NEW HUMAN GATE, 2026-08-09 — full gate for the D-33 first-person rebuild, everything
> approved, no retunes.** Mouse-look feel at `MOUSE_SENSITIVITY_DEG = 0.15` — **approved**,
> reads right for the target age. Walk pace (`MOVE_UNITS_PER_SECOND = 4.0`), eye height
> (`EYE_HEIGHT = 0.9`), and pitch limits (`PITCH_LIMIT_DEGREES = ±80°`) — **approved**,
> comfortable view, no clipping through terrain or geometry. Collision against
> `IMPASSABLE_TERRAIN_IDS = [forest, rock, water]` at `MOVEMENT_BLOCK_HEIGHT = 2.5` —
> **approved**, stops the player cleanly at every approach angle tried. The Home-key map peek
> (`OverviewCameraRig`) and the save-thumbnail capture (`ThumbnailViewport` at 480×270) —
> **approved**, both read correctly under the new camera. **This closes the human-gate half
> of this row's reopening.** One item stays open and unrelated to this gate: 4 suites
> (`test_camera_rails.gd`, `test_mode_tap_model.gd`, `test_resident_lookup.gd`,
> `test_fact_card.gd`) still fail purely because they were written against the OLD camera's
> API/framing — a QA rewrite, not a design question, and this human gate does not wait on it.
> **`status` stays 🚧 until that rewrite lands and the suite is green again** — this row's own
> ✅ criterion is built + headless-validated + human-played, and headless validation is not yet
> back to green.
>
> **DISCLOSED GAP, found 2026-08-09, after the gate above — real, confirmed, not yet fixed.**
> The Tab-toggle between "look" (captured, hidden pointer, drives mouse-look and the
> crosshair tap) and "point" (visible pointer, for HUD/palette clicking) has **zero on-screen
> indication anywhere** — it exists only in code comments (`camera_rig.gd`, `tap_router.gd`).
> Confirmed live: the human tried to terraform, could not (the HUD is unusable while the
> pointer is captured, which is the default state), and only succeeded once told to press Tab
> first — then confirmed it "is not intuitive at all." **Not a code defect** — the mechanism
> works exactly as D-33 designed it, and painting/placing/removing all still pass headlessly.
> This is an onboarding/discoverability hole opened by D-33's input-model change, invisible to
> every prior human_gate because none of them exercised "select a terrain and paint a tile"
> under the new capture split. **Logged here, not fixed** — the human's call on treatment
> (a contextual hint, a persistent HUD label, or something else) is still open. Does not
> reopen this row's gate above or change `status`; flagged so it isn't lost.

| Field | Value |
|---|---|
| `thin_form` | Safety rails and the three-mode tap model ship whole (Pillar 3) — modes, tap semantics, priority rule. Pan/zoom clamped, no easing; Inspect's taps thin except tap-to-tend. |
| `invariants` | The three-mode tap model and the camera safety rails are **not depth axes** (spec.md). The one-interaction-pattern pillar is absolute — "pick a mode (or none), then tap." The animal-wins-the-tap priority rule ships with it. |
| `constants` | **REVERSED 2026-08-01 (→ D-29): zoom clamp, close end = 4.0 tiles across** (was 7.0) — the human played the build and found 7.0 too far from the animals. **Landed in code 2026-08-01 (`CameraRig.ZOOM_MIN_TILES == 4.0`, pinned by `test_camera_rails.gd`), and gdd.md → World Structure now reads "a continuum from ~4 tiles close" — both confirmed 2026-08-02, nothing pending.** · **default zoom = 14.0 tiles across — DECIDED 2026-08-01 (→ D-29)** (gdd.md's "~12–15-tile street-level default", midpoint; `CameraRig.ZOOM_DEFAULT_TILES`). **Both are measured on the EAST–WEST axis**, which is the only axis a fixed 45° pitch leaves undistorted — a north–south tile count would depend on the pitch and would not mean what the GDD's sentence means · **the far end of the zoom range is deliberately NOT a constant**: it is whatever distance frames the whole revealed world, binary-searched against the engine's own frustum each time (`CameraRig._overview_distance()`), so safety rail 2 ("full zoom-out always frames everything") and "as far out as the player can go" are the same number by construction and cannot drift apart · **camera fov = 50° — DECIDED 2026-08-01 (→ D-29)** (no GDD value; set on the `Camera3D` node in `scenes/Main.tscn`, not in code, so it is tunable in the editor. Narrower than Godot's 75° default for a flatter, more picture-book-map read — art.md's target) · **zoom step = 1.15× per wheel notch — DECIDED 2026-08-01 (→ D-29)** (`CameraRig.ZOOM_STEP`; **MEASURED 2026-08-02, supersedes both prior figures: 18 notches** across the whole range at viewport (1152.0, 1152.0), 4.0 tiles out to 49.3 (`test_camera_rails.gd`'s `_measure_zoom_notches()`) — replaces both the stale "measured 2026-07-27: 14 notches" (taken before the 7.0 → 4.0 retune widened the range) and the "~18 arithmetic estimate" that stood in for a measurement in between; 18 falls inside this row's own 12–24 sanity band, so no `ZOOM_STEP` retune is indicated) · **keyboard pan = 16 tiles/s at default zoom, scaled by the zoom ratio — DECIDED 2026-08-01 (→ D-29)** (`CameraRig.KEY_PAN_TILES_PER_SECOND`) · **overview framing margin = 1.06 — DECIDED 2026-08-01 (→ D-29)** (`CameraRig.OVERVIEW_MARGIN`; keeps the world's edge tiles off the screen edge. **It is also what funds panning at full zoom-out**: the slack it buys is the only room the pan clamp has left there — measured at ±1.05 tiles on a 36×36 world — so raising it widens both the border *and* how much the map can be nudged when fully zoomed out) · **recentre begin = 0.60 of the overview distance — DECIDED 2026-08-01 (→ D-29)** (`CameraRig.RECENTRE_BEGIN`; where the pannable rectangle starts shrinking toward the world centre as the player zooms out. **A feel number only** — it spreads the recentring over the last ~4 wheel notches instead of one, and safety rail 2 holds at any value below 1. At 0.60 the ~12–15-tile street-level default and everything closer still pan to the true world edge) · **resident tap radius = max(44 px, min(0.8, 0.5) × one tile on screen)** — three terms in `ResidentPicker` in `project/scripts/world/` (one copy, not two; carried there verbatim when `resident_index.gd` was deleted). This is the number that implements gdd.md's "generous animal hitboxes beat ambiguous taps for young kids", and the floor is a *pixel* measurement on purpose — generous has to mean generous to a finger, not to a world unit. **`MIN_TAP_RADIUS_PIXELS = 44` — marker removed 2026-07-28 (D-27 #3 ruled it kept unchanged; the floor is for far zoom and is what the cap must not eat)** · `TAP_RADIUS_TILE_FRACTION = 0.8` stays `PROPOSED (2026-07-27)` — a *feel* number now bounded by the cap · **`DECIDED 2026-08-02 (playtest gate) — MAX_TAP_RADIUS_TILE_FRACTION = 0.5`, the cap D-27 #3 ordered and the value the 2026-08-02 playtest gate approved, no retune.** The fraction is applied as a RADIUS, so 0.8 produced a target **1.6 tiles wide** — wider than the tile the animal stands on (measured 162.8 px against a ~203 px tile at street zoom), which is why a villager on its House won the tap on the adjacent field and made the floor's likeliest displacement unreachable while the family was home. 0.5 makes the hitbox exactly one tile wide: the animal still wins every tap on its own tile and never one on a neighbour's. **Kept as a second constant rather than a retune of the first, deliberately** — the cap is a structural bound and the fraction is taste, the same shape as `ResidentRoamer`'s waypoint radius being clamped to the home site's own radius. **Tune against the relation, not an absolute:** the operative pixel radius is viewport-dependent (~110 px in a real window vs 64 px headless at the same zoom), which is why the cap is expressed in tiles. **Measured after the change** (headless, black-box — probing outward from the tap point until `pick()` stops resolving): at closest zoom the hitbox is **0.999 tiles wide** (was 1.6), at the 14-tile street default the 44 px floor binds in the small headless viewport and gives **1.04 tiles**, and at full zoom-out the 44 px floor still holds against a 23.5 px tile · **preview poll = 0.1 s — DECIDED 2026-08-01 (→ D-29)** (`TapRouter.PREVIEW_POLL_SECONDS`; the "cursor rate" in gdd.md → Performance's bound, made into a number. Caps the preview at ten screen→grid raycasts a second however fast the mouse moves; the capacity read behind it runs only when that raycast lands on a *different* tile or an edit invalidated the last one) · **preview type size = 26 px — DECIDED 2026-08-01 (→ D-29)** (`UiPalette.FONT_PREVIEW`; between the HUD counter and the fact card's body — a sentence read at a glance while the hand is busy) · **auto-select the first palette entry on entering Terraform/Build — DECIDED 2026-08-01 (→ D-29)** (`GameHud.AUTO_SELECT_FIRST_ENTRY = true`; a judgment call, not a tuning number — see the build report. gdd.md's beat 3 reads "pick a terrain, tap a tile", but "nothing happened" reads as broken to a six-year-old and every floor terraform is free and reversible) · **pan bounds are not a constant**: `CameraRig.world_bounds()` derives them from `WorldRoot.grid_size()`, and it is the single function row 13's mist reveal will change · **LOD tier thresholds: not started** — no LOD exists (`TerrainView` draws every tile), and it is row 2 *depth*, not floor · **Ten constants in five groups, ruled at the 2026-08-02 playtest gate (Appendix A) and approved at the values already shipped in code — no retune, markers promoted to `DECIDED 2026-08-02 (playtest gate)` in the source files:** `READOUT_SECONDS = 4.0` (how long the Inspect tile readout holds; `game_hud.gd`) · `PALETTE_BUTTON_WIDTH = 148.0` / `PALETTE_BUTTON_HEIGHT = 96.0` (palette button footprint in px; `game_hud.gd`) · `ACCEPT_DURATION = 0.32` / `SOFT_DURATION = 0.40` (cue lifetimes in seconds; `tap_cue.gd`) · `ACCEPT_RADIUS_START = 14.0` → `ACCEPT_RADIUS_END = 46.0`, `SOFT_RADIUS = 22.0`, `RING_WIDTH = 5.0` (the cue's ring geometry in px; `tap_cue.gd`) · `BODY_CENTRE_HEIGHT = 0.45` (how far above ground the tap point sits, so the target lands on the animal rather than its feet; `resident_picker.gd`) · **`TAP_RADIUS_TILE_FRACTION` is currently INERT (recorded 2026-08-02):** `min(tile × 0.8, tile × 0.5)` means the cap binds at every zoom, so the fraction has no effect at its current value or at any value ≥ 0.5. **Human ruling 2026-08-02: both constants kept, the pin documented** — the bound is structural, the value inside it is taste, and collapsing them would leave the neighbouring tile unprotected if generosity is ever tuned down. The number the playtest actually rules on is the cap, 0.5. · **Cross-claim recorded 2026-08-02 (row 3's closeout playtest, Appendix A item 4), the same pattern row 2's own closeout used for its cross-claims into row 3's territory:** the remove-tool reset in `game_hud.gd:197` — entering a mode always starts on that mode's content brush, never on the remove tool, so switching Terraform → Build → Terraform loses the remove selection every time — had no recorded ruling anywhere (the `PROPOSED` marker at that line had stood unruled since it was written). **Human ruling 2026-08-02: approved, kept exactly as shipped** — losing the remove selection on every mode switch is the safer default, not an annoyance; a player can't carry a "delete" tool around by accident. No behaviour change. Recorded here, not in row 3, because `game_hud.gd` is row 2's claimed file. If a future gate reverses this to "sticky," that is a `game_hud.gd` logic change for a later dispatch, not this one. |
| `implementation_location` | **declared 2026-07-27** — `project/scripts/ui/`: `camera_rig.gd` (the camera and all three safety rails; **replaces the deleted 18-line `project/scripts/camera_rig.gd`**), `tap_router.gd` (the whole gameplay input path — one left-click, resolved against the mode), `game_hud.gd` (the corner-anchored HUD, mode switch and the data-driven palettes), `game_ui.gd` (wiring only), `tap_cue.gd` (the soft cue — the entire "no" vocabulary), `ui_palette.gd` (every colour, radius and type size in one file). `project/scenes/ui/GameUI.tscn` is the scene, instanced into `scenes/Main.tscn`. **Deleted here:** `project/scripts/world/debug_paint_input.gd` and its node in `Main.tscn` — the temporary input path this row replaces. **Updated 2026-07-28:** `resident_index.gd` (screen-space hit-testing against **arrival-time** positions) is **deleted** — residents wander now, so the tap resolves through `WorldRoot.resident_record_at()` over live positions, and the UI keeps no resident list at all (asserted structurally in `test_fact_card.gd`: `GameUI` must declare no `residents` property). Added: `neighborhood_preview.gd` (row 6's live preview, computed — see row 6), driven from `tap_router.gd`'s cursor poll and rendered by `game_hud.gd` into `HUD/PreviewPanel`. **Updated 2026-08-01 (→ D-29, implementing rulings #3 and #7):** `camera_rig.gd`'s `ZOOM_MIN_TILES` retuned 7.0 → 4.0, with its own and `ZOOM_STEP`'s notch-count comments corrected to match (arithmetically ~18 notches now, was measured ~14, since the range widened — not re-measured headless, flagged in the code comment for a real re-measurement); `tap_router.gd`'s `handle_tap()` is now mode-aware — the animal-wins-the-tap priority rule (`resident_record_at()` asked before the grid) fires **only in Inspect**, and in Terraform/Build the resident query is not run at all, so the tile action under the cursor always wins regardless of resident-hitbox overlap. No file added or removed; both edits land inside this row's already-claimed directory. **Cross-claim recorded 2026-08-02:** row 2's three tap-radius constants and `BODY_CENTRE_HEIGHT` live in `project/scripts/world/resident_picker.gd`, a directory the claims table assigns to rows 1/3/6/9/13 and not to row 2. They arrived there when `resident_index.gd` was deleted and the tap moved onto live resident positions — correct, since there must be one copy. Named here so the cross-claim is visible rather than silent; row 2 only ever edits comments in that file. |
| `validation_status` | **pass (2026-08-01, updated → D-29) — ZOOM_MIN_TILES RETUNED AND THE TAP-PRIORITY RULE MADE MODE-AWARE, BOTH VERIFIED, INCLUDING AGAINST A LIVE MOVING RESIDENT.** `bash scripts/run-tests.sh` **51/51 green, 1655 assertions** (50/50, 1609 before this pass — the one new suite, `test_save_store.gd`, landed from a concurrent row-1 dispatch during this pass and is not this row's). **Measurement update, 2026-08-02, not a re-validation of this row's behaviour:** `bash scripts/run-tests.sh` run today shows **57 suites, 57 passed, 2,131 assertions** (2,130 as of the initial 2026-08-02 measurement; +1 from this same dispatch's fix-wave pinning `test_camera_rails.gd`'s notch count against the exact viewport `ZOOM_STEP`'s comment quotes, closing the last gap this row's premise exists to close) — the 2026-08-01 counts above are stale by the growth of the suite since, not by anything wrong with this row; recorded here only because closing exactly this kind of drift is this dispatch's subject. **Investigated first, in isolation, under a bounded timeout, per the build report's explicit flag:** `test_event_driven_simulation.gd`'s natural-frame wander check span a null-reference every frame under the new `wild_grass` default — confirmed real (reproduced the hang standalone before touching anything else), root-caused to the fixture painting only `cover` and relying on the old ambient `grass` for the rabbit's OTHER need, re-pointed to paint both explicitly, and hardened so a future precondition failure now fails clean instead of spinning (68/68 green after). **`test_camera_rails.gd` re-pointed** (stale pin `ZOOM_MIN_TILES == 7.0`; confirmed `CameraRig.ZOOM_MIN_TILES == 4.0` exactly by reading source): 57/57, everything else — `ZOOM_DEFAULT_TILES`, FOV, `ZOOM_STEP`, `KEY_PAN_TILES_PER_SECOND`, `OVERVIEW_MARGIN`, `RECENTRE_BEGIN`, both safety rails, pitch — confirmed unmoved. **`test_mode_tap_model.gd`'s `_check_priority_rule_in_all_three_modes` rewritten** (91 → 99 assertions; it asserted the OLD uniform rule, a stale pin of the exact conflict D-29 #7 resolves): Inspect keeps the animal-wins-the-tap behaviour verbatim, unchanged; Terraform and Build are new — a resident is placed directly on a tile the tile action would succeed on, `resident_record_at()` confirms it is really standing there, the tap is driven, and the tile action (paint to water / place a House) is asserted to succeed WITH NO FACT CARD OPENING — proving the resident query was never run, not merely that it lost a priority tie. **`test_resident_lookup.gd`'s equivalent section rewritten** (60 → 63 assertions) against a resident actually WALKING under its own `ResidentRoamer` via the real causal path, not a teleported stub: Terraform/Build now paint/place through the live, moving resident with no card opening; Inspect is unchanged (the animal still wins, including that the stale arrival point misses while the live point hits). Both suites' negative controls (resident removed → the identical tap performs the tile action) still hold. `terrain_options()` still includes `wild_grass` — **no longer a defect: CLOSED BY RULING 2026-08-02, the human ruled it a supported paintable terrain that stays in the palette (row 3's `validation_status` carries the full ruling)**. Inspect still has no House card — unchanged, still open, and depth rather than floor. |
| `human_gate` | **2026-08-02 — full gate, everything approved, nothing rejected. Supersedes the 2026-08-01 partial gate.** The human ran the Appendix A playtest script once, in order. The two items the 2026-08-01 gate was waiting on both landed and both read right: **close zoom** — `ZOOM_MIN_TILES = 4.0` (`camera_rig.gd`), the value retuned under D-29 #3 after 7.0 was rejected on 2026-08-01 as too far from the animals — **approved**, close enough to the animals now. **Mode-aware tap priority** (D-29 #7) — Terraform/Build act on the tile under an animal with no fact card opening, Inspect gives the animal the tap — **approved**, the division feels right in the hand. Every other item on the script was also approved, all at the values already shipped, no retune: **tap generosity** — `MAX_TAP_RADIUS_TILE_FRACTION = 0.5` and `BODY_CENTRE_HEIGHT = 0.45` — **approved**. **HUD feel** — `READOUT_SECONDS = 4.0`, `PALETTE_BUTTON_WIDTH = 148.0` / `PALETTE_BUTTON_HEIGHT = 96.0`, and `AUTO_SELECT_FIRST_ENTRY = true` (re-confirmed) — **approved**. **Cue feel** — `ACCEPT_DURATION = 0.32`, `SOFT_DURATION = 0.40`, `ACCEPT_RADIUS_START = 14.0` → `ACCEPT_RADIUS_END = 46.0`, `SOFT_RADIUS = 22.0`, `RING_WIDTH = 5.0` — **approved**; the refusal reads as "not there," not failure. **Rails re-walk** — the three safety rails hold, plus `OVERVIEW_MARGIN = 1.06`, `RECENTRE_BEGIN = 0.60`, `KEY_PAN_TILES_PER_SECOND = 16.0`, `ZOOM_DEFAULT_TILES = 14.0`, and fov 50° (all re-confirmed) — **approved**; the human never felt lost. No constant changes as a result of this gate — every number stands exactly as shipped. |
| `depth` | not started |
| `est_hours` | 3–5 |
| `actual_hours` | — |
| `status` | 🚧 — **D-44 (camera rotation, 90° steps) also applies to this row and extends its still-pending human gate**, which must now additionally cover the rotation control itself (does it feel right, do `↺`/`↻` match the direction each turns), whether the sun-lock fix keeps lighting consistent across all four headings in real play, and D-44's own flagged "not exhaustively art-reviewed" open item. **REOPENED 2026-08-14 (→ D-41), REVERSING D-33: the first-person walk camera this cell's history describes is no longer what ships.** See the note above the table for the full account — a fixed orthographic camera (`yaw=45°`, `pitch≈-26.565°`) plus a bounded per-resident occlusion fade replace it. Headless-validated (68/70 suites; the two failures are pre-existing and unrelated); not yet human-played — the zoom-range re-tuning, the occlusion-fade scope/tuning, and the tree-canopy asset fix are all still open, human-owned calls. Nothing below this line has been rewritten, only superseded twice over. Was (2026-08-03, → D-33): 🚧 — **REOPENED 2026-08-03 (→ D-33): the ✅ below closed the OLD camera, which this row no longer ships.** See the note above the table for why and what replaces it; nothing in this cell has been rewritten, only superseded. Was: ✅ — **the floor is closed: built, headless-validated, and played by a human end to end (2026-08-02, full gate — see `human_gate`).** `camera_rig.gd` is the whole camera: fixed −45° pitch set once with no yaw or roll term anywhere in the file, right-drag and WASD/arrow pan, cursor-centred wheel zoom, and all three safety rails as hard guarantees rather than tuned behaviours. Rails 1 and 2 are **one mechanism** — the pannable rectangle is a function of zoom, shrinking as the camera pulls back onto exactly the rectangle the frustum search has verified frames the world from every one of its corners — which is what closed the rail-2 defect QA reported on 2026-07-27 (max zoom-out *by the wheel* while panned into a corner used to leave the world off screen; distance and focus were being treated as independent when `_overview_distance()` is searched for a camera at the bounds centre and so was never independent of it). Rail 3 is on the Home key and the HUD button, one function. The three-mode tap model ships whole: `game_hud.gd`'s mode switch with data-driven Terraform/Build palettes, `tap_router.gd` resolving one left-click against the mode with the animal-wins-the-tap priority rule in all three, and `tap_cue.gd` as the entire "no" vocabulary. **What is NOT here, correctly — depth, not floor, and neither blocks ✅:** no easing, no framing polish and no zoom-dependent detail — that is deliberately what this row's depth purchase buys; no LOD; and Inspect has **no House card** — `WorldRoot` exposes no `get_tile_building()`, so a tap on a placed building has nothing to read. **Closed 2026-07-28 — the stale-hitbox defect this row carried is gone.** `resident_index.gd` is deleted and the priority rule now resolves through `WorldRoot.resident_record_at()` against live resident nodes. The symptom it removes is worth naming, because it was not "the animal stops being tappable": at default zoom the effective radius is ~110 px, so an arrival-time index left a **ghost hitbox standing over the empty den** — a tap on bare ground opened a fact card and silently declined to paint the tile the player was aiming at — while the animal itself was tappable too. Measured over 120 s of real wander: the animal crossed a whole tap radius within ~4–8 s, reached 420–820 px from its arrival point, and in one of three runs **wandered back inside the stale hitbox by the end**, so the old code failed intermittently rather than cleanly. **Glyph moves to ✅, 2026-08-02:** the full human gate is recorded above, and it is what closes this row's floor. |

## Row 3 — Terraform

| Field | Value |
|---|---|
| `thin_form` | Five of six v1 terrains: grass, water, forest, rock, cultivated. Pricing and removal/refund policy ship whole. Cultivated because capacity reads cultivated tiles in radius; **rock as the v1 `cover` source** (#5 resolved). |
| `invariants` | Removal/undo & refund policy ships whole (grace window 100%, then ~50% recycle). The inert-land invariant: untouched revealed land emits no tags. Extending the ten-tag vocabulary is always a system-wide human decision. |
| `constants` | **cultivated_field cost = 2 Wood — DECIDED 2026-08-01 (→ D-29)** (#8; terrain.md's stated "~2 Wood / tile — fencing & tools"; lives in `data/terrain/cultivated_field.tres`, not in code) · **grass/water/forest/rock/wild_grass cost = 0 — DECIDED 2026-08-01 (→ D-29)** (not really tuning: terrain.md's decided pricing rule "nature is free", plus the free-Forest recovery guarantee; `TerrainDefinition.FREE_COST`) · **world start size = 36 × 36 tiles — DECIDED 2026-08-01 (→ D-29)** (#18; gdd.md → World Structure's stated "~36×36" baseline; `WorldGrid.DEFAULT_WIDTH` / `DEFAULT_DEPTH`) · **REVERSED 2026-08-01 (→ D-29): every tile starts as `wild_grass`, not `grass`** — landed 2026-08-01 (`WorldGrid.START_TERRAIN_ID`), verified inert on a fresh world (see validation_status); means the First 60 Seconds "open meadow" read now rests on `wild_grass`'s still-unresolved look-pass (Open Question #29) (not tuning, but a call worth seeing: gdd.md's First 60 Seconds beat 1 asks for "a small, calm, mostly-grass world … an open meadow, not an empty lot", and wild grass is what the **mist** reveals, row 13. The authored varied start is #10; `WorldGrid.START_TERRAIN_ID`) · **grace window = 12.0 s — DECIDED 2026-08-01 (→ D-29)** (#16; spec.md → Pacing Constants' "~10–15 s", midpoint; **defined once** in `SettlementWindow.GRACE_WINDOW_SECONDS` and read here, because spec.md gives the refund window and the settlement window one table row) · **recycle fraction = 0.5 — DECIDED 2026-08-01 (→ D-29)** (#16; gdd.md → Removal / undo & refund policy's stated "placeholder ~50%, tunable"; `RemovalLedger.RECYCLE_FRACTION`, applied as `floor(cost × fraction)` so a refund is never rounded up into a profit) · **free natural terrain refunds nothing — DECIDED 2026-08-01 (→ D-29)** (not tuning: gdd.md states it outright, and it falls out of `cost = 0` rather than being special-cased). **#5 and #17 are closed (→ D-25): tags are per-tile with no emission radius, and the brush is single-tap** — neither is a constant to decide |
| `implementation_location` | **declared 2026-07-27** — `project/scripts/definitions/terrain_definition.gd` (schema; **shared directory**, see the claims table), `project/data/terrain/` (six data entries), `project/assets/placeholder/{grass,water,forest,rock,cultivated_field,wild_grass}/` (grey-box tile visuals). **System landed 2026-07-27:** `project/scripts/world/world_grid.gd` (tile data + tag derivation + the free-Forest assertion), `project/scripts/world/terrain_view.gd` (visuals, colliders, screen→grid raycast), `project/scripts/world/world_root.gd` (the public API facade, root of `scenes/Main.tscn`), `project/scripts/world/debug_paint_input.gd` (**deleted 2026-07-27** by the rows 2/7 dispatch, exactly as designed — the shipping input path is now `project/scripts/ui/tap_router.gd`). `project/scripts/grid_manager.gd` (the 141-line pilot-1 spike) is **deleted**. **Removal/undo & refund declared 2026-07-28, before building:** `project/scripts/economy/removal_ledger.gd` (per-tile and per-building edit receipts + the refund arithmetic; owns `RECYCLE_FRACTION`, reads `SettlementWindow.GRACE_WINDOW_SECONDS`), with `WorldRoot.remove_at(x, z)` as the single public entry point alongside `paint_tile()` / `place_building()`, and `WorldGrid.clear_building()` as its raw half. **D-29 #1 landed 2026-08-01, same file, no new location:** `WorldGrid.START_TERRAIN_ID` is now `"wild_grass"`. **Verified consequence, not yet repaired — flagging for QA's step 4 before it re-runs the suite:** several suites that instantiate the real `Main.tscn` assumed the pre-existing ambient `grass` backdrop (either for `open_grass` tag counts feeding rabbit qualification, or for `House`'s `allowed_terrain = ["grass"]`) without painting it explicitly, and now fail or — in `test_event_driven_simulation.gd`'s natural-frame check specifically — spin for minutes spamming a null-reference script error every frame because no resident ever arrives to walk. Confirmed by isolating the single-line change: reverting only `START_TERRAIN_ID` to `"grass"` (nothing else touched) restores every one of these suites to green. Not fixed here — out of this dispatch's scope and QA's per the pipeline. **Repaired 2026-08-01/-02 — this paragraph is superseded, kept so the record reads honestly.** Every suite named above was re-pointed and is green; see `validation_status` for the specific counts (`test_removal_refund.gd` 77/77, `test_economy_rules.gd` 50/50, `test_causality_end_to_end.gd` 46→48). Full suite: 57/57. |
| `validation_status` | **pass (2026-08-01, updated → D-29) — `WorldGrid.START_TERRAIN_ID` IS NOW `wild_grass`, VERIFIED INERT ON A FRESH WORLD, AND THE REMOVAL/REFUND OBLIGATION STILL HOLDS AGAINST IT.** `bash scripts/run-tests.sh` **51/51 green, 1655 assertions** (50/50, 1550 before this pass). **Measurement update, 2026-08-02, not a re-validation of this row's behaviour:** `bash scripts/run-tests.sh` run today shows **57 suites, 57 passed, 2,131 assertions** (2,130 as of the initial 2026-08-02 measurement; +1 from this same dispatch's fix-wave, unrelated to this row — see row 2's `validation_status`) — the 2026-08-01 counts above are stale by the growth of the suite since, not by anything wrong with this row; recorded here only because closing exactly this kind of drift is this dispatch's subject. **Confirmed directly, not only by the data-layer suites:** a fresh `Main.tscn` reports every unpainted tile as `wild_grass` via `get_tile_terrain()`, and the existing `test_bare_tags_derivation.gd` (11 assertions, unchanged, still green) already asserts `derive_bare_tags(load_all())` is empty and that a synthetic `wild_grass` entry with any tag fails `validate()` — so the inert-land invariant (no tile emits a habitat tag until the player acts on it) holds on the shipped terrain, not just in the abstract. **`test_removal_refund.gd` re-pointed, count unchanged (77/77)** — several assertions' expected "before" terrain flipped from `grass` to `wild_grass` (that really is what an untouched tile is now), and the one tile this suite places a House on (`allowed_terrain = [\"grass\"]`, buildings.md) is now painted `grass` explicitly rather than borrowing the old ambient default. The grace-window boundary, the flat-recycle table, the mint-proof loop and "free terrain refunds nothing" are all unchanged and still green. **`test_economy_rules.gd` re-pointed** (50/50): the same fix, plus a genuine grass-onto-grass no-op case built explicitly (the tile no longer arrives pre-painted `grass` for free). **`test_causality_end_to_end.gd` re-pointed** (46 → 48 assertions; also row 6's suite): the rabbit's habitat-forming paint now supplies `open_grass` explicitly since `wild_grass` no longer does, and the villager's House tile is painted `grass` first — see row 6 for the full account, including the capacity-overshoot note this same suite carries. `test_mode_tap_model.gd` (91 → 99; also row 2's, see there) confirms a Terraform tap still converts the tapped tile through the real camera→ray→collider chain regardless of the tile's starting terrain. **CLOSED BY RULING 2026-08-02, not by code (previously carried here as a REPORTED DEFECT).** `terrain_options()` includes `wild_grass` and will continue to: **the human ruled it a supported paintable terrain that stays in the palette.** It is tag-inert by design, which makes painting it the player's way back to untouched land rather than a leak. No filter and no `palette_visible` flag will be added. The prior wording — "hands the player a brush that paints the exact tag-inert terrain the whole starting world is made of" — described the behaviour correctly and drew the wrong conclusion from it. Prior data-layer suites unchanged and still green: `test_terrain_schema.gd` (79), `test_placeholder_scenes.gd` (22). |
| `human_gate` | **2026-08-01 — partial (→ D-29).** All ratified constants (cultivated_field cost, free terrain pricing, world start size, grace window, recycle fraction) approved as-is. One reversed: every tile now starts as `wild_grass`, not `grass` — implementation pending (`WorldGrid.START_TERRAIN_ID`). Row cannot close until that lands and is re-verified. **Updated 2026-08-02.** The named blocker is resolved: `WorldGrid.START_TERRAIN_ID` has read `wild_grass` since 2026-08-01, and `validation_status`'s 2026-08-01/-02 entries confirm the inert-land invariant holds against it and `test_removal_refund.gd` was re-pointed to match. What the 2026-08-01 gate approved was constants **on paper**, not from playing — the row has never had an actual step-5 playtest of pricing, painting, and removal in the hand. `docs/superpowers/plans/2026-08-02-row3-terraform-closeout.md` Task 5 is that playtest; the row cannot move to ✅ until it returns. **Updated 2026-08-02 — full gate, everything approved, nothing rejected.** The human ran the Appendix A playtest script (`docs/superpowers/plans/2026-08-02-row3-terraform-closeout.md` Task 5) once, in order, and returned "all items approved" across all four sections. **1. Palette and pricing** — approved: the five-terrain palette (grass, water, forest, rock, cultivated) is exactly what's shown, grass/water/forest/rock paint free and cultivated costs 2 Wood shown on its button, and the insufficient-Wood refusal painting cultivated reads as a soft "not there" cue, not an error. **2. Removal and refund** — approved: full 2 Wood refund removing a cultivated tile inside the 12s grace window, `floor(2 × 0.5) = 1` Wood refund removing one after the window, free terrain (grass/water/forest/rock) refunds 0 regardless of timing — and 12 seconds is approved as the right amount of time to change your mind for a six-year-old's pace of play; `GRACE_WINDOW_SECONDS = 12.0` stands, no retune. **3. `wild_grass` re-confirm** — approved: every tile reads `wild_grass` on a new world with no habitat tags, paints to true grass and back, and reads tag-inert again once repainted — holding the 2026-08-02 ruling already recorded by row 2's closeout. **4. The remove-tool decision** — approved: the remove tool resetting to the mode's content brush on every mode switch (never staying selected across a Terraform ↔ Build switch) is the ruling, kept as-is. This is a genuinely new ruling on a constant (`game_hud.gd:197`'s `PROPOSED` marker) that had never been recorded anywhere before; recorded as a cross-claim in row 2's `constants` field since `game_hud.gd` is row 2's claimed file, not row 3's — no behaviour change made in this pass. **Nothing rejected, nothing unjudged. Row moves to ✅.** |
| | **ADDENDUM 2026-08-03 (→ D-33), does not reopen this row.** `terrain_view.gd` (this row's own claimed file) gained a whole-tile movement-blocking collision layer for first-person walking — see row 2's note above for the full account (`PICKING_COLLISION_LAYER` / `MOVEMENT_BLOCKING_COLLISION_LAYER`, `IMPASSABLE_TERRAIN_IDS`, `MOVEMENT_BLOCK_HEIGHT`). This is a camera/movement concern (D-33 files it under row 2's "Camera & modes," and gdd.md's own Technical Overview names it as first-person's new gap, not Terraform's), not a change to painting, pricing, or removal — nothing above this line is touched, and this row's own ✅ and its four-section playtest still stand for what they actually cover. The collision layer's own human confirmation (does walking into a freshly-painted forest tile actually feel right?) belongs to row 2's still-open `human_gate`, not a new gate here. |
| | **ADDENDUM 2026-08-22, does not reopen this row.** `terrain_view.gd` and a new `terrain_chunk_lod.gd` (both this row's own claimed directory) close gdd.md → Performance's still-unbuilt "zoom-distance LOD strategy": tap-to-select collision collapsed from one `StaticBody3D` per tile to a single resizable body, and far-from-camera tile visuals now batch into `MultiMeshInstance3D` per chunk instead of one scene instance per tile. Four new **PROPOSED (2026-08-22)** constants, all in `terrain_chunk_lod.gd`, none yet ruled on: `LOD_CHUNK_SIZE = 8` (tiles/chunk edge), `LOD_NEAR_MULTIPLIER = 1.0` (× `zoom_tiles()` for the near-tier radius), `LOD_NEAR_MARGIN_TILES = 4.0` (transition buffer), and `LOD_REBUILD_CHECK_INTERVAL_SECONDS = 0.25` (camera-tier recheck cadence, alongside `occlusion_fader.gd`'s existing `CHECK_INTERVAL_SECONDS = 0.1`). This is a performance change to an already-shipped, already-gated row — the original ✅ and its four-section playtest still stand for what they actually cover (palette/pricing, removal/refund, `wild_grass`, the remove-tool default). What this addendum still needs, and does not itself provide, is the row's own step-5 **human-run desktop validation over a synthetic full-size (128×128) world** gdd.md → Performance names explicitly — headless suites (`test_terrain_lod.gd`) cover the node-count and tier-switching mechanics but cannot measure real framerate. Full spec: `docs/superpowers/specs/2026-08-22-terrain-lod-web-threading-design.md`. |
| `depth` | not started |
| `est_hours` | 2–3 |
| `actual_hours` | **~3–4 h cumulative (estimate, not a stopwatch figure — no earlier entry existed to build on; the field read `—` throughout the row's history, so this is a first cumulative estimate, not a re-measurement).** Covers the row's full history, not just this pass: the original thin build and the removal/undo & refund system (Step 2/Step 3 dispatches, 2026-07-27/-28); the D-29 constant-ruling review that produced the 2026-08-01 partial gate (all five ratified constants plus the `wild_grass` start-terrain reversal); and this closeout's reconciliation-and-playtest pass (2026-08-02) — five stale-comment fixes, a verification pass, and the human's Appendix A playtest session that closed the gate. Lands at or slightly above the 2–3 h original estimate, in line with the row carrying a reversed constant and a second, fuller playtest most other rows didn't need. |
| `status` | ✅ — **the system now exists and consumes the data.** The pilot-1 spike is gone: `WorldGrid` builds a 36×36 grid from `TerrainDefinition.load_all()`, holds only `terrain_id` + an optional building reference per tile, and **derives** tags on read (→ D-25) rather than storing them. Nothing hardcodes a terrain list or a habitat tag. `paint_tile(x, z, terrain_id)` validates cost against Wood, converts, and marks the neighbourhood dirty; the free-Forest recovery guarantee is asserted at grid build, not merely commented. **Removal/undo & refund now exists (2026-07-28)** — `WorldRoot.remove_at(x, z)` is one entry point for both Terraform reverts and Build removals, refunding 100% inside the grace window and `floor(cost × 0.5)` after it, in the resource originally spent; free natural terrain refunds 0 because its receipt records a cost of 0, not because of a rule. A removal is an edit like any other: it marks the neighbourhood dirty immediately and arms the settlement window, so **removing a House with a family in it is allowed and warns** rather than being refused (refusing would be a fail state). Drag-to-paint is depth (#17 closed). **Correction, 2026-08-02: this was already stale when written.** The Terraform/Build palette has existed since Step 2 (2026-07-27, commit `bc94527`) — `project/scripts/ui/game_hud.gd`'s `build_palettes()` reads live from `WorldRoot.terrain_options()`, wired in by `project/scripts/ui/game_ui.gd:85`. There is no missing UI dispatch; the palette has been player-usable the entire time this row has carried the opposite claim. **Glyph moves to ✅, 2026-08-02:** the full human gate is recorded above (see `human_gate`) — the five-terrain palette and pricing, removal/refund including the 12s grace window and the 50% recycle fraction, and `wild_grass`'s tag-inert re-confirm all held in the hand, nothing rejected and nothing left unjudged. This closes row 3's floor. |

## Row 4 — Build

| Field | Value |
|---|---|
| `thin_form` | House at 1×1, grass only (**full form 2×2**). **A villager moves in when its habitat is met** ships whole — the USP requires the proof, not the building. |
| `invariants` | The villager move-in is the USP proof and does **not** thin. Gentle Displacement ships with placement (row 10). No rotation; `allowed_terrain` enforced; building footprints suppress underlying terrain tags. |
| `constants` | **House cost = 15 Wood — DECIDED 2026-08-01 (→ D-29)** (#8, #26; buildings.md's stated floor baseline "~15 Wood" at 1×1, ~30 at 2×2; lives in `data/buildings/house.tres`) · **House footprint = Vector2i(1, 1) — DECIDED 2026-08-01 (→ D-29)** (#18; the Tier 1 floor form — `PlaceableDefinition.DEFAULT_FOOTPRINT`) · **not tuning, decided in buildings.md and entered as-is:** `allowed_terrain = ["grass"]`, `emitted_tags = ["house"]` · **deliberately NOT a constant:** a building's home-site radius is derived at runtime as the widest `scout_radius` among species one of its `emitted_tags` satisfies (`HabitatSimulation._home_site_radius_for()`). A declared constant here would be a fourth number for the human to rule on with no independent meaning |
| `implementation_location` | **declared 2026-07-27** — `project/scripts/definitions/placeable_definition.gd` (schema; **shared directory**, see the claims table), `project/data/buildings/house.tres` (data entry), `project/assets/placeholder/house/` (grey-box fallback only — the shipped `model_scene` is the real imported asset). **System landed 2026-07-27:** `project/scripts/economy/building_placement.gd` (`allowed_terrain` + footprint + cost validation, `load_all()` for the Build palette), with `WorldRoot.place_building(x, z, placeable_id)` as the public entry point; footprint tag-suppression lives in `WorldGrid.get_tile_tags()`, and the House's home-site registration in `HabitatSimulation._sync_structure_site()`. Row 8's `project/data/animals/human.tres` also landed here, since row 4's USP proof cannot run without it |
| `validation_status` | **pass (2026-07-27, updated) — THE USP PROOF IS VALIDATED.** `bash scripts/run-tests.sh` **43/43 green** (34/34 before this pass). `test_causality_end_to_end.gd` (46 assertions) instantiates the real `scenes/Main.tscn` and drives `WorldRoot`'s public API: **a House placed on grass reports villager capacity 0 and nobody moves in — the building alone does not summon a villager** — then one cultivated tile painted beside it takes capacity 0 → 1, an arrival enqueues, and at due time a villager lands, with `resident_arrived` firing `species_id == "human"` and the villager counted in the *House's own* home site rather than on the field. The footprint's tag suppression is asserted directly (`get_tile_tags()` on the House tile is exactly `["house"]`), and the House's 15 Wood is debited exactly. `test_mode_tap_model.gd` (56) asserts a Build tap places, an ineligible target (rock) refuses with one soft cue and no error state, and nothing is built on top of a resident in any mode. `test_economy_rules.gd` (49) asserts an unaffordable placement changes nothing and emits no signal. ~~STILL UNVALIDATED BECAUSE UNBUILT: Gentle Displacement (row 10) — a build that drops capacity below population still does nothing; recorded as `note_expected_pending()` in three suites.~~ **Superseded 2026-08-03, kept so the record reads honestly: Gentle Displacement (row 10) landed 2026-07-28.** `test_causality_end_to_end.gd`'s own `note_expected_pending()` block already says so — this suite still owns the arrival direction only; the de-qualification direction (warn at settlement iff `capacity < population`, then relocate or depart) is `test_gentle_displacement.gd`'s, driven on this same `Main.tscn` by removing an occupied House. **Still genuinely untested anywhere:** arrival after a departure on the same spot — rebuild the habitat and see whether the species comes back. Also unbuilt: removal/refund (#16) — **superseded, this also landed 2026-07-28**, see row 3. **Closed 2026-08-03:** the one genuinely open gap this entry named — arrival after a departure on the same spot — is now covered. `test_gentle_displacement.gd` gained `_check_rebuilding_after_a_departure_lets_the_species_return()`, driven on the real `WorldRoot`/villager fixture: after Gentle Displacement departs the family (removing an occupied House), the vacated site is confirmed to have left the registry entirely (`not registry.any_site_at(...)`), a House is rebuilt on the same tile through the public `place_building()` API, and a fresh villager family is confirmed to arrive through the real `ArrivalQueue` timing — no shortcuts — with Species Hosted staying at 1 (not double-counted) and no residual displacement gesture left pending. **No bug existed**: `HomeSiteRegistry.release()` fully unregisters a departed site and `rebuild_ownership()` drops it from the exclusivity index, so a rebuilt tile re-enters through the ordinary qualification path indistinguishable from a first arrival; the only persistent state, `_ever_hosted`, never gates re-arrival. `test_causality_end_to_end.gd`'s own note was updated to point here instead of restating the gap. Full suite: **57/57 green.** Data layer, unchanged and still green: `test_placeable_schema.gd` (27 assertions — `house.tres` binds to `PlaceableDefinition`, `validate()` clean both with and without a terrain roster, `footprint == Vector2i(1,1)`, `allowed_terrain == ["grass"]` and every entry resolves to a real `TerrainDefinition`, `emitted_tags == ["house"]` and in the shared vocabulary, `cost` 15, `model_scene` pinned to the real imported asset and instantiable). The `PLACEHOLDER` `fact_text` is pinned as reported by **`pending_signoff()`, not `validate()`**, with the contrast against `AnimalDefinition` asserted on a synthetic definition so the divergence stays deliberate rather than becoming an accident someone "fixes". Also `test_house_import.gd` and `test_placeholder_scenes.gd` (grey-box fallback). *(Two superseded notes, kept so the record reads honestly: an earlier entry claimed `PlaceableDefinition` has no code and `project/data/buildings/` does not exist — both false as of 2026-07-27; a later one said "the data layer only … this row's USP proof is entirely unvalidated because it is unbuilt" — true when written, and no longer.)* |
| `human_gate` | **2026-08-01 — partial (→ D-29).** House cost (15 Wood) and footprint (1×1) ratified as-is. **Row cannot close yet:** `content-pipeline-status.md`'s `house` entry flags two still-unconfirmed human checks that today's camera playtest (Row 2) didn't cover — the variant pick (`Houses_FirstAge_1_Level1`, chosen from 9 candidates) and its facing under the fixed 45° camera. Needs an actual look at a placed House before this row signs off. **Correction, 2026-08-03 (→ D-33) — the outstanding check's own camera is gone.** D-33 replaced the fixed ~45° pitch with a first-person walk camera the same day, and row 2 itself reverted to 🚧 pending its own new gate (`terrain_view.gd` now blocks movement onto any building-occupied tile, so a House is walkable-up-to under the new camera too). The variant pick and facing still need an actual human look — but at ground level, walking up to the House, not from the old overhead framing this entry names. Do not close this gate against the camera described above; re-run it once row 2's new gate is settled enough to look through. **Resolved 2026-08-03 — confirmed.** The human looked at a placed House under the new first-person camera and approved both outstanding counts: the variant pick and the facing. `House.tscn`'s header and `content-pipeline-status.md`'s `human_signoff` are updated to match. **This closes every item this field ever named as a blocker.** What still stands between this row and ✅ is not a `human_gate` item but a `validation_status` one: the arrival-after-departure round trip (rebuild a habitat after Gentle Displacement departs its resident, confirm the species can return) is a genuine, currently-untested gap, and a dispatch is in flight against it as of this same date — see `validation_status` for whatever it lands. |
| `depth` | not started |
| `est_hours` | 1.5–2 |
| `actual_hours` | — |
| `status` | ✅ — **closed 2026-08-03; this cell was stale since 2026-07-27 and is rewritten rather than patched, per the mechanics rule.** `place_building()` validates `allowed_terrain` against every footprint tile and refuses ineligible or unaffordable placements silently; the footprint suppresses the ground's terrain tags and emits `house` instead; the House registers as a home site at placement, which is what makes the exclusivity tie-break put a villager *in* the house rather than on the field beside it. Verified headlessly: House + one cultivated tile → `capacity(human) = 1` → arrival → villager instantiated and `resident_arrived` fired; a second House keeps its own `house` tile and gets its own family; and, as of this pass, a departed site's tile fully clears and a rebuilt House gets its own fresh arrival, not a stuck one. **Every item the 2026-07-27 cell listed as open is closed:** Gentle Displacement (row 10, 2026-07-28) and removal/refund (row 3, 2026-07-28) both landed; House cost (15 Wood) and footprint (1×1) were ratified 2026-08-01 (→ D-29); `human.tres`'s `fact_text` (#31) was closed 2026-07-28; the asset's variant pick and facing were confirmed 2026-08-03, under the first-person camera D-33 shipped that same day rather than the fixed 45° pitch this cell originally named. `human_gate` records the full approval; `validation_status` records the last closed test gap. Nothing named here is still open. |

## Row 5 — Economy

| Field | Value |
|---|---|
| `thin_form` | Passive Wood from Forest only; tap-to-tend is depth. **The free-Forest recovery guarantee ships whole** (pillar invariant). |
| `invariants` | The free-Forest no-dead-ends guarantee ships whole — the player can never be stranded without a path back to Wood. Wood is a pacer, not an economy; no scarcity pressure (Pillar 1). |
| `constants` | **passive Wood rate = 1 Wood per forest tile per 60 s — DECIDED 2026-08-01 (→ D-29)** (#8; gdd.md → Economy calls this "v1's most load-bearing constant" and states exactly this baseline; `WoodLedger.SECONDS_PER_WOOD_PER_FOREST_TILE = 60.0`, expressed as seconds-per-Wood-per-tile so it reads as the GDD writes it) · **starting stockpile = 50 Wood — DECIDED 2026-08-01 (→ D-29)** (#26; gdd.md's stated "~50 Wood (floor ~35 …)", sized to cover the nudge's first build only; `WoodLedger.STARTING_WOOD`). Both live in the economy evaluator, not in a per-tile definition — the same reasoning that keeps the habitat-to-individuals curve off `AnimalDefinition`. **Not tuning, decided in terrain.md and entered as-is:** Forest's `resource_type = "wood"`, `land_use = "wild"`, and `removes_habitat_when_harvested = false` — the last is a pillar obligation (the free-Forest recovery guarantee), not a preference |
| `implementation_location` | **declared 2026-07-27** — `project/scripts/definitions/harvestable_tile_definition.gd` (schema; **shared directory**, see the claims table), `project/data/terrain/forest_harvest.tres` (the project's only harvestable; `project/data/terrain/` is shared with row 3 because `forest.tres` holds the reference to it). **System landed 2026-07-27:** `project/scripts/economy/wood_ledger.gd` (counter, passive accrual, `wood_changed` signal, silent-refusal `spend()`), reading an incrementally-maintained forest count off `WorldGrid.forest_tile_count()` so accrual never scans the world and a forest-free world does no work at all |
| `validation_status` | **pass (2026-07-27, updated)** — `bash scripts/run-tests.sh` **43/43 green** (34/34 before this pass). **The economy SYSTEM is now validated.** `test_economy_rules.gd` (49 assertions) drives the real `Main.tscn`: **the free-Forest recovery guarantee is asserted AT ZERO WOOD** — the balance is reset to 0, Forest is confirmed present in `terrain_options()` (a recovery path the player cannot see is not a recovery path), `can_paint()` reports true, `paint_tile()` returns true, the tile converts and the balance is still 0; every other natural terrain paints at zero too, and the one priced terrain is correctly refused. **An unaffordable edit changes nothing and produces no error state**, asserted four ways: the tile is unchanged, the balance is unchanged, **neither `wood_changed` nor `tile_changed` fires at all**, and the whole public signal surface is pinned (`WorldRoot` declares exactly `wood_changed`/`tile_changed`/`resident_arrived`; `WoodLedger` exactly `wood_changed`) so there is no channel an error could arrive on — with a control proving the identical edit succeeds once Wood is restored. **Passive accrual is measured at 0, N and 2N forest tiles** (0 pays nothing over ten minutes; 5 tiles pay 5 Wood per 60 s; 10 pay 10), the fractional carry is proven not to round away across six 10-second ticks, and painting the forest away stops payment. Costs pinned: cultivated 2, House 15, every natural terrain 0. **Unvalidated because unbuilt: refund/recycle and the grace window (#16).** Tap-to-tend is depth and correctly absent. Data layer, unchanged and still green: `test_harvestable_schema.gd` (22 assertions — `forest_harvest.tres` binds to `HarvestableTileDefinition` rather than bare `Resource`, `validate()` clean, `resource_type == "wood"` and inside `RESOURCE_TYPES`, `land_use == "wild"` and inside `LAND_USES`, **`removes_habitat_when_harvested == false`** asserted as an exact value because it is the free-Forest recovery guarantee and not a preference, `model_scene` instantiable, `forest.tres` holds *this instance* rather than a copy, and forest is the **only** terrain carrying a harvestable in v1). *(Superseded note, kept for the record: "the data layer only — no resource counter, no accrual loop, nothing that could validate the passive rate". True when written; the rate is now measured in `test_economy_rules.gd`, still not as a field on this schema — #8 stands.)* |
| `human_gate` | **2026-08-01 — approved (→ D-29).** Passive Wood rate (1/forest tile/60s) and starting stockpile (50) ratified as-is; nothing else outstanding for this row. |
| `depth` | not started |
| `est_hours` | 0.5–1 |
| `actual_hours` | — |
| `status` | ✅ — **the system landed.** Wood starts at 50, accrues ~1 per forest tile per 60 s, and pays for cultivated tiles and the House. **The free-Forest recovery guarantee ships whole and is asserted in code**, not commented: `WorldGrid` `push_error`s *and* `assert`s at build time if Forest is missing or non-zero-cost, and `free_forest_guarantee_holds()` is public so a test can read the invariant directly. Verified headlessly at zero Wood: Forest still paints, and 10 forest tiles pay out 10 Wood over 60 s. An unaffordable edit returns `false` and changes nothing — no error, no message, no penalty (Pillar 1). Tap-to-tend is depth; both pacing numbers still await the human's ruling. **Human sign-off recorded 2026-08-01 (→ D-29).** |

## Row 6 — Habitat & move-in

| Field | Value |
|---|---|
| `thin_form` | Animals move in *only because a real spot met their needs* — never scripted, never timed. Causality ships whole; coarser cadence, slower scout tick, waypoint wander, home prop. **Carrying capacity ships whole in its decided thin form — min-over-needs, no lower clamp, `capacity = 0` is the unsuitable state**; rows 3, 4 and 10 read it; only scoring richness and readout thin (#27). **The live neighborhood preview ships too.** |
| `invariants` | Move-in causality ships whole and is not a depth axis. Carrying capacity ships whole. The inert-land invariant. Qualification is event-driven on a dirty-neighbourhood queue — an idle world does no simulation work (D-22). Tile exclusivity: nearest home site, ties to the older. |
| `constants` | **`max_individuals` = 6, uniform across the roster — DECIDED 2026-07-28 (D-27 #1), marker removed.** It is now a real `@export_range(1, 64, 1)` field on `AnimalDefinition`, defaulted from `AnimalDefinition.DEFAULT_MAX_INDIVIDUALS = 6` and set explicitly in all three `.tres`. `CapacityEvaluator.MAX_INDIVIDUALS_PER_HOME_SITE` is **deleted** — the evaluator now reads `species.max_individuals`, so the cap moves with the data. The value itself stays roster.md's "~6" baseline and is still tunable under #23; what closed is *where it lives*, not what it is · **`capacity_radius` = `scout_radius` — DECIDED 2026-07-28 (D-27 #1), marker removed, and the spec/code contradiction is closed in spec.md's favour.** `animal_definition.gd`'s "there is deliberately no second radius field" comment is deleted; `capacity_radius` is a real `@export_range(0, 32, 1)` field, and **`CapacityEvaluator.tag_counts()` now walks `species.effective_capacity_radius()`, not `scout_radius`** — the field is doing the job it exists for. **Data expresses the v1 default as the RELATION, not a copy of the number:** `capacity_radius = 0` is the sentinel `AnimalDefinition.CAPACITY_RADIUS_FOLLOWS_SCOUT`, resolved through `effective_capacity_radius()`; any value 8–12 is a deliberate per-species divergence and is band-checked in `validate()`. Sentinel-vs-explicit was the implementer's call and is flagged for the human — see the build report · **arrival delay = 20–60 s randomised — DECIDED 2026-08-01 (→ D-29)** (#28; spec.md → Pacing Constants verbatim; `ArrivalQueue.ARRIVAL_DELAY_MIN_SECONDS` / `_MAX_SECONDS`) · **dirty-queue drain budget = 4 evaluations/frame — DECIDED 2026-08-01 (→ D-29)** (#28; spec.md's "N evaluations/frame … the CPU budget". One evaluation is one candidate position × the whole roster, which is gdd.md → Performance's `scout_radius × roster size` unit; `HabitatSimulation.MAX_EVALUATIONS_PER_FRAME`) · **`Human scout_radius = 8` — DECIDED 2026-07-28 (D-28), marker removed.** Proposed at the tight end of spec.md's ~8–12 band because a villager's two needs are a House and the field beside it; approved as-is. #20 stays open generally — the nine cleared-pool species still have no scout_radius at all — but Human's own value is no longer a proposal. Lives in `data/animals/human.tres` · **`tiles_per_individual` — DECIDED 2026-07-28 (D-27 #2): Human 1, Fox 5, Rabbit 4, straight from roster.md's Already-Defined table, now on disk in all three `.tres`.** The uniform **12** in `fox.tres`/`rabbit.tres` was never a decision, only `AnimalDefinition.DEFAULT_TILES_PER_INDIVIDUAL` showing through; the two sources of truth are now one. **Live, not cosmetic — measured after the change:** rabbit capacity first reaches 1 at the **fourth** cover tile, not the twelfth, so time-to-first-move-in was previously being measured against a ~3× harder world than the ≤2 min target / 5 min ceiling assumes. Expect the floor world to fill faster: the same twelve-rock-tile edit that used to land **one** rabbit now lands **two to three** (measured; acreage/divisor = 3). The default constant stays 12 and now applies only to a `.tres` that omits the field · **RULED 2026-08-01 (→ D-29): option (b) — extend the arrival check.** With several sites competing, a site can end up **over capacity the moment its neighbour's arrival steals tiles it was nearer to** (measured: 1 of 3 sites, population 1 against capacity 0). The arrival predicate checks only the arriving site's `capacity ≥ population + 1`, never whether registering it drops a *neighbour* below its own population, and nothing consumes that fall. **Decided fix, implementation pending:** the arrival check must also re-evaluate neighbouring sites' capacity, and if registering the new arrival would drop a neighbour below its own population, run Gentle Displacement for that neighbour too — rather than shipping the overshoot as a known v1 limitation. Invisible at divisor 12 because only one site could ever qualify at floor scale. See the build report · **Not constants — decided by D-25:** the tag model is per-tile (no emission radius, no weights, no thresholds), and arrival group size is 1 with no field until the sizing rule exists (#7) · **Wander and home prop, added 2026-07-28. NO GDD OR spec.md NUMBER EXISTS FOR ANY OF THE FIRST THREE** — they are not stated baselines being transcribed, they are starting values, and they are the ones most worth eyeballing at step 5: **walk speed = 0.6 tiles/s — DECIDED 2026-08-01 (→ D-29)** (`ResidentRoamer.WALK_SPEED_TILES_PER_SECOND`; chosen to read as an unhurried amble at person scale, one tile = one world unit) · **pause between waypoints = 2–6 s randomised — DECIDED 2026-08-01 (→ D-29)** (`ResidentRoamer.PAUSE_MIN_SECONDS` / `_MAX_SECONDS`; randomised so residents do not step off in lockstep) · **waypoint-pick radius = 3 tiles — DECIDED 2026-08-01 (→ D-29)** (`ResidentRoamer.WANDER_RADIUS_TILES`; **clamped at construction to the home site's own radius**, so however it is tuned it can never leave the home neighbourhood — gdd.md's "freely within their home neighbourhood" is structural, not a value) · **global roamer budget = 64 active roamers — DECIDED 2026-08-01 (→ D-29)** (`ResidentPresentation.ROAMER_BUDGET`; **a pure performance backstop, never a design tool** — gdd.md's own words. Flat rather than scaling with revealed world size because row 13 (mist) is unbuilt and there is no reveal to scale against. Set far above anything the floor's capacity values can produce, so **if it ever binds that is a signal capacity is tuned too rich**, exactly as gdd.md says) · **not constants:** facing (`FACING_YAW_OFFSET_RADIANS = 0.0` is the glTF 2.0 spec's "the front of an asset faces +Z", not a taste value) and arrival epsilon (0.02 tiles, an anti-overshoot guard) · **The live neighborhood preview, added 2026-07-28.** Its one tuning number, **preview poll = 0.1 s — DECIDED 2026-08-01 (→ D-29)** (`TapRouter.PREVIEW_POLL_SECONDS`), is filed under row 2 with the rest of the UI's numbers. **RULED 2026-08-01 (→ D-29): shipping with the two-meaningful-band gap accepted for the floor** — no stopgap "getting closer" band before Row 12 lands. The preview reports `wild` / `welcoming` / `home`, where `welcoming` is `capacity(h, S) >= 1` for some species — the qualification predicate itself, so the preview and the arrival that follows it read the same function and cannot disagree. **Deliberately no fourth band for "room for more" even though `population_at()` would supply it**: room-for-more is the container-to-fill shape Open Question #27 names, and it belongs to the resident inspect readout (spec.md's *"This meadow supports 6 · 4 live here"*), which is out of scope at the floor. **`NeighborhoodPreview.read()` returns a band string and no counts at all**, so #27's numeric form is structurally unreachable rather than merely unused |
| `implementation_location` | **declared 2026-07-27** — `project/scripts/simulation/`: `species_roster.gd` (loads `data/animals/*.tres`; deliberately not a static on `AnimalDefinition`, so the shared `definitions/` directory gains nothing), `home_site.gd`, `home_site_registry.gd` (the tile-exclusivity ownership index), `capacity_evaluator.gd` (the formula, verbatim from gdd.md), `arrival_queue.gd`, `habitat_simulation.gd` (the four triggers, the dirty queue, the bounded drain, move-in). Reached only through `project/scripts/world/world_root.gd`. **`project/scripts/definitions/animal_definition.gd` — MODIFIED 2026-07-28 (D-27 #1), no new directory:** it gained the `capacity_radius` and `max_individuals` exports, `DEFAULT_MAX_INDIVIDUALS`, `CAPACITY_RADIUS_FOLLOWS_SCOUT`, `effective_capacity_radius()` and `validate()` coverage for both. This is the SHARED `definitions/` directory the disjointness precondition does not cover — the schema was in flux for this dispatch, so rows 3, 4, 5 and 6 were not safe to run in parallel with it. Also modified, all inside directories already claimed by this row: `simulation/capacity_evaluator.gd` (reads both fields; walks `effective_capacity_radius()`; its module constant deleted), `simulation/home_site.gd` (the radius comment only), `data/animals/{fox,rabbit,human}.tres` (the three capacity fields), and `world/resident_picker.gd` (the D-27 #3 hitbox cap — the constant itself is filed under row 2). **Extended 2026-07-28 (wander + home prop), `project/scripts/world/`:** `resident_roamer.gd` (one resident's pick-waypoint → walk → pause state machine), `resident_presentation.gd` (owns the roamers and the den props; **its own node with its own `_process`, structurally separate from the simulation's**), `animal_clips.gd` (resolves Idle/Walk per model — the roster has no shared clip-name contract), `resident_picker.gd` (live-position resident hit-test behind `WorldRoot.resident_at()`). `habitat_simulation.gd` gained exactly one line of coupling — an optional `presentation` argument to `attach()`, called once at move-in: **no fifth trigger, no third signal, nothing polled**. **Extended 2026-07-28 (the live neighborhood preview), `project/scripts/ui/`:** `neighborhood_preview.gd` (the read itself — bands, the cursor-tile-only query, and the public query log the cost bound is measured against), polled by `tap_router.gd` (`refresh_preview()`, `PREVIEW_POLL_SECONDS`) and rendered by `game_hud.gd` (`show_neighborhood_preview()` and the three band strings) into `HUD/PreviewPanel` in `project/scenes/ui/GameUI.tscn`. It reads `WorldRoot.capacity_at()` / `population_at()` and nothing else — the simulation gained **no** new surface for it. **D-29 #5 landed 2026-08-01 (the capacity-overshoot edge case), inside directories already claimed by this row plus one line in `project/scripts/world/world_root.gd`:** `project/scripts/simulation/gentle_displacement.gd` gained `on_arrival(tile, species_id)` and its two private helpers (`_reach_could_overlap()`, `_reach_of()`) — called once per landed arrival, never per frame, from `world_root.gd`'s existing `simulation.resident_arrived` connection (which now also calls `displacement.on_arrival(grid.world_to_tile(world_position), species_id)` after re-emitting the public signal). Reuses `SettlementWindow.touch()` exactly as `on_edit()` does — no parallel mechanism, no new signal on `HabitatSimulation` (its trigger/signal counts are unchanged). Self-verified with a scratch probe (since deleted): registering a second competing site left the first at population 2 against capacity 1 with nothing noticing pre-fix; post-fix, the same sequence arms the first site's settlement window, and letting it settle raises exactly one warning naming only the affected site, resolving the overshoot. Not yet re-verified against `test_gentle_displacement.gd`/`test_settlement_window.gd` (QA's step 4) — see the note below on row 3's `wild_grass` change, which currently breaks several of that suite's fixtures for an unrelated reason (default terrain, not this fix) and must be sorted out before those two suites can confirm this one cleanly. |
| `validation_status` | **pass (2026-08-01, updated → D-29) — THE CAPACITY-OVERSHOOT FIX (#5) IS NOW DIRECTLY TESTED, NOT JUST OBSERVED, AND EVERY ROW-6 SUITE SURVIVES THE `wild_grass` DEFAULT.** `bash scripts/run-tests.sh` **51/51 green, 1655 assertions** (50/50, 1609 before this pass; one new suite added by this pass, one new suite unrelated to row 6 landed from a concurrent row-1 dispatch).<br><br>**THE HANG RISK WAS REAL, INVESTIGATED FIRST, IN ISOLATION, BEFORE ANY BROAD RUN.** Reproduced standalone under a bounded timeout (`timeout 30 ... --script test_event_driven_simulation.gd`): confirmed `SCRIPT ERROR: Invalid access to property or key 'position' on a base object of type 'Nil'` spamming every frame, never exiting inside 30s. ROOT CAUSE, confirmed by reading the fixture: the natural-frame wander check paints only `rock` (cover) beside `RABBIT_SITE`, which relied on the old ambient `grass` backdrop to supply the rabbit's OTHER need (`open_grass`) for free — `wild_grass` emits nothing, so capacity never left 0, no resident ever arrived, `_resident` stayed null, and the phase machine advanced into the natural-frame check regardless, dereferencing it forever. RE-POINTED, not weakened: the fixture now paints an explicit `grass` border around the rock block (same fix applied to every other row-6/7/10 suite below), and — since a fixture bug of this exact shape could recur — the phase machine was hardened to check its own precondition's return value and fail clean instead of proceeding blind. Re-run standalone 3× after the fix: clean exit, 0 failures, no hang, every time.<br><br>**RE-POINTED (stale `grass`-default assumptions, not defects):** `test_causality_end_to_end.gd` (46 → 48 assertions — the rabbit paint now supplies `open_grass` explicitly, the villager's House tile is painted `grass` first; also updated its own `_note_capacity_is_not_a_binding_cap()` doc, which had gone stale in the other direction — it used to say nothing consumes an overshoot, and D-29 #5 is precisely the fix that now does, so the note now says an armed-but-not-yet-settled window is what this suite's own short real-time budget leaves on screen, and points at the dedicated test below for the settled proof); `test_home_prop.gd` (57/57); `test_neighborhood_preview.gd` (64 → 66); `test_resident_lookup.gd` (60 → 63, plus its Terraform/Build sections rewritten for D-29 #7's mode-aware priority — see row 2). None of these needed their actual claims weakened — every fix is "state the habitat's `open_grass` need explicitly" or "paint `grass` before placing a House," the same shape the build report itself named.<br><br>**VERIFICATION #2 FROM THE BRIEF — THE OVERSHOOT FIX ITSELF, REPRODUCED AND CONFIRMED, NOT JUST OBSERVED.** No existing suite drove `GentleDisplacement.on_arrival()` directly before this pass — the phenomenon was only ever a `note_expected_pending()` in `test_causality_end_to_end.gd`, reporting the overshoot without asserting anything about whether it is consumed. Added `_check_arrival_extends_to_a_competing_neighbour()` to `test_gentle_displacement.gd` (row 10's suite; `on_arrival()` itself lives in `gentle_displacement.gd` but is still row 6's per the directory-claims table), reproducing the exact mechanism `on_arrival()`'s own header describes: an 8-tile cover row at divisor 4, site A registers alone (owns all 8, capacity 2, settled at population 2 — no warning, correct), then site B lands for real through `HabitatSimulation`'s own arrival path at a position that only qualifies for four of those eight tiles as a **prospective** candidate (capacity 1, `1 >= 0 + 1` lands it). B's registration rebuilds ownership and takes those four tiles from A — **measured live**, A's own capacity drops to 1 against a population that never moved (2), over capacity by one, and A was never asked. **Confirmed: landing B's arrival ALREADY arms A's settlement window** (`displacement.is_idle()` flips false, `pending_gestures() == 1`) **before the suite ticks the clock forward at all** — not left silently over capacity. Advancing past the grace window settles it: exactly one warning, naming A, `capacity: 1`, `population: 2`; no nearby alternative exists so A departs its one excess resident (relocation needs room for the WHOLE household of 2, finds none), leaving `population == capacity == 1`; the world reports idle again. **B itself is untouched throughout** — the site that stole the tiles is not the one that pays for it, only the one the theft actually left over capacity is. Re-run 3× (the fixture's `ArrivalQueue` is seeded): clean and identical every time.<br><br>**PRIOR VALIDATION (2026-07-28), STILL STANDING:** `bash scripts/run-tests.sh` **47/47 green, 1187 assertions** (43/43 and 924 before that pass; four suites added, two extended). **Clause 1, waypoint wander — `test_resident_wander.gd` (67 assertions), new.** Driven with a seeded RNG so every number is reproducible, not sampled. A rabbit's `position` changes and it covers **39.0 tiles of path in 120 simulated seconds**; across 1200 samples it is **never once outside its home site's own radius** — asserted against `HomeSite.radius`, never against a literal — and **the clamp is proven in both directions**: a radius-8 site leaves `WANDER_RADIUS_TILES` (3.0) in charge, a radius-2 site overrides it to 2.0 and the resident measurably stays inside 1.90 tiles, so "within their home neighbourhood" is structural rather than a value to keep in sync. Non-vacuity throughout: it reached 2.85 of its 3.00-tile radius, so containment is not true merely because it barely moved. Walk/Idle alternated **28 times** over the run (658 walking frames, 542 idle) with the correct clip playing on **every** frame of each. **The clip names resolve for EVERY roster species, through the roamer and not only through the resolver** — fox `Idle`/`Walk`, rabbit `Bunny|Bunny_idle`/`_walk`, human `CharacterArmature|Idle`/`|Walk`, each pinned as an exact string and each asserted to be the clip actually playing while that species walks — with a non-vacuity check that the **three conventions really are three** (a resolver that returned `"Idle"` could not pass) and that the exact-leaf rule beats the traps that exist in the shipped data (`Idle_2` on the fox, `Idle_Gun` on the villager). Facing: **0 of 658 walking samples** had the model pointing away from travel, worst angular error **0.000211 rad**, across 11 distinct headings — checked against the node's own `basis.z` as the engine computed it, deliberately **not** by re-running the implementation's `atan2`, which would share a sign error with it. **Clause 2, the home prop — `test_home_prop.gd` (53 assertions), new.** A den appears at a wild home site; **a villager gets none, asserted as a COUNT** (prop count 1 before the villager's move-in and 1 after, and the `HomeProps` node still holds exactly one child) rather than as an absence on the house tile, which would not catch a den that merely landed elsewhere. The rule is proven to read the **data** and not the species: a synthetic structure site gets no prop, a second *wild* site immediately does (1 → 2, the control that stops the first zero being a saturated counter), and two residents at one home share one den. Decoration, measured across the `present()` call itself so nothing but the prop is in frame: **tags byte-for-byte unchanged, terrain unchanged, tile still unoccupied, capacity identical (1 → 1), and the tile under the den still paints**; the prop subtree contains **no `CollisionObject3D`, `CollisionShape3D` or `Area3D` at all** while containing 3 real meshes, so it is not vacuously collision-free; and it is parented to the visual props root, never to the grid and never to the resident that walks away from it. `release()` drops that home's prop and its roamers and leaves the other home's alone. **Clause 3, the live neighborhood preview — `test_neighborhood_preview.gd` (64 assertions), new.** **#27 is asserted as an absence of digits, and structurally:** all four band constants, the band `read()` returns for every reachable band, and the copy the HUD actually renders are each checked for any of `0`-`9`, for `/` and for `%`; and `read()` is asserted **declared to return a `String`**, so the numeric form is unreachable by construction rather than merely unused. An unknown band renders nothing instead of leaking its own identifier. **`welcoming` is the qualification predicate itself, proven as an equivalence and not at one convenient point:** across a 15-step sweep that adds one cover tile at a time under a fixed cursor, `band == welcoming` matched `(some species has capacity >= 1)` at **15 of 15 steps**, with non-vacuity assertions that the sweep saw both sides and crossed the boundary **mid-sweep, at the 12th tile**, not at an endpoint. **The cost bound is measured, not asserted-about:** one read costs 6 world queries for 3 species, **every one of them originating at the cursor tile** (the origin log accounts for all of them, so a scan would show); a home site 30 tiles away changes the cost by **nothing**; a still cursor costs **0 further queries across 50 polls**, with `invalidate()` proven to make the very next poll recompute so that zero is a short-circuit and not a dead preview; and **60 reads moved `evaluations_run` by 0** with a control proving one real edit does move it. **Performance smoke against gdd.md's ~128×128 cap:** the same read on a 128×128 world costs the **same number of queries** and **1.00× the time** of the 36×36 start (299.3 ms vs 298.0 ms for 200 reads), where a re-scan would be ~13×. **AND THE ASSERTION THAT MATTERS MOST — `test_event_driven_simulation.gd` extended 34 → 53.** Wander must not move `evaluations_run`, because motion is presentation and a resident moving is not one of the four triggers. Asserted **twice**: hand-driven (120 s of `ResidentPresentation.tick()`, 37.2 tiles walked, counter moved by exactly 0, queue still empty, world still reporting idle) and — the path that would regress silently — **across natural `_process` frames**: `Engine.time_scale` buys 120 simulated seconds across **869 real engine-driven frames** during which this suite calls neither `tick()`, the resident walks **38.7 tiles** moved by `ResidentPresentation._process` alone, and `evaluations_run` moves by **exactly 0**, sampled **every single frame** so a transient that drained again could not hide, with the world reporting idle on all 869. **The non-vacuity control is the important half:** one dirtied neighbourhood then moves the counter on the engine's own frames (13 → 14 in one frame) with wander still running, so the zero is a real zero and not a disabled node. **Negative controls run on every load-bearing assertion above, confirmed red, restored, re-run** — including two that emulate the actual regression rather than merely flipping an expected value: making wander enqueue a dirty neighbourhood on every natural frame turns the zero red (`expected 13, got 881`, plus 869 non-idle frames and a non-empty queue), and mutating the roamer's forward vector to -Z turns the facing assertion red (`658 of 658 samples backwards`). **THE TWO SPEC/CODE CONTRADICTIONS STILL STAND AND ARE STILL REPORTED, NOT RESOLVED:** `max_individuals` and `capacity_radius` are required by spec.md → Data Schemas and roster.md but have no `@export` in `animal_definition.gd`, and `AnimalDefinition.BARE_TAGS` is still hardcoded where spec.md requires derivation (over-strict, so the inert-land invariant is not weakened). **WHAT IS STILL SHORT, AND NEITHER IS A ROW-6 CLAUSE.** (1) **There is no "getting closer" band**, and this suite demonstrates the gap rather than describing it: a spot **eleven** cover tiles in reports `capacity_at() == 0`, **identically to bare ground**, both read `wild`, and the twelfth tile flips `wild` straight to `welcoming` with nothing in between. gdd.md has the preview read the qualification system's **near-miss summary**, which is row 12's and is unbuilt, so gdd.md's own exemplar line fires when a spot **already** suits somebody and **the First 60 Seconds' beat-4 "becoming" moment at ~0:20 is not detectable by this build and cannot be observed at the step-5 playtest.** Recorded as `note_expected_pending()`. (2) **Gentle Displacement (row 10) is unbuilt** — capacity falling below population still does nothing; `note_expected_pending()` in five suites now. Also pending and recorded: the `home` band is exact-tile only (`population_at()` answers for a position, and `WorldRoot` exposes no "residents in the ring" read), so a cursor one tile from a resident reads `wild`/`welcoming` while the animal is standing right there; `release()` is never called by anything, and when row 10 calls it, note that it erases its map entry immediately but frees with `queue_free()`, so release-then-re-present inside one frame would briefly leave two dens; and two of the three band strings are literally `[COPY]` stubs that a playtester would read on screen. **Row-6 suites unchanged and still green:** `test_capacity_formula.gd` (34), `test_tile_exclusivity.gd` (26), `test_causality_end_to_end.gd` (46). |
| `human_gate` | 2026-07-28 (D-27/D-28 rulings). 2026-08-01 (→ D-29), continued: remaining constants (arrival delay, dirty-queue budget, wander/roamer numbers, preview poll) ratified as-is. The two-band preview gap accepted for the floor. The capacity-overshoot edge case ruled — extend the arrival check to re-evaluate neighbours. Implementation landed 2026-08-01 and re-verified the same day: test_gentle_displacement.gd's `_check_arrival_extends_to_a_competing_neighbour()` reproduces a competing arrival stealing a neighbour's tiles and confirms `on_arrival()` arms that neighbour's settlement window immediately, resolving to exactly one warning and one departure, re-run 3x identically. **Row closes in full as of this verification.** |
| `depth` | not started |
| `est_hours` | 4–6 — the largest single row |
| `actual_hours` | — |
| `status` | 🚧 — **the causal spine is built and runs end to end.** Capacity is the gdd.md formula verbatim: min-over-needs with a linear divisor, capped by `max_individuals`, **no lower clamp — `capacity = 0` is the unsuitable state** and survives to the caller. `qualifies(h, S) ≡ capacity ≥ 1` is literally the same function, and an arrival is enqueued only where `capacity ≥ population + 1` — one read, not two systems. Tile exclusivity is an O(1) ownership index (nearest site, ties to the older), so an evaluation stays one pass over the tiles in radius tallying counters (→ D-25). Qualification is event-driven on exactly four triggers — terraform, building add/remove, resident arrive, resident depart — and **mist reveal is not one of them**; `tick()` returns on its first line when both queues are empty, and `evaluations_run` is public so the zero is observable rather than asserted. Arrivals ride a 20–60 s randomised delay and **re-check at due time**, with a de-qualified arrival silently dropped, never warned. Verified headlessly: 12 rock tiles beside grass raised rabbit capacity 0 → 1, enqueued 12 arrivals, landed exactly 1 and silently dropped 11 once the first resident's site claimed the rock. **Waypoint wander and the home prop landed 2026-07-28** — residents pick a waypoint inside their home site's radius, walk to it playing Walk, pause playing Idle, and pick another, facing the direction of travel; a den appears at a wild home and **not** at a House, because a House is already a villager's home site and its own prop. Proven headlessly: a rabbit walked 35.4 tiles over 120 simulated seconds, never further than 2.95 tiles from its den (radius 3.00), split 600/600 frames between Walk and Idle clips — and **`evaluations_run` moved by exactly 0 the whole time, with the world still reporting idle**, so the zero that gdd.md → Performance's CPU argument rests on survives a world full of moving animals. **What is NOT here, and matters:** de-qualification currently does nothing — **Gentle Displacement (row 10) is unbuilt**, and it is a pillar invariant that must ship before any kid playtest. **The live neighborhood preview landed 2026-07-28, and with it this row's thin form is met in full — all three clauses of it.** While the cursor is over land in Terraform or Build, a single line reads the neighbourhood qualitatively and never numerically: `NeighborhoodPreview.read()` returns a band and **carries no count out of the function that computed it**, so the `X / Y` form Open Question #27 rules out cannot be rendered even by a later refactor. Proven headlessly: a cursor over untouched land read *wild*; 24 rock tiles painted around it and **the same unmoved cursor** re-read as *welcoming* (the change rides `WorldRoot.tile_changed`, so the read follows the player's edit rather than the player's hand). The cost bound is measured, not asserted-about: one read of a new tile cost **6 world queries, every one of them originated at the cursor tile** (never a scan), 50 further polls with a still cursor cost **0**, and 40 alternating reads moved `HabitatSimulation.evaluations_run` by **0** — the preview is a read, not simulation work. **What the preview cannot say, and why it matters:** gdd.md has it read the qualification system's **near-miss summary**, which is what makes it meaningful *before* anywhere qualifies — that summary is row 12's and is unbuilt, and `capacity_at()` reports the same 0 for a spot one tile short of cover as for bare ground. So the thin form ships two honest bands either side of qualification and **no "getting closer" between them**; gdd.md's own exemplar line, *"this spot is getting cozy for someone"*, therefore fires when a spot **already** suits somebody rather than at beat 4's ~0:20 "becoming" moment. The `home` band is likewise exact-tile only, because `population_at()` answers for a position and `WorldRoot` exposes no "residents in the ring" read. Also absent: avoids distance-keeping (row 9) and Discovery's near-miss summary (row 12). Roam quality is explicitly depth (spec.md → What Deepening Buys): no personality bias, no flocking, no pathfinding, no obstacle or water avoidance, no turn easing, and residents pass through each other — animals occupy no tiles. |

## Row 7 — Fact cards

| Field | Value |
|---|---|
| `thin_form` | The signature move-in card fires, **plus tap-to-replay in Inspect**. One card per species; **the Read-Aloud thin slice ships here** (🔊 via OS-level TTS). Source verification is **not** a depth axis. |
| `invariants` | Tap-to-replay is a pillar invariant, not depth — Pillar 4 delivers on curiosity, not only on success. Source verification is a hard, non-skippable gate. The Read-Aloud slice ships so the payoff is hearable by pre-fluent readers. |
| `constants` | **card dismiss behaviour is not a constant — it is spec.md → Screen Layouts, implemented as stated:** a dismiss control on the card *and* tapping anywhere outside (the full-screen scrim), with no timeout and no auto-dismiss, because a card that vanishes on its own is a timer (Pillar 1) · **TTS rate = 0.9 — DECIDED 2026-08-01 (→ D-29)** (Godot's range is 0.1–10.0 with 1.0 normal; a touch under normal for pre-fluent readers — `ReadAloud.SPEECH_RATE`) · **TTS volume = 80 — DECIDED 2026-08-01 (→ D-29)** (of 100; `ReadAloud.SPEECH_VOLUME`) · **voice is not a constant**: `ReadAloud` asks the OS for a voice in `OS.get_locale_language()` and falls back to the first available, so nothing hardcodes a machine-specific voice id · **REVERSED 2026-08-01 (→ D-29): cards DO speak themselves on open** (`FactCard.AUTO_SPEAK = true`) — closes Open Question #13. Implementation pending. A settings-level mute must exist; whether Master Volume covers Read-Aloud or it needs its own toggle is still open, carried to Row 15 (`FactCard.AUTO_SPEAK = false`) — **this is Open Question #13** (Read-Aloud default state), surfaced as a one-line switch rather than decided · **card minimum width = 620 px — DECIDED 2026-08-01 (→ D-29)** (spec.md's "never fills the screen"; ~54% of the 1152-wide default window — lives in `scenes/ui/FactCard.tscn`, not in code) |
| `implementation_location` | **declared 2026-07-27** — `project/scripts/ui/fact_card.gd` (the card; both entry points — the move-in and the Inspect replay — go through one `show_species()`), `project/scripts/ui/read_aloud.gd` (the 🔊 slice over `DisplayServer.tts_*`, which returns false rather than erroring where there is no voice), `project/scenes/ui/FactCard.tscn` (spec.md's layout), instanced inside `project/scenes/ui/GameUI.tscn`. The card is fired from `game_ui.gd` on `WorldRoot.resident_arrived` and replayed from `tap_router.gd` on an Inspect tap. **Updated 2026-07-28:** the replay tap resolves through `WorldRoot.resident_record_at()` — live resident positions — instead of the deleted `resident_index.gd`, and `_on_resident_arrived` now does exactly one thing (open the card). The UI holds no resident list; `test_fact_card.gd` asserts that structurally, so re-introducing one fails there. **Enabled in `project/project.godot`:** `audio/general/text_to_speech=true`, without which Godot's TTS API is inert. **Updated 2026-08-01 (→ D-29 #2, closing #13):** `fact_card.gd`'s `AUTO_SPEAK` flipped `false` → `true`. No new logic — `show_card()` already called `ReadAloud.speak(_spoken_text)` behind this flag (the same path the 🔊 button's `read_aloud()` uses), so every card now speaks itself the moment `show_species()`/`show_card()` makes it visible, at the same ratified TTS rate (0.9) and volume (80). Row 15's mute-path question (whether Master Volume should cover Read-Aloud, or it needs its own toggle) is untouched — left for that row. |
| `validation_status` | **pass (2026-08-01, updated → D-29) — AUTO_SPEAK IS NOW A REAL ASSERTION, NOT A PINNED `note_expected_pending()`.** `bash scripts/run-tests.sh` **51/51 green, 1655 assertions** (50/50, 1609 before this pass). **`test_fact_card.gd` 53 → 60 assertions.** The prior pass had pinned `AUTO_SPEAK = false` as a `note_expected_pending()` rather than an assertion, which is exactly why D-29 #2's flip to `true` landed silently green instead of red — closed here with a real check (`FactCard.AUTO_SPEAK == true`) plus two more that verify the WIRING, since `ReadAloud.speak()` cannot be observed to actually fire headlessly (no TTS voice in the container, so it is a no-op either way and its return value cannot tell "asked and declined" from "never asked"): a structural read of `show_card()`'s own function body confirms it reads `AUTO_SPEAK` and calls `ReadAloud.speak(_spoken_text)` — the identical call the visible 🔊 button's `read_aloud()` makes — and a live check confirms `spoken_text()` is exactly the card's own title + body the instant `show_species()` makes it visible, so a card genuinely reads itself aloud on becoming visible with no manual tap, through the same path the button always used. **Everything this row already had stands unchanged and still green:** `test_fact_card.gd` (**40 assertions**, was 39) on the real `Main.tscn`. **The load-bearing assertion is unchanged and still EQUALITY, not presence:** the card's rendered title and body are read off `%SpeciesName` / `%Body` and compared `==` against the species' `display_name` and `fact_text`, for every species in the roster, with a non-vacuity check that the three fact texts really differ. Both entry points are still proven to be one function, including that the move-in fires through the **real `WorldRoot.resident_arrived` connection** (the connection itself is asserted). **REVIEWED THIS PASS — ui-engineer edited this suite, and the edits are sound with one weakness QA closed.** The old `_ui.residents.count()` assertion was replaced by a **structural** one: `GameUI` must declare no `residents` property, so re-introducing a UI-side resident list fails here. That is the right shape — but an absence-of-a-name check passes for free if the name list is empty, so QA added the missing non-vacuity control: the property list is asserted to really enumerate `GameUI`'s own members (42 properties, `_world` and `_camera` among them), so the absence is a measurement. Negative control run: pointing the same check at `_world` turns it red. The replay fixture was also rewritten by ui-engineer to register a **real `HomeSite` with a real node** and hit-test through `WorldRoot.resident_record_at()` at `ResidentPicker.BODY_CENTRE_HEIGHT`, replacing the deleted `ResidentIndex`; it cleans up after itself (residents cleared, site unregistered) so it leaks no home site into later checks. **What it does not do, and does not claim to: it teleports rather than roams.** Tap-to-replay against a resident moved by its **own `ResidentRoamer`** is asserted in the new `test_resident_lookup.gd` instead — an Inspect tap on a genuinely walking rabbit replays its card, and a tap where it arrived opens no card at all. Dismiss is still asserted on both routes, including that a tap while the card is open is swallowed and does not paint the tile behind it. No auto-dismiss is still asserted structurally — `FactCard` declares no `_process` and the scene contains no `Timer`. Read-Aloud still degrades as designed headless. **`human.tres`'s `PLACEHOLDER` copy is still pinned rendering EXACTLY as authored** (`note_expected_pending()`, **#31**), and `AUTO_SPEAK = false` (**#13**) is still a pinned switch. **Not testable headless:** that a voice actually speaks — a human check on a real desktop. **New this pass and also not testable headless:** the card's portrait frame is still empty for every species. |
| `human_gate` | **2026-08-09 — full gate. Supersedes the 2026-08-01 partial gate.** TTS rate (0.9), volume (80) and card minimum width (620px) ratified as-is. `AUTO_SPEAK` reversed false → true, closing Open Question #13. **Both items the partial gate was waiting on are now verified on a real desktop: approved.** A voice audibly reads the title + body on a move-in card and on tap-to-replay, for all three floor species, at the ratified rate and volume. On a machine with no matching-locale voice, the fallback (first available voice) neither errors nor hangs. **Named separately, not blocking this gate:** the card's portrait frame is still an empty rectangle for every species — a missing per-species-asset gap (art/content pipeline work), not an unverified behaviour, and Row 15's Read-Aloud mute-path question is untouched, carried there as before. |
| `depth` | not started |
| `est_hours` | 1.5–2 |
| `actual_hours` | — |
| `status` | ✅ — **built, headless-validated, and heard out loud (2026-08-09, full gate — see `human_gate`).** `FactCard.tscn` is spec.md's layout — a scrimmed card that never fills the screen, with no timeout and no auto-dismiss (structurally: `fact_card.gd` declares no `_process` and the scene contains no `Timer`). Both entry points are literally one function, `show_species()`: the move-in fires off the real `WorldRoot.resident_arrived` connection, and tap-to-replay in Inspect goes through `TapRouter` against the animal's live position, so a wandering resident replays its card where it actually stands and an empty den no longer opens one. Title and body are read from the species' own `display_name` / `fact_text`, so the card cannot drift from source-verified copy. `read_aloud.gd` is the 🔊 slice over `DisplayServer.tts_*`, confirmed to actually speak on a real desktop. **What is NOT here, and is disclosed, not blocking:** the card's **portrait frame is empty** — `Card/Stack/Portrait` is a 220×150 `PanelContainer` with nothing inside it (needs a per-species portrait or rendered model view; art/content pipeline work, not scoped here); and `human.tres`'s copy still literally reads `PLACEHOLDER` (**#31** — content-writer's, not this row's). |

## Row 8 — The decided roster

| Field | Value |
|---|---|
| `thin_form` | **Human, Fox, Rabbit.** Fox and Rabbit are cleared and shipped; **Human still faces both gates — asset audit (#4) and fact-card content (#31)**. Everything beyond the three is depth. |
| `invariants` | Every species passes the structural predation check and the avoids symmetry rule before shipping. A floor species failing audit substitutes from the base asset set with no re-look pass. |
| `constants` | per-species values live in roster.md, not here; the three capacity constants gate at row 6 |
| `implementation_location` | `project/data/animals/` — `fox.tres`, `rabbit.tres` |
| `validation_status` | fox and rabbit pass — `test_fox_schema`, `test_rabbit_schema`, `test_fox_spawn`, `test_rabbit_spawn`, `test_inert_land_invariant`, plus per-model animation suites. **Human: pass (2026-07-27)** — `test_human_schema.gd` (41 assertions) validates `human.tres` against the `AnimalDefinition` contract and roster.md's decided row, with `validate()` reporting exactly one problem (the `fact_text` placeholder, #31) and a real-copy clone validating clean; the villager also lands end to end in `test_causality_end_to_end.gd`. **The copy itself is still unwritten and unvalidatable** — per-item detail: [content-pipeline-status.md](content-pipeline-status.md). |
| `human_gate` | **Corrected 2026-08-01 — this entry was stale.** `content-pipeline-status.md` (the sign-off record of owner) already shows Rabbit and Fox backfilled 2026-07-28 (→ D-27 #4), and Human approved 2026-07-28 (→ D-28). All three floor species are signed off; this row's note simply hadn't been updated to match. |
| `depth` | not started. **The cleared pool — deer, stag, horse, donkey, cow, bull, alpaca, husky, shiba_inu — is imported, audited, attributed and import-tested; steps 3/4/5/8 open on each.** The cheapest available depth in the project, because the expensive gates are behind them. **Not Tier 1.** Sequence at the velocity review; batch by step, not by species. |
| `est_hours` | 1.5–2 (one Add-an-Animal run: Human) |
| `actual_hours` | — |
| `status` | ✅ — all three floor species (Rabbit, Fox, Human) are sourced, data-entered, copy-verified and human-signed-off; see per-item detail in [content-pipeline-status.md](content-pipeline-status.md). The cleared-pool depth backlog (deer, stag, horse, donkey, cow, bull, alpaca, husky, shiba_inu — steps 3/4/5/8 open on each) is tracked separately and is not Tier 1. |

## Row 9 — Minimal Avoids

| Field | Value |
|---|---|
| `thin_form` | One real pair, **Rabbit ↔ Fox**, already in the thin roster, so mutual distance-keeping ships with it; symmetry rule and copy framing ship whole. |
| `invariants` | Avoids symmetry is not a depth axis. Avoids never gates a move-in — it affects distance-keeping only. Copy frames it as "keeps its distance," never as threat (the operational predation ban). |
| `constants` | **DECIDED 2026-08-01 (→ D-29) — avoid distance = 5 tiles; re-pathing cadence piggybacks on Row 6's wander pause (2–6 s randomised).** Nothing built yet — this closes step 2 only, not step 3 |
| `implementation_location` | **Landed 2026-08-01 (D-29 #4), `project/scripts/world/`** (not `simulation/` — this is roam bias, presentation-layer, same as the rest of wander): `resident_roamer.gd` gained `AVOID_DISTANCE_TILES` (5, decided), `AVOID_BIAS_HALF_ARC_RADIANS` (proposed, no GDD number exists), `species_id()`/`avoid_ids()`/`set_nearby_avoid_provider()`, and `_pick_angle()` — called only from the existing `_pick_waypoint()`, itself only reached once per wander-pause cadence (`PAUSE_MIN/MAX_SECONDS`, row 6's), so no new timer and no per-frame cost. `resident_presentation.gd` gained the roster reference (`attach()`'s new optional 4th argument), resolves each new resident's `avoids` list in `present()`, and owns `_nearby_avoid_positions()` — the only place the symmetric union of two species' `avoids` lists is actually resolved (gdd.md: the relation "may be declared on either species... the resolver must union both directions"), so a pair declared on only one side behaves identically to one declared on both. One line changed in `project/scripts/world/world_root.gd`: `presentation.attach(grid, _props_root)` → `presentation.attach(grid, _props_root, 0, roster)`. No change to `HabitatSimulation` or anything in `project/scripts/simulation/` — avoids never gates a move-in, so it never touches the qualification/arrival path, only roam. Self-verified with a scratch probe (since deleted): a predator whose own `avoids` names `prey` biases 200/200 waypoint picks away from a nearby prey (never expanding past its home radius), stops biasing once prey moves outside 5 tiles, and — the symmetry check — a prey with an EMPTY `avoids` list still biases away from a nearby predator purely through the union, because the predator's side names it. `test_resident_wander.gd` (67/67) and `test_home_prop.gd` re-run clean against this change specifically (the latter's 2 reds are the unrelated row-3 `wild_grass` consequence noted above, confirmed by isolating the two changes). Not yet exercised by a dedicated `test_avoids_*.gd` suite — that's QA's step 4. |
| `validation_status` | **pass (2026-08-01, → D-29) — BEHAVIOUR IS NOW VALIDATED, NOT JUST SCHEMA.** `bash scripts/run-tests.sh` **53/53 green, 1702 assertions.** **`test_avoids_distance_keeping.gd` (29 assertions), new — no suite exercised this row's behaviour at all before this pass**, exactly as this field itself said ("Not yet exercised by a dedicated `test_avoids_*.gd` suite — that's QA's step 4"). Four things pinned, matching the human's own brief: **(1) THE BIAS IS GENUINELY AWAY, EXACTLY, NOT ON AVERAGE.** `_pick_angle()`'s own contract is a hard bound (chosen angle within 90° of true "away"), so every waypoint pick made while a threat was in reach is recovered EXACTLY — a roamer's own `position` at the instant of arrival IS `_target`, verbatim (`ResidentRoamer.tick()` snaps to it, never approaches asymptotically) — and checked against that bound directly, never against a re-derived `atan2` that would share a sign error with the implementation. Two synthetic roamers, driven by hand in fixed order over 240 simulated seconds: the predator's 24 picks (13 in-reach) all landed within the away cone, worst measured 1.302 of a 1.571 rad (90°) limit; a genuinely uniform control sample misses the same cone 97 of 200 times, so the zero violations is a real bound holding and not a cone nothing could miss. **(2) THE SYMMETRIC UNION HOLDS FROM ASYMMETRIC DATA.** The fixture deliberately gives the predator `avoids = ["prey"]` and the prey an EMPTY `avoids` list — and the prey's own 32 picks (16 in-reach) are bounded identically, purely because `_nearby_avoid_positions()` checks both directions regardless of which side declared it. **(3) AVOIDS NEVER GATES A MOVE-IN.** Structural: `HabitatSimulation` and `ArrivalQueue`'s own CODE (comments stripped) names nothing "avoid" at all. Causal: the shipped rabbit/fox pair (already mutual avoiders in `fox.tres`/`rabbit.tres`) both qualify and land through the ordinary arrival path at habitat placed 4 tiles apart — closer than the 5-tile avoid distance — proving proximity between declared avoid-partners costs neither a move-in. **(4) NO ADDED PER-FRAME COST.** On the real `Main.tscn` with a landed rabbit/fox pair wandering near each other, 2400 hand-driven steps moved `HabitatSimulation.evaluations_run` by exactly 0 and enqueued nothing, with only 349 Idle↔Walk transitions across those 2400 frames — confirming the provider rides the existing wander-pause cadence rather than a new timer. **A REAL BUG WAS FOUND AND FIXED IN THIS TEST DURING AUTHORING, RECORDED SO THE FIX ITSELF IS AUDITABLE:** the first draft measured "in reach" from each roamer's HOME position rather than its own current position (which is what `_nearby_avoid_positions()` actually measures from — a resident can be anywhere in its own wander disc when the next pick happens), producing false violations; corrected before this pass's numbers were taken, and the corrected version's worst-case margins (1.302 and 1.489 rad against a 1.571 rad limit) leave no doubt the bound genuinely holds. Schema side (unchanged, still green): `avoids` id form, self-reference and unresolved-id validation in `animal_definition.gd`. |
| `human_gate` | 2026-08-01 (→ D-29) — constants decided: avoid distance 5 tiles, re-pathing cadence piggybacked on Row 6's wander pause (2-6s). Built 2026-08-01 (D-29 #4) and validated the same day: test_avoids_distance_keeping.gd (29 assertions) proves the bias is genuinely away with zero violations across 240 simulated seconds, the symmetric union holds from asymmetric data, avoids never gates a move-in (shipped rabbit/fox pair lands 4 tiles apart, inside the 5-tile avoid distance, via the ordinary arrival path), and there is no added per-frame cost (2400 steps, `evaluations_run` +0). **Row-level sign-off recorded.** |
| `depth` | not started — a second pair drawn from the cleared pool; the village dogs (Husky / Shiba Inu) are the natural candidate |
| `est_hours` | 1 |
| `actual_hours` | — |
| `status` | ✅ — data exists (`fox.tres` avoids rabbit; `rabbit.tres` avoids fox) and is schema-validated with zero problems. **Distance-keeping behaviour landed 2026-08-01 (D-29 #4) and is validated**, not merely schema-checked: `test_avoids_distance_keeping.gd` (29 assertions) proves the bias away from a nearby avoid-partner is genuinely within the 90° bound (worst margin 1.302 of 1.571 rad across 240 simulated seconds), holds symmetrically even when only one side declares the relation, never gates a move-in, and costs nothing per frame. |

## Row 10 — Gentle displacement

| Field | Value |
|---|---|
| `thin_form` | Guarantee and no-unexplained-vanish rule ship whole (pillar invariant), **including the warning's specifics** — from the capacity formula, mode-agnostic, evaluated at settlement — and **it carries the Read-Aloud slice**. Only presentation thins. |
| `invariants` | The whole guarantee is a pillar invariant: nothing ever leaves unexplained, one warning per settled gesture, relocation attempted before departure. The warning must also fire for **capacity-reducing terraform**, not only for building placement. The villager-displacement copy doctrine is decided and ships as written. |
| `constants` | **settlement / grace window = 12.0 s — DECIDED 2026-08-01 (→ D-29)** (#16; spec.md → Pacing Constants' stated "~10–15 s" band, midpoint; `SettlementWindow.GRACE_WINDOW_SECONDS`. **One number serves both halves**: the irreversible-consequence gate here and row 3's 100%-refund window, because spec.md gives them one table row. Splitting them is a human call) · **relocation search radius = 8 tiles — DECIDED 2026-08-01 (→ D-29)** (**no GDD or spec.md baseline exists** — gdd.md says only "if a suitable spot exists"; 8 is the low end of the #20 home-site radius band, so a family relocates roughly within its own neighbourhood rather than teleporting across the map; `GentleDisplacement.RELOCATION_SEARCH_RADIUS_TILES`) · **deliberately NOT a constant: there is no "warning threshold relative to capacity."** gdd.md states the trigger exactly — "`capacity(h, S)` would fall **below** `population(h, S)` … including to 0" — so the predicate is `capacity < population`, strict, with no margin. A standing tolerance margin for a *settled* neighbourhood is **#25**, explicitly still open and explicitly not v1 · **presentation half, ui-engineer:** **`DisplacementNotice.MOMENT_SECONDS = 4.5` — DECIDED 2026-08-01 (→ D-29)** (one consequence line's dwell on the banner; sized for a fluent 8-year-old reading a one-sentence line twice — spec.md has no banner constant and row 12's News Report toast, which will own the register, is unbuilt) · **`DisplacementNotice.MARKER_SECONDS = 1.6` — DECIDED 2026-08-01 (→ D-29)** (the move marker's fade) · `DisplacementNotice.HOLD_MOMENTS_UNTIL_DISMISSED = true` (**the consequence beat** — see `status`) · **`DisplacementNotice.MAX_QUEUED_MOMENTS = 8` — DECIDED 2026-08-01 (→ D-29)** (a cap so a dialogue left open cannot bank an unbounded backlog; drops oldest) · **`MARKER_RADIUS = 26 px` / `MARKER_RADIUS_END = 54 px` / `MARKER_WIDTH = 5 px` / `MARKER_DASH = 14 px` — DECIDED 2026-08-09 (human gate, seen on screen for the first time and approved with no retune)** · **`UiPalette.NOTICE_FACE = SAND` — DECIDED 2026-08-09 (human gate)** (the warning panel's face — the title screen's existing second face colour, not a new hue, so the warning is unmistakably not a cream fact card or toast) · **`UiPalette.MOVE_TRAIL = BARK @ 0.55α` — DECIDED 2026-08-09 (human gate)** (**deliberately neither `CUE_ACCEPT` nor `CUE_SOFT`** — those two are the whole tap-feedback vocabulary and reusing either would teach one colour two meanings) · **`UiPalette.FONT_NOTICE_LEAD = 32` / `FONT_NOTICE_LINE = 26` / `FONT_BANNER = 26` — DECIDED 2026-08-09 (human gate)** · `GameHud.REMOVE_ENTRY_LABEL` is a `[COPY]` stub (see `status`) |
| `implementation_location` | **declared 2026-07-28, before building** — `project/scripts/simulation/settlement_window.gd` (the per-neighbourhood restartable window; owns `GRACE_WINDOW_SECONDS`, which row 3's refund policy also reads), `project/scripts/simulation/gentle_displacement.gd` (the settlement-time warning, the relocation search, the departure), plus edits to `project/scripts/simulation/home_site_registry.gd` (`release()`, `relocate()`), `project/scripts/world/world_root.gd` (the three public signals + wiring) and `project/scripts/world/resident_presentation.gd` (prop/roamer follow on relocate). **No new `on_*` method on `HabitatSimulation`** — displacement rides the existing four triggers, and the displacement signals deliberately live on their own node so `test_event_driven_simulation.gd`'s "the simulation declares exactly two signals" assertion stays true. **Presentation half, built 2026-07-28 (ui-engineer):** `project/scripts/ui/displacement_copy.gd` (content-writer's `docs/content/displacement-copy.md` verbatim — leads, per-family warning lines, departure and relocation lines, plus the floor-coverage guard; **an interim home**, see row 12's schema gap), `project/scripts/ui/displacement_notice.gd` + `project/scenes/ui/DisplacementNotice.tscn` (the one-dialogue warning with Read-Aloud 🔊, the consequence banner, the move marker), plus edits to `project/scripts/ui/game_ui.gd` (the three signals wired), `project/scripts/ui/ui_palette.gd` (`NOTICE_FACE`, `MOVE_TRAIL`, three type sizes), `project/scripts/ui/game_hud.gd` (the removal palette tool; the two `[COPY]` preview bands replaced with approved copy) and `project/scripts/ui/tap_router.gd` (`RESULT_REMOVED`, the remove tap). **`WorldRoot.remove_at()` is now reachable from the UI** and it is *not* a fourth mode. **Updated 2026-08-01 (→ D-29 #7, shared with row 2):** `project/scripts/ui/tap_router.gd`'s `handle_tap()` made mode-aware — the resident-wins-the-tap check now only runs in Inspect, so in Terraform/Build `_tap_remove()` (and every other tile action) resolves against the tapped tile regardless of a resident's hitbox, which is what makes the field beside an occupied House reachable for the remove tool while the family is home. No new file; the fix lives entirely in row 2's already-claimed `tap_router.gd` — row 10 declares no new directory for it. |
| `validation_status` | **pass (2026-08-01, updated → D-29) — THE REACHABILITY CONFLICT (SHARED WITH ROW 2) AND THE ARRIVAL-CHECK EXTENSION (#5) ARE BOTH NOW DIRECTLY VERIFIED.** `bash scripts/run-tests.sh` **53/53 green, 1702 assertions** (50/50, 1550 before this pass). **`test_gentle_displacement.gd` re-pointed for `wild_grass`** (130 → 147 assertions): the villager fixture's `HOUSE_TILE`/`SPARE_HOUSE` now paint `grass` explicitly before `place_building()`, since a House's `allowed_terrain = [\"grass\"]` and the world no longer arrives pre-painted that way — same shape as every other row-6/7 re-point this pass, nothing weakened. **THE RULING ROW 10 NEEDED IS RESOLVED, VERIFIED HERE, THIS PASS: the floor's likeliest displacement — clearing the field beside an occupied House — is REACHABLE BY TAP.** The conflict this row's `human_gate` named ("the animal-wins-the-tap priority rule makes the floor's likeliest displacement unreachable while the villager is home") is closed by D-29 #7's mode-aware priority rule, shared with row 2: `test_mode_tap_model.gd` and `test_resident_lookup.gd` both now assert directly that in Terraform/Build the tile action wins even with a resident standing on the tapped tile (no fact card opens, the resident query never runs) — see row 2's own entry for the full account. `test_removal_refund.gd` (unchanged, still 77/77) continues to prove the mechanical removal-while-occupied path this reachability fix now lets a player actually reach by tap. **NEW THIS PASS — `_check_arrival_extends_to_a_competing_neighbour()` added to `test_gentle_displacement.gd`, directly reproducing and confirming D-29 #5** (the neighbour-capacity-overshoot fix, filed under row 6's `implementation_location` but landing here since `on_arrival()` lives in this row's own `gentle_displacement.gd`): a synthetic two-site fixture reproduces the exact overshoot the build report measured (site B's real arrival, through `HabitatSimulation`'s own path, steals four of site A's eight cover tiles and drops A to capacity 1 against a population of 2) and confirms `on_arrival()` arms A's settlement window the instant B lands — before any clock advances — then that the window resolves it exactly as `_check_the_trigger_is_exact()` etc. already prove more generally: one warning naming A (`capacity: 1`, `population: 2`), a departure of the one excess resident (no relocation destination exists), B itself untouched throughout. Re-run 3× (seeded `ArrivalQueue`): identical every time. **THE PRESENTATION HALF IS STILL NOT VALIDATED, unchanged and still open** — `DisplacementNotice` / `displacement_copy.gd` still have no suite exercising them; the payload contract is pinned, the widgets are not. **Everything already validated on 2026-07-28 stands, unchanged and still green:** **`test_gentle_displacement.gd` (130 assertions), new.** **The trigger is asserted as an `iff`, not as a positive:** capacity swept across seven values against a fixed population of 2 (capacities 3, 2, 2, 1, 1, 0, 0) with warn/silent asserted at every point, and **the boundary is asserted on both sides** — `capacity == population` must **not** warn and `capacity == population − 1` must, which is the one assertion an implementation using `<=` would fail and nothing else would catch. Falling **to 0** warns and reports `capacity: 0` verbatim, because 0 is where an implementation is most tempted to treat a neighbourhood as invalid rather than as unsuitable. Non-vacuity on the sweep itself: it produced **both** outcomes (4 warned, 3 silent), so it is not a machine stuck on one answer. **Mode-agnostic, three ways:** behaviourally in a synthetic fixture (a terraform, a building placed *over* cover, and a tag-emitting building removed — all three warned identically); structurally (`GentleDisplacement.on_edit()` takes exactly one argument and it is a `Vector2i`, so there is no mode to branch on); and on the shipped API, where `paint_tile()` / `place_building()` / `remove_at()` each arm and restart the *same* neighbourhood's window, with a non-vacuity control that a tile nobody lives near reports no pending settlement at all. **One warning per settled gesture, summarising every affected home** — three overlapping home sites across **two** species, three taps, **one** gesture, **one** warning naming all three homes, every entry satisfying `capacity < population`, and `species_ids` de-duplicated to two (3 homes − 2 ids = 1, so the de-duplication is a measurement). **Warning first, consequence after** is asserted on a single shared event log (`["warned", "departed"]`), holds again on a second displacement in the same world (a warning is never suppressed because one was already shown), and **the consequence still runs with nobody connected to `displacement_warned` at all** — the converse trap of an implementation that only acts when someone is listening. **Relocation preferred, with the destination rule's own boundary:** a spot where `capacity == population` takes the family (whole household moved, nobody lost); **one tile short and the identical world departs instead**; nowhere to go departs. **No fail state**, asserted structurally as well as behaviourally: row 10's whole signal surface is three signals and none is an error channel, no method on it or on `WorldRoot` contains block/cancel/confirm/deny/refuse/reject/veto/abort/forbid, and there is **no tolerance constant** (a standing margin is #25 and is not v1) — with a non-vacuity control that the method list really enumerates the class. **On the real `Main.tscn`, the floor's likeliest displacement is driven end to end:** a villager family moves in through the ordinary path, `can_remove()` says **yes** on the occupied House, `remove_at()` **succeeds** (refusing would be a fail state), the family is still there while the window is open, and at settlement one warning fires carrying `read_aloud: true`, `is_structure_home: true`, `display_name: "Villager"`, `capacity: 0`, `population: 1`, `outcome: depart`, `copy_key: displacement.warn.depart.structure` — then the departure. **Species Hosted survives it** on the public API while `resident_species_ids()` correctly drops the villager. **`test_settlement_window.gd` (81 assertions), new.** Immediate arithmetic vs deferred consequence asserted with **nothing ticked in between**: capacity moves 2 → 1 on the same call as the edit and the neighbourhood is marked dirty, while warnings, departures, relocations and settlements are all 0 — then the same fixture proves the consequence really does arrive, so the zeros are deferral and not inaction. **Restarts are uncapped, driven 60 times across 660 simulated seconds (55 grace windows)** with the clock asserted to reset to a full 12.0 s on every one, nothing settled, both residents still home, and **still exactly one gesture** — then stop tapping and the very next window closes. **Revert-inside-the-window is asserted on FREE terrain**, which is the case that makes it a property of the arithmetic rather than of the ledger: the edit's `refund_for()` is **0 Wood**, so there is no transaction to carry the undo; painting it back by hand mid-window makes the settled gesture raise **no warning, no departure, no relocation**, with the population, the home's position and the registry all unchanged — **and the negative control is the assertion that gives it meaning**: the identical edit left alone warns exactly once. Five taps in one neighbourhood coalesce into one gesture and one warning naming one home (never one per tile), with a later gesture warning again so the "one" is coalescing rather than a cap. **Arrivals are proven to sit outside the window** by jamming it open: an edit every five simulated seconds for 400 s keeps `settlements_resolved` at **0** and a resident still moves in — asserted to have landed *while* a gesture was pending — with a structural half showing `HabitatSimulation` and `ArrivalQueue` name neither `SettlementWindow` nor `GentleDisplacement` (control: the same stripped source does contain `ArrivalQueue`). **`test_event_driven_simulation.gd` extended 53 → 66** with row 10's CPU question, **verified independently rather than taken on report**: 50 edits on a resident-free world open **no** gesture, **500 displacement ticks resolve nothing** and move `evaluations_run` by exactly 0, and after a real gesture settles another 500 ticks resolve nothing further — so idleness is a state the world *returns* to. Non-vacuity: with someone actually living there the same call does open a gesture. **`test_tile_exclusivity.gd` extended 26 → 40:** `release()` and `relocate()` are now driven against the four exclusivity rules — a released site's tiles really go back to its neighbour (26 → 43 contested tiles, including the one that used to be strictly nearer it), a relocated site keeps its **sequence** so the tie-break still reads it as the older site, its old tile is unclaimed and its new one claimed, and a **House left standing keeps its home site and its own `house` tile** rather than being removed. **MUTATION-VERIFIED, restored byte-identical afterwards.** Three mutations, each emulating the actual regression rather than flipping an expected value: `capacity >= population` → `capacity > population` (the non-strict trigger) turned **11 assertions red across both fixtures**, led by the two boundary cases; `_affected_homes()` stubbed to return nothing ("never warn") turned **6 red**, including the revert-inside-the-window **negative control**, which is precisely the assertion a weak revert test would lack; and removing the window restart turned **12 red**, including the uncapped-restart drive and the arrival-outside-the-window jam. **THE PRESENTATION HALF IS NOT VALIDATED.** ui-engineer's `displacement_notice.gd` / `DisplacementNotice.tscn` / `displacement_copy.gd` landed in parallel with this pass and **no suite exercises them** — the payload contract they consume is pinned here (field by field, on the real signal), the widgets that render it are not. **REPORTED DEFECT (`resident_presentation.gd`, gameplay-engineer's file, not patched):** `release()` erases its prop-map entry immediately but frees the node with `queue_free()`, and row 10's `_relocate()` now calls `release()` then `present()` for the same home **inside one frame** — so `HomeProps` holds 3 children for 2 live dens for a frame, and the replacement den is forced to take a mangled node name. One-frame **visual** artefact only: no tile, no collision, no capacity effect, and every count stays correct (asserted). Repro and measurement are in `test_home_prop.gd`; fix is a direct `free()` or a defer in `release()`. **Not machine-checkable and not claimed:** whether the warning reads as gentle, whether a six-year-old gets their tap back inside 12 s, and whether a voice actually speaks it — all step-5 kid-playtest and human-desktop checks. |
| `human_gate` | **2026-08-09 — full gate. Supersedes the 2026-08-01 partial gate.** Relocation search radius (8 tiles) and the presentation timing constants (banner dwell 4.5s, marker fade 1.6s, queue cap 8) already ratified as-is; the mode-aware tap-priority fix (shared with Row 2) already resolved and implemented. **The presentation half, seen by a human for the first time this pass, is fully approved, no retunes:** the sand-panel warning (`NOTICE_FACE = SAND`) reads as clearly distinct from a cream fact card or a News Report toast; the move marker (26→54px radius, 5px width, 14px dash, 1.6s fade) reads as "this resident moved from here to there," not as noise or a tap-cue; the lead/body font sizes (32/26) and banner size (26) read comfortably at normal viewing distance for the target age; the move-trail (`BARK @ 0.55α`) stays visually distinct from the tap-feedback cues; and the warning reads as **gentle** — a kid can get their tap back inside the 12s grace window. **This row closes in full.** Two previously-reported items stand, disclosed, and do not block this gate: the one-frame `HomeProps` visual artefact on same-frame relocate (`resident_presentation.gd`, gameplay-engineer's file, unpatched — no tile/collision/capacity effect), and `WARN_HUMAN_FIELD`/`WARN_HUMAN_HOUSE` being unreachable so the floor ships the vaguer `WARN_HUMAN` (the payload doesn't expose which need fell short). |
| `depth` | thin |
| `est_hours` | 2–3 |
| `actual_hours` | — |
| `status` | ✅ — **mechanism and presentation both built thin and now through the human gate (2026-08-09, full gate — see `human_gate`).** The settlement rule, the warning, relocation and departure all exist, run headlessly, and have been played and approved by a human on a real build. Capacity is still re-evaluated immediately on every edit (unchanged); what waits for the window is only the irreversible half. Arrivals remain outside the window.<br><br>**Presentation half (ui-engineer, 2026-07-28).** One `displacement_warned` emission renders **one** dialogue: a lead sentence, then one line per affected home, in payload order, never re-sorted and never flattened — content-writer's split form, which is what makes gdd.md's forbidden sentence (the player's action and a family's hardship in one sentence) *unwriteable* rather than avoided. It carries Read-Aloud 🔊 on the whole dialogue, lead first, and degrades to no button at all where there is no voice — the text is never gated on it. It is **purely informational**: `DisplacementNotice` declares exactly one signal (`warning_dismissed`, which nothing branches on), the dialogue has exactly one visible control, and there is no channel back into the simulation. It carries **no scrim** — gdd.md's "no modal blocks the world" — so the world stays live, pannable and editable behind it, which is what makes the ordinary undo path real rather than claimed. It dismisses to nothing: no badge, no counter, no history.<br><br>**Register, made structural rather than intended.** The warning is a centred sand panel; the after-the-fact consequence lines use a top-centre cream banner, which is the toast register spec.md gives News Reports. They are different widgets in different places, so consent copy cannot be mistaken for flavour.<br><br>**The beat, decided here because it is presentation.** `GentleDisplacement` emits the warning and its consequences inside one settlement, so there is no enforced gap and the view layer must not add one to the simulation. What the UI *can* order is what the player is told: consequence lines are **held while the disclosure is on screen** and released on dismissal (`HOLD_MOMENTS_UNTIL_DISMISSED`). The residents themselves have already moved by then — that is the simulation's own timing and is not fixable from here.<br><br>**Two `[COPY]` preview stubs closed** with content-writer's approved lines (`this land is wild` / `this spot is somebody's home`), both clean of the ten habitat-tag words. **One new `[COPY]` stub opened:** `GameHud.REMOVE_ENTRY_LABEL`, the removal tool's palette label — the handoff doc was never asked for it.<br><br>**`WARN_HUMAN_FIELD` / `WARN_HUMAN_HOUSE` are unreachable and the floor ships the vaguer `WARN_HUMAN`.** The warning payload exposes `capacity`, `population` and `is_structure_home` but **not which need fell short**, and `is_structure_home` cannot recover it (it is true whether the field was cleared or the House taken down). `GentleDisplacement._describe()` already computes the min-over-needs and could return the argmin for free; one payload field makes the floor's likeliest displacement read sharply. Reported, not worked around. |

## Row 11 — Species status

| Field | Value |
|---|---|
| `thin_form` | HUD counters + a flat Field Guide list. **Amended 2026-08-10 (D-40): the list is all-time-hosted-or-silhouette, not resident-state only** — every roster species renders a row, silhouetted until ever hosted; the "hinted at" column still needs row 12's per-species News Reports. |
| `invariants` | Counters are informational, never goals (Pillar 1 — no win/loss). |
| `constants` | **Updated 2026-08-07 (ui-engineer).** None — confirms `thin_form`'s own note. Nothing here is a tuning number in spec.md's sense: the four counters and the Field Guide list are plain reads of `WorldRoot` state that already exists (`species_hosted_count()`, `resident_species_ids()`, and this row's own addition, `population_of(species_id)`). One **visual** judgment call surfaced during the build, not a gated constant: `UiPalette.FONT_HUD_SECONDARY = 22`, sized down from Wood's `FONT_HUD = 30` so the three added counter rows fit the top-left corner without towering over it — flagged under Proposals for a taste pass, not blocking. |
| `implementation_location` | **Declared and built 2026-08-07 (ui-engineer).** New: `project/scripts/ui/field_guide.gd` + `project/scenes/ui/FieldGuide.tscn` (the screen). Edits: `project/scripts/ui/game_hud.gd` (three counter setters/getters, the top-right Field Guide button, `field_guide_pressed` signal), `project/scripts/ui/game_ui.gd` (wiring — `_refresh_counters()` on bind/arrival/departure, opens `FieldGuide` on the HUD signal), `project/scenes/ui/GameUI.tscn` (the counter rows, the top-right button, the `FieldGuide` instance), `project/scripts/ui/ui_palette.gd` (`FONT_HUD_SECONDARY`). Data-layer additions this row needed and the other in-flight rows (2, 7) do not touch: `project/scripts/simulation/home_site_registry.gd` (`population_of(species_id)`, summing population across every site of one species — `total_residents()` cannot answer "how many villagers" without it) and its one-line wrapper on `project/scripts/world/world_root.gd`. Checked against rows 2/7's concurrent claims on `game_hud.gd`/`game_ui.gd` (D-33 camera work, `fact_card.gd`) before editing — no overlapping nodes or methods touched; row 11's edits are additive (new signal, new setters, new onready refs) and every existing connection/call in both files is unchanged. |
| `validation_status` | Verified by hand (see `status`); no checked-in headless suite yet — qa-engineer's to add. |
| `human_gate` | **Full sign-off 2026-08-08 — see D-34.** All six proposals ratified: all four counters stay persistent on the HUD (spec.md's sketch corrected to match); "Currently Resident" is distinct-species count; Village Population's hardcoded `"human"` id accepted as-is; `FONT_HUD_SECONDARY = 22` ratified; the empty-state copy approved as written; Species Hosted stays a bare count with no denominator. **Amended 2026-08-10 — see D-40**, on two of the six: the empty-state copy (#5) is retired, not carried forward — the state it described no longer exists once every roster species always renders a row; a new `[COPY]` stub is open. Species Hosted stays a bare count (#6 unchanged for the counter itself), but the Field Guide's row list now renders one row per roster species (discovered or silhouetted), a deliberate, narrowly-scoped exception D-40 states in full — not a reopening of #6. |
| `depth` | not started |
| `est_hours` | 1 (half of the paired 11–12 ledger line) |
| `actual_hours` | — |
| `status` | ✅ — **closed 2026-08-08 (human gate, D-34).** Built thin 2026-08-07 (ui-engineer). All four top-level counters (Resources, Species Hosted, Currently Resident, Village Population) render in the HUD's top-left corner **at all times in both first-person and map-peek view** — true by construction, since `GameHud` is never hidden or rebuilt when `CameraRig` swaps the live camera for the peek, only the camera changes. None of the four can flash, tween, or gate anything (Pillar 1's indicator test): each setter (`set_wood()` and its three new siblings) assigns a string and does nothing else. The Field Guide, reachable from the new top-right icon, opens over the still-simulating world (light scrim, no pause, tap-outside dismisses — the same non-modal treatment as the fact card) and renders a **flat, unranked list** of every currently-resident species by `display_name`, plus the all-time Species Hosted count as a bare integer with **no denominator anywhere on the screen** — gdd.md's ban is read precisely as forbidding the ratio "N of the total species that exist," not the running tally itself, which the Economy section names as one of the four counters directly. Both the counters and the guide's list refresh live on `resident_arrived`/`resident_departed` (open or closed), so a move-in or a Gentle Displacement departure updates the screen without a re-open. **Two semantic reads made without a spec line to point to, both flagged under Proposals:** "Currently Resident" counts **distinct species**, not individuals (`resident_species_ids().size()`, not `total_residents()`) — inferred from `HomeSiteRegistry.resident_species_ids()`'s own doc comment, left by row 10's build, naming it as feeding this exact counter; and Village Population reads `population_of("human")`, a hardcoded species id with no data-side flag marking "the villager species" (matches the precedent `displacement_copy.gd` already set for the same id, but is fragile the same way that file's `FLOOR_SPECIES_IDS` is). **Out of scope by the dispatch brief, not by oversight:** the "hinted at" column (row 12 — `AnimalDefinition` has no News Report field yet, per that row's own status) and any per-species detail page (spec.md's own depth-axis line for this row). **One `[COPY]` stub opened:** `FieldGuide.EMPTY_STATE_TEXT`, shown before any species has ever moved in — no approved string existed for this case. **Verified by hand, not by a checked-in suite** (this row's own `validation_status` is qa-engineer's to fill): three scratch scripts confirmed `HomeSiteRegistry.population_of()` sums correctly across multiple sites of one species and stays 0 for an unrelated species, that `GameUI.tscn`/`FieldGuide.tscn` instantiate and wire without error under `res://scenes/Main.tscn`, and that the empty state renders correctly against a real, resident-free world. `bash scripts/run-tests.sh`: 56/57 green, unchanged from before this row's edits — the one red (`test_placeable_schema`, `house.tres`'s placeholder `fact_text`) predates this dispatch and touches no file this row claims. **A layout deviation from spec.md's own ASCII sketch, flagged under Proposals:** spec.md's Screen Layouts diagram draws only one line in the top-left corner (`🪵 Wood: 240`); this row's dispatch brief explicitly required all four top-level counters "at all times" in the persistent HUD, which is a wider reading than that sketch shows and is what got built — a human call on whether the corner should instead stay Wood-only with the other three counters living solely on the Field Guide screen (which gdd.md's own prose already assigns them to). **Amended 2026-08-10 (D-40, human-raised, not a dispatch brief):** the Field Guide's list is no longer resident-state-only — `refresh_from()` now sources `species_hosted_ids()` (all-time, matching the "stay permanent" rule Gentle Displacement already names) and renders every roster species, not just discovered ones, with undiscovered species silhouetted (`FieldGuide.UNDISCOVERED_GLYPH`, `"???"`). The "resident state only" line in this row's own `thin_form` cell above is superseded by this change; the four HUD counters and their mechanics are untouched. This row's status stays ✅ — nothing D-34 gated was broken, only extended by a decision naming its own scope. |

## Row 12 — Pointers

| Field | Value |
|---|---|
| `thin_form` | First-time nudge + one small shared News Report pool. **The Gameplay Hints toggle ships whole** — pillar invariant. |
| `invariants` | The Hints toggle is a pillar invariant, not depth. Hints are invitations, never assignments — copy stays pool-generic and never names the player's own spot or what it lacks (Pillar 1). Discovery reads the near-miss summary one-way and never writes to the simulation; if the summary is empty it degrades to terrain bias. |
| `constants` | `DECIDED 2026-08-09 (→ D-37) — nudge trigger delay = ~3 s` (gdd.md's First 60 Seconds beat 2, "~0:03 — the first News Report fires," transcribed verbatim) · `DECIDED 2026-08-09 (→ D-37) — News Report cadence = 90–150 s, randomised` (no GDD/spec.md precedent existed; picked at a different scale than row 6's 2–6 s wander pause and row 1's 90 s autosave interval, neither of which fits a narrative-hint rhythm — randomised for the same reason those two are, so reports don't fall into a noticeable lockstep) · **`NewsReportToast.DISPLAY_SECONDS = 7.0` — DECIDED 2026-08-09 (human gate)**, seen on screen and approved with no retune · **`NewsReportToast`'s 120 px vertical offset against `DisplacementNotice`'s 28 px — DECIDED 2026-08-09 (human gate)**, approved: the two read as distinct on screen, not clutter |
| `implementation_location` | **Declared and built 2026-08-09 (ui-engineer).** New: `project/scripts/ui/news_report_scheduler.gd` (delta-driven clock — `advance(delta)`, the `ArrivalQueue`/`SettlementWindow` idiom, not a `Timer` node — carrying D-37's two constants), `news_report_content.gd` (one-pass tag-tile-count over the revealed grid + terrain-bias species/line pick — the near-miss summary gdd.md wants does not exist yet, row 6's own build note confirms it, so this is the documented, flagged fallback the row's `invariants` cell requires), `news_report_toast.gd` + `project/scenes/ui/NewsReportToast.tscn` (the non-modal banner spec.md's Screen Layouts names — no Read-Aloud button, per spec.md's own deferral), `news_report_presenter.gd` (wires the two above to a live world + toast; skips the nudge on `WorldRoot.is_new_world == false`), `gameplay_settings.gd` (the Hints toggle's persisted value, `user://settings.cfg` — deliberately separate from a world save), `settings_overlay.gd` + `project/scenes/ui/SettingsOverlay.tscn` (the minimal Settings screen — Hints toggle only — that row 15's own `thin_form` already calls "the existing overlay"; row 15 is expected to extend it with Master Volume/Credits, not replace it). Schema: `project/scripts/definitions/animal_definition.gd` gains `news_reports: Array[String]` — this row's first task, the exact gap this row's `status` cell named. `HABITAT_TAGS`/`BARE_TAGS`/`ID_PATTERN` untouched, so the shared ten-tag vocabulary rows 3/4/5/6/8/1 depend on does not move, and no other file in that directory was opened. Data: `project/data/animals/fox.tres`, `rabbit.tres`, `human.tres` each gain a populated `news_reports` array (9 lines apiece — Discovery/hint + Ambient/flavor sub-pools only, transcribed from `docs/content/*-news-report-pool.md`; fox's flagged-suspect ambient line is excluded; Move-in/Avoidance-relocation/Symmetric-avoids sub-pools are left out because they fire on different triggers this row's cadence never reaches — see `constants`' neighbour cell and Proposals). Modified: `project/scripts/world/world_root.gd` (`WorldRoot.is_new_world`, set once in `_ready()` from `GameSession`'s own intent — additive, no existing field touched), `project/scripts/ui/game_hud.gd` (`_settings_button.disabled` flips to `false` now that `SettingsOverlay` gives it real content), `project/scripts/ui/game_ui.gd` (wires `GameHud.settings_pressed` to `SettingsOverlay.open()`, `SettingsOverlay.hints_toggled` to the live `NewsReportPresenter`, and binds the presenter inside the same `bind_world()` every other per-world connection already goes through), `project/scenes/ui/GameUI.tscn` (three new nodes/instances). New suite: `project/tests/test_news_report.gd`. |
| `validation_status` | **pass (2026-08-09, qa-engineer).** Independently re-run, not re-trusted: `bash scripts/run-tests.sh` (`--import` first) reports the same **58 suites, 57 passed** ui-engineer measured, and `project/tests/test_news_report.gd` independently re-run alone reports the same **158/158**. The one red, `test_placeable_schema` (4 assertions), is confirmed pre-existing and untouched by this dispatch: it fails because `house.tres`'s `fact_text` was written for real on 2026-08-07 (Content Pipeline step 5, content-pipeline-status.md row "House" `copy_content_location`), which invalidated `pending_signoff()`/PLACEHOLDER pins dated 2026-07-28 in a suite last modified 2026-07-28 — both files predate this row-12 dispatch (2026-08-09) by days, and nothing this dispatch touched (`animal_definition.gd`, the three animal `.tres`, `world_root.gd`, `game_hud.gd`, `game_ui.gd`, `GameUI.tscn`) is anywhere near `PlaceableDefinition`/`house.tres`. This is row 4/8's re-point to make, not row 12's. Read the source for every claim rather than re-trusting the numbers: `NewsReportScheduler.advance()` genuinely gates on `hints_enabled` first and returns `EVENT_NONE` unconditionally when off, so the "suppressed live, not just hidden behind a disabled control" claim holds by construction; `set_hints_enabled(false)` really does route through `retire_nudge()` so a pre-nudge toggle-off both retires the nudge AND arms `_report_remaining` (no 0.0-default false-fire on later re-enable); `NewsReportContent.tag_tile_counts()`/`pick_species()` touch nothing but read a grid and an RNG (no simulation writes, matching "Discovery reads, never drives," per gdd.md); `HABITAT_TAGS`/`BARE_TAGS`/`ID_PATTERN` are byte-unchanged in `animal_definition.gd`, confirmed by direct grep, so the shared ten-tag vocabulary rows 3/4/5/6/8/1 depend on did not move. `GameUI.bind_world()`'s existing `world != _world` guard is what actually keeps `NewsReportPresenter.bind()` idempotent across per-frame calls — checked directly, not assumed. The fox pool's excluded suspect line ("tumbling in the leaves all morning") was cross-checked directly against `docs/content/fox-news-report-pool.md`'s own ⚠️ flag and confirmed absent from `fox.tres`. D-37's two constants (nudge ~3 s, cadence 90–150 s) are recorded in `decisions.md` and match this row's `constants` cell verbatim. The full-suite tail shows a `WARNING: 5 ObjectDB instances were leaked at exit` / `1 resources still in use` block; traced by running `test_world_snapshot.gd` alone (the suite that sorts last, immediately after `test_news_report`, in the alphabetical run order) — it reproduces there in isolation, so it is that suite's pre-existing leak, not one row 12 introduced, and it does not affect either suite's pass/fail (no `SCRIPT ERROR`, exit 0). Two things worth the human's attention that are not defects: (1) `GameplaySettings` persists to the real `user://settings.cfg` — a shared file also read by an actual played build — but `reset_for_test()` always leaves it at the ON default the real game also defaults to, so no cross-contamination was found; (2) the toast's `DISPLAY_SECONDS = 7.0` and the vertical-offset judgment call against `DisplacementNotice` are both flagged PROPOSED in-file already and are for the human, not re-litigated here. |
| `human_gate` | **2026-08-09 — full gate, everything approved, no retunes.** A real build, new save, walked the full checklist: **the first-time nudge** fires at ~3 s, reads as an invitation, never names the player's own spot or what it lacks — **approved**. **Ambient News Reports** land on a visibly non-lockstep 90–150 s rhythm — **approved**. **`NewsReportToast.DISPLAY_SECONDS = 7.0`** — **approved, DECIDED 2026-08-09 (human gate)**, no retune. **The toast's 120 px vertical offset against `DisplacementNotice`'s 28 px** — **approved, DECIDED 2026-08-09 (human gate)**: simultaneous toast + notice read as distinct, not clutter. **The Hints toggle** genuinely suppresses the nudge/News Report layer live, and toggling off before the nudge fires retires it permanently even across a later re-enable — **approved**. **The News Report banner's visual register** reads as the same voice as `DisplacementNotice` while staying distinguishable from it and from a fact card — **approved**. **This row closes in full.** Two items stand, disclosed, and do not block this gate: two `[COPY]`-placeholder strings (the nudge's own text, the Hints toggle's label — content-writer's, not this row's) and the Field Guide "hinted at" column, deliberately left unwired pending a save-persistence question that touches row 1. |
| `depth` | not started |
| `est_hours` | 1 (half of the paired 11–12 ledger line) |
| `actual_hours` | — |
| `status` | ✅ — **thin form built, headless-validated, and played by a human end to end (2026-08-09, full gate — see `human_gate`).** The schema gap is closed: `AnimalDefinition.news_reports` exists and all three floor `.tres` carry real, checklist-passed copy — nothing is homeless any more. The first-time nudge fires at D-37's ~3 s on a brand-new save only (`WorldRoot.is_new_world`); the ambient cadence then fires every 90–150 s (D-37), drawing a species by terrain bias (the near-miss summary's stand-in) and a line from that species' pool; the Hints toggle (`GameplaySettings`, surfaced on the new minimal `SettingsOverlay`) suppresses both live, and switching it off before the nudge fires retires the nudge permanently even across a later re-enable, matching gdd.md's "disables it forever" twice-stated. `bash scripts/run-tests.sh`: 58 suites, 57 passed (`project/tests/test_news_report.gd` new, 158/158 assertions) — the one red, `test_placeable_schema`, predates this dispatch and is row 4/8's content-pipeline gap, untouched by anything here. **What is deliberately NOT built:** the Field Guide "hinted at" column `field_guide.gd`'s own header already anticipates — `NewsReportPresenter.hinted_species_ids()` is exposed as a ready, session-only hook, but wiring it in was left for the human to weigh against a persistence decision it would raise. The first-time nudge's own copy and the Settings toggle's own label are both `[COPY]`-marked placeholders, not this row's to write. |

## Row 13 — Mist

| Field | Value |
|---|---|
| `thin_form` | Build-proximity reveal per World Structure: band unfurls, chime plays. Instant, no sprouting — Tier-1-cheap. |
| `invariants` | Nothing exists behind the mist — no terrain data, no animals, no simulation. Reveal is a deterministic function of `(world_seed, x, y)`. Revealed land is always tag-inert wild grass, so revealing never hands the player finished habitat. Animals are never pre-placed. Mist reveal is **not** a qualification trigger (D-22). |
| `constants` | `DECIDED 2026-08-09 (→ D-38) — reveal proximity = 2 tiles` (gdd.md → World Structure, "build or terraform within ~2 tiles of the mist"; also spec.md #19) · `DECIDED 2026-08-09 (→ D-38) — reveal band depth = 2 tiles` (gdd.md only said "a few tiles deeper" with no baseline; set equal to reveal proximity rather than a separate number) · `DECIDED 2026-08-09 (→ D-38) — world cap = 128×128` (gdd.md and spec.md #18; same number row 1's `MAX_SAVED_WORLD_TILES` rules in D-35, decided together so the two cannot drift apart). Step 2 closed at dispatch; step 3 (implementation) landed 2026-08-09 — see `implementation_location`. Both values live in `project/scripts/world/mist_reveal.gd` (`MistReveal.REVEAL_PROXIMITY_TILES` / `REVEAL_BAND_TILES`); the cap is read directly off `WorldRoot.MAX_SAVED_WORLD_TILES`, not duplicated. |
| `implementation_location` | **Declared and built 2026-08-09 (gameplay-engineer).** New: `project/scripts/world/mist_reveal.gd` (`MistReveal` — the two D-38 constants plus `reveal_terrain_id(world_seed, x, z)`, the deterministic-reveal contract the row's invariant names; a constant function in v1 per D-22's inert-land invariant). Edits, all inside the reserved directory: `project/scripts/world/world_grid.gd` (`grown` signal; `_origin_offset` now fixed once at `build()` rather than recomputed, so growth never shifts an already-placed tile; `grow(requested_width, requested_depth, world_seed)` — append-only, clamped to `WorldRoot.MAX_SAVED_WORLD_TILES`, never touches `set_terrain()`/`tile_changed`), `project/scripts/world/terrain_view.gd` (`_tile_bodies`/`_tile_visuals` switched from flat `Array` to `Dictionary` keyed by `Vector2i` — the flat-index formula depends on `depth`, which growth changes; `on_grown()` builds real tiles for new land; boundary walls are now tracked and rebuilt at the new edge on every grow), `project/scripts/world/world_root.gd` (`mist_revealed` signal — the chime hook row 14 has nothing to attach to yet; `_maybe_unfurl_mist()` called from all three edit entry points — `paint_tile`, `place_building`, both branches of `remove_at` — "the three edit entry points are one shape" extended to this trigger; a stale `PROPOSED` marker on `MAX_SAVED_WORLD_TILES` corrected to `DECIDED (→ D-35)` in passing, no value changed). New suite: `project/tests/test_mist_reveal.gd` (37 assertions). One necessary edit outside the reserved directory: `project/tests/test_economy_rules.gd`'s pinned `WorldRoot` signal-surface list gained `mist_revealed`, the same mechanical update row 10's dispatch made to the same list when it added its own signals — confirmed not an error/failure channel. **THE DISCLOSED SCOPE LIMIT, not hidden:** only the high-x ("east") and high-z ("north") edges can ever recede — `grow()`'s own header explains why (append-only is what keeps `WorldSnapshot`/`HomeSiteRegistry`/`RemovalLedger`, all outside this directory, correct with zero changes to any of them); the low corner `(0, 0)` is a permanent boundary in this thin form. Flagged under Proposals. |
| `validation_status` | not started — qa-engineer's to independently re-run. `bash scripts/run-tests.sh`: 58/59 green (the one red, `test_placeable_schema`, predates this dispatch — `house.tres`'s placeholder `fact_text`, untouched by anything here); `test_mist_reveal.gd` alone: 37/37. One finding surfaced while validating, recorded as a `note_expected_pending` in the suite itself and repeated under Proposals: growing the grid to the full 128×128 cap through the real `TerrainView` (measured, not the suite's own cap check, which uses a standalone `WorldGrid`) exceeds Jolt's default physics-body ceiling (10240) partway through — a pre-existing rendering/collision scalability gap (`terrain_view.gd`'s own already-disclosed absence of LOD) that row 13 is what makes reachable for the first time. |
| `human_gate` | not started |
| `depth` | not started |
| `est_hours` | 1.5–2 |
| `actual_hours` | — |
| `status` | 🚧 — **built and headless-verified 2026-08-09 (gameplay-engineer); awaiting qa-engineer's independent re-run and the human gate.** Building or terraforming within `MistReveal.REVEAL_PROXIMITY_TILES` (2) of the grid's current east or north edge instantly grows that edge by `MistReveal.REVEAL_BAND_TILES` (2), with `WorldGrid.grown` firing once per edit (never twice, even when one edit is near both edges) and `WorldRoot.mist_revealed` re-emitting it as the row's chime hook — no audio asset exists yet to attach to it (row 14). New land is always `wild_grass`, tag-inert, with no building and no resident on it, and reveal never calls `set_terrain()`/`tile_changed`, so it can never reach `HabitatSimulation`'s four triggers (D-22) — asserted directly, not inferred, by counting `tile_changed` events across a growth-triggering edit. Growth is append-only (tile `(0, 0)` is never renumbered), which is what makes an already-placed tile's world position provably unchanged across a reveal (also asserted directly) with zero changes to `WorldSnapshot`, `HomeSiteRegistry` or `RemovalLedger`. Growth clamps at the D-38 cap and no-ops past it. **The disclosed trade-off of that same append-only choice: only two of the four edges can ever recede** — the low corner is a permanent boundary in this thin form, not mist — flagged for the human under Proposals rather than hidden. `project/tests/test_mist_reveal.gd` (37 assertions) covers every clause above; `bash scripts/run-tests.sh` is unchanged elsewhere (58/59, the one red pre-existing and unrelated). |

## Row 14 — A thin audio slice

| Field | Value |
|---|---|
| `thin_form` | One ambient bed + one soft confirmation SFX. Silence reads as broken; the tag-driven layer system is deferred. |
| `invariants` | Ambient-only — no music that demands attention, nothing startling (Pillar 5). Row 13's reveal chime and this row's confirmation SFX are **one sound** in the floor. |
| `constants` | none expected — this row has no meaningful step 2 |
| `implementation_location` | not started — `project/scripts/audio/` reserved |
| `validation_status` | not started |
| `human_gate` | not started — a listen, not a taste debate |
| `depth` | not started |
| `est_hours` | 0.75–1 (half of the paired 14–15 ledger line) |
| `actual_hours` | — |
| `status` | ⬜ — nothing built and **no audio assets sourced**: `project/assets/audio/` does not exist. Sourcing is a tech-art asset-audit task (freesound.org is the anticipated per-file-licensing case `AttributionAsset` was written for). |

## Row 15 — Settings & Credits

| Field | Value |
|---|---|
| `thin_form` | **Credits ships whole — attribution is a license obligation, not polish.** Master volume + Hints toggle on the existing overlay. |
| `invariants` | Credits ships whole and does not thin. The Hints toggle is a pillar invariant carried from row 12. Master volume replaces the separable Ambient/SFX sliders in the floor. |
| `constants` | none expected — this row has no meaningful step 2 |
| `implementation_location` | not started — `project/scripts/ui/`, `project/scenes/ui/` reserved. Existing generator: `project/attribution/generate_credits.gd` |
| `validation_status` | the data side passes — `test_attribution` asserts 6 sources, exactly 1 binding obligation, and that the CC BY credit fragment survives into `CREDITS.md`. The **screen** is not validated because it does not exist. |
| `human_gate` | not started |
| `depth` | not started |
| `est_hours` | 0.75–1 (half of the paired 14–15 ledger line) |
| `actual_hours` | — |
| `status` | 🚧 — **the strongest subsystem in the repo below the waterline, and an unmet obligation above it.** `project/attribution/` is complete: two Resource schemas, a generator that fails closed on malformed entries, 6 source entries, license texts on disk, and a regression test. But the rabbit ships under **CC BY 3.0**, whose condition requires the credit be visible *to the player* — `CREDITS.md` does not satisfy that. **The in-game Credits screen is a release blocker**, tracked in [release-checklist.md](release-checklist.md). No Settings screen, no volume control, no Hints toggle. |

---

## Reading this against gdd.md

gdd.md's accepted residual says that if no depth is bought, v1 ships three species, five
terrains, a 1×1 house and untuned camera feel. This table is where that claim gets checked
against reality rather than intent. As seeded: **0 rows ✅, 8 rows 🚧, 7 rows ⬜.** Read the
🚧 rows carefully — nearly all of them are data-and-assets-without-systems. The schema
groundwork, the imported assets and the attribution subsystem are real; the simulation
that would use them is not there yet. No row has passed step 3.

**Update 2026-07-27 — now 0 ✅, 10 🚧, 5 ⬜.** Rows 4 and 5 moved ⬜ → 🚧 on the schema and
data-entry pass that gave rows 3, 4 and 5 their missing content definitions
(`TerrainDefinition`, `PlaceableDefinition`, `HarvestableTileDefinition`, six terrain
entries, one harvestable, one building). **This does not weaken the paragraph above — it
is another instance of it.** Every one of those rows is still data-and-assets-without-
systems: not one line of `grid_manager.gd` changed, nothing reads a `TerrainDefinition`,
and no row has passed step 3. What did change is that `spec.md`'s inert-land invariant is
now *derivable* rather than aspirational (`TerrainDefinition.derive_bare_tags()`), which
was a stated prerequisite of row 6's validation.

**Update 2026-07-27 (Step 2, dispatch 1 of 3 — the simulation spine) — now 0 ✅, 11 🚧,
4 ⬜.** Row 6 moved ⬜ → 🚧, and rows 3, 4, 5 and 6 are **the first rows in the project to
pass step 3**. The paragraph above stops being true of them specifically: they are no longer
data-and-assets-without-systems. The causal path the USP rests on —
*player edit → tile changes → neighbourhood dirty → capacity re-evaluated → arrival enqueued
→ delay → re-check → resident moves in* — runs headlessly, for a wild species and for a
villager, on data alone.

**Read the four rows' `status` for what is still missing, because it is load-bearing.** Two
items in particular are pillar invariants attached to rows already in Tier 1, and neither is
built: **Gentle Displacement** (row 10 — capacity falling below population currently does
nothing) and **removal/undo & refund with its grace window** (row 3's stated thin form,
#16). Both must ship before the step-5 kid playtest, and neither is a depth purchase.

`actual_hours` is the instrument for Open Question **#30** (ledger re-verification at the
week 2–3 velocity review). `costs.md` tracks tokens per work unit; this column tracks the
currency that actually binds. It is worthless if nobody fills it in.
