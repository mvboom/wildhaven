# Godot 4+ specific ignores
.godot/
.nomedia

# Godot-specific ignores
.import/
export.cfg
export_credentials.cfg
*.tmp

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json


# Claude Code local state (settings.json IS committed; these are per-machine)
.claude/settings.local.json
.claude/local/
.worktrees/

godot/**

# Secrets — never commit; the agent is also denied read access to these
.env
.env.*
!.env.example
*.pem
*.key

.claude/worktrees
.superpowers/
builds/
node_modules/
runs/

personal-local-only/
docs/superpowers/