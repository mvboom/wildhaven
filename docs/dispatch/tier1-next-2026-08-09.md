# Tier 1 Planner — dispatch pass, 2026-08-09

Sources read this pass, in full: `game-design/gdd.md` (Phases of Work; Scope: the floor and
the depth — the 15-row table), `game-design/tier1-status.md` (all 15 row blocks, the scan
table, and the directory-claims table), `game-design/systems-pipeline.md`,
`game-design/next-steps.md` (dated 2026-07-27, unchanged since the last pass — used only as
context for build-order deviation), `game-design/decisions.md` (D-35–D-39, confirmed via
grep). `docs/dispatch/tier1-next-2026-08-08.md` and `docs/dispatch/tier1-human-2026-08-09.md`
were read as *context only*, per instructions — every classification below was re-derived
from the current tracker text, not copied. No `git` command was run. No tracker, GDD, or
code file was edited — this report is the only file written. `bash scripts/run-tests.sh`
(full and scoped) was run read-only, for the step-4 spot-check in §3 only.

## 1. Classification of all fifteen rows

| # | System | Phase | Scan glyph | Next incomplete step | Dispatchable? |
|---|---|---|---|---|---|
| 1 | Start & persist | 1, 5 | 🚧 *(stale — row's own `human_gate`/`status` are ✅, full gate 2026-08-09)* | done | Already done |
| 2 | Camera & modes | 1 | 🚧 | 4, verify — *tracker names 4 stale-camera-API suites as failing; **spot-check in §3 found them passing on disk**, a contradiction* | Nominally yes; see §3 fall-through |
| 3 | Terraform | 1 | ✅ | done | Already done |
| 4 | Build | 2 | ✅ | done | Already done |
| 5 | Economy | 2 | 🚧 *(stale — row's own `human_gate`/`status` are ✅ since 2026-08-01)* | done | Already done |
| 6 | Habitat & move-in | 3, 4 | 🚧 *(stale — `human_gate`'s own text now reads "Row closes in full")* | done | Already done |
| 7 | Fact cards | 5 | 🚧 *(stale — full gate 2026-08-09)* | done | Already done |
| 8 | The decided roster | 4 | 🚧 *(stale — ✅ since 2026-08-01)* | done | Already done |
| 9 | Minimal Avoids | 4 | 🚧 *(stale — `human_gate` now reads "Row-level sign-off recorded")* | done | Already done |
| 10 | Gentle displacement | 5 | 🚧 *(stale — full gate 2026-08-09)* | done | Already done |
| 11 | Species status | 6 | ✅ | done (D-34) | Already done |
| 12 | Pointers | 6 | 🚧 | 3, implement | **Yes — top pick (see §3)** |
| 13 | Mist | 6 | ⬜ | 3, implement | Yes |
| 14 | A thin audio slice | 7 | ⬜ | 3, implement | Yes |
| 15 | Settings & Credits | 7 | 🚧 | 3, implement | Yes |

**No row carries `⛔`. No row's next incomplete step is 1 or 2 this pass** — every
constant every row still touches (D-35 row 1, D-36 row 2, D-37 row 12, D-38 row 13) was
ruled today, and every other row is already past step 2. **The blocked-on-a-human list is
empty for the first time** (§5) — a real shift from the 2026-08-08 pass, which had eight
rows there.

**Tracker-hygiene finding, reported not corrected:** ten of the fifteen rows' *scan-table*
glyphs (1, 5, 6, 7, 8, 9, 10) still show 🚧 even though each row's own `human_gate`/`status`
detail fields record a full, dated closure — rows 1, 7, 10 today (2026-08-09); rows 5, 6, 8,
9 as far back as 2026-08-01. Row 11's scan glyph *did* get synced to ✅ on 2026-08-08, so the
sync mechanism exists and works — it just hasn't been re-run since. This doesn't change any
row's classification (classification is decided from each row's own `human_gate`/`status`
fields, per "Classify every row," never from the summary table), but a design-integrity pass
on `tier1-status.md`'s own scan table is overdue.

## 2. Identified next step

**Mechanical ranking, before the spot-check, in order:** Row 2 (phase 1) → Row 12 (phase 6,
🚧) → Row 13 (phase 6, ⬜) → Row 15 (phase 7, 🚧) → Row 14 (phase 7, ⬜). Row 2 ranks first
on phase alone. **But Row 2 does not survive its spot-check (§3) — the tracker and the disk
disagree, so per instructions this pass falls through to the next-ranked candidate.**

**Row 12 — Pointers → step 3 (implement thin).** Owner (tracker's Owner column):
`ui-engineer`.

Ranking reasons, once Row 2 is set aside:

- **Phase.** Row 12 is Phase 6, the lowest phase among the four rows that survive
  (13, 15 also compete at 6/7; 12's own phase, 6, ties with row 13's).
- **Finish-before-starting (decisive against row 13).** Row 12's scan glyph is 🚧 (real
  work exists — three per-species News Report pools, fox/rabbit/human, are written and
  checklist-passed) vs. Row 13's ⬜ (nothing built at all, not even content). 🚧 outranks ⬜.
- **Named dependency (textual, not inferred).** Row 15's own `invariants` names row 12 once
  ("The Hints toggle is a pillar invariant carried from row 12"), and row 14's `invariants`
  names row 13 once ("Row 13's reveal chime and this row's confirmation SFX are one sound in
  the floor"). Neither demotes row 12 or row 13 — both named rows already rank *below* the
  row that names them by phase alone, so the dependency direction is consistent with the
  ranking, not contrary to it. No row's own text names a prerequisite that would place row 12
  itself behind anything.
- **Tiebreak 4 (row number).** Not reached — the glyph rule already separated 12 from 13.

**Why this disagrees with `next-steps.md`:** that doc (2026-07-27, unchanged) groups rows
11, 12, 14, 15 as "the shell," a natural first parallel-dispatch batch. The 2026-08-08 pass
disagreed on row 12 specifically because its constants were still unruled. That reason is
now gone (D-37, today) — this pass's ranking agrees with `next-steps.md`'s grouping again on
row 12, though it still ranks 12 ahead of 14/15 on phase (6 vs. 7), which `next-steps.md`
itself doesn't attempt to order.

**Directory-claim note (does not change the ranking).** Row 12's `implementation_location`
is reserved as `project/scripts/ui/`, which the claims table also assigns to rows 2, 7, 11,
and 15. Rows 7 and 11 are done and out of the way, but **row 2 is still formally 🚧
(pending its own §3 resolution) and row 15 is also a dispatchable 🚧 gap in this same
directory** — a row-12 dispatch touching `game_hud.gd`/`game_ui.gd` is not parallel-safe
against either without sequencing (gdd.md Technical Strategy #6). Separately, row 12's own
status text says its first real task is adding a News Report field to `AnimalDefinition` —
that lands in `project/scripts/definitions/`, the one directory the claims table already
flags as shared-and-in-flux (rows 3, 4, 5, 6, 8, 1); adding a field with no vocabulary change
is low-risk, but it is still a touch on that shared file and worth sequencing consciously
rather than in parallel with any other definitions/ work.

## 3. Spot-check of the top candidate

**Row 2 (the mechanically-top-ranked row) was spot-checked first, and failed it.**
`implementation_location` paths all exist on disk (`project/scripts/ui/camera_rig.gd`,
`tap_router.gd`, `game_hud.gd`, `game_ui.gd`; `project/scripts/world/terrain_view.gd`,
`overview_camera.gd`). `validation_status` names four suites as still failing against the
new camera API and awaiting a QA rewrite: `test_camera_rails.gd`, `test_mode_tap_model.gd`,
`test_resident_lookup.gd`, `test_fact_card.gd`. **Running the full suite and each of the
four scoped suites directly found all four passing, and the full suite at 57 total, 56
passed, 1 failed** — the one red is `test_placeable_schema` (a pre-existing, unrelated
`house.tres` placeholder-text pin, not one of the four row-2 names). `git log` on the four
test files shows they were rewritten as part of `9b9962c` ("Merge branch
'feature/first-person-camera'", 2026-08-07) — the exact rewrite row 2's `validation_status`
still says is outstanding. **The tracker and the disk disagree: the QA rewrite this row's
own text says is still needed already landed and already passes.** Per instructions, this
is reported, not silently corrected — row 2's classification in §1 stands as the tracker
literally reads (step 4, dispatchable), but this pass **falls through to the next-ranked
candidate, row 12,** rather than recommending a dispatch built on a stale validation claim.
This is worth a design-integrity pass on row 2's `validation_status` field specifically.

**Row 12 (the resulting top candidate) was then spot-checked:**

- `project/scripts/ui/` exists and contains exactly the files the tracker names for rows
  2/7/11/15 — no Pointers/News-Report UI file exists yet, confirming row 12's own
  `implementation_location` "not started" claim.
- The three News Report pool docs the row's `status` field names all exist on disk:
  `docs/content/fox-news-report-pool.md`, `docs/content/rabbit-news-report-pool.md`,
  `docs/content/human-news-report-pool.md`.
- `project/scripts/definitions/animal_definition.gd` was read directly: it has no
  `news_report`/`NewsReport` field or property anywhere, and no `@export` beyond `id`,
  `display_name`, `habitat_needs`, `personality`, `avoids`, `farm_tolerant`, `scout_radius`,
  `capacity_radius`, `tiles_per_individual`, `max_individuals`, `model_scene`, `fact_text` —
  confirming the row's own claim ("nowhere for any of them to live") exactly.

**No contradiction between tracker and disk for row 12. Row 12 stands as this pass's actual
top pick.**

## 4. Ready-to-dispatch brief (Row 12 — Pointers)

```
thin_form: First-time nudge + one small shared News Report pool. The Gameplay Hints toggle
ships whole -- pillar invariant.
invariants: The Hints toggle is a pillar invariant, not depth. Hints are invitations, never
assignments -- copy stays pool-generic and never names the player's own spot or what it
lacks (Pillar 1). Discovery reads the near-miss summary one-way and never writes to the
simulation; if the summary is empty it degrades to terrain bias.
acceptance condition: a first-time nudge fires ~3 seconds after a new world starts
(DECIDED 2026-08-09 -> D-37); after that, a small News Report toast fires on a shared,
90-150-second randomised cadence (DECIDED 2026-08-09 -> D-37) drawing from the
already-written, checklist-passed per-species pools (docs/content/fox-news-report-pool.md,
rabbit-news-report-pool.md, human-news-report-pool.md); a Gameplay Hints toggle exists and,
when off, actually suppresses the nudge/News Report layer -- not merely hides a control.
The schema gap named in this row's own status text (AnimalDefinition has no field to hold a
News Report pool) is this row's first real task; adding it touches
project/scripts/definitions/animal_definition.gd, the one directory the tracker's own
claims table flags as shared and in-flux with rows 3/4/5/6/8/1 -- sequence accordingly, and
do not touch the shared ten-tag habitat vocabulary while doing it. This row's constants are
already decided (D-37) -- do not re-derive or re-propose them.
owner_agent: ui-engineer
```

This block is written to be pasted as an `Agent` tool call's `prompt`, verbatim. Flag the
§2 directory-claim caveats (rows 2 and 15 sharing `project/scripts/ui/`; the shared
`definitions/` touch) to whoever dispatches it — sequencing notes, not scope changes.

## 5. Runner-up list

- **Row 13 — Mist** — step 3 (implement), Phase 6, scan glyph ⬜. Constants decided today
  (D-38: reveal proximity 2 tiles, reveal band depth 2 tiles, world cap 128×128). Owner:
  gameplay-engineer.
- **Row 15 — Settings & Credits** — step 3 (implement), Phase 7, scan glyph 🚧 (the
  attribution backend, `project/attribution/`, already exists and is tested). No
  constants gate (systems-pipeline.md's own carve-out for rows 13–15). Owner: ui-engineer.
- **Row 14 — A thin audio slice** — step 3 (implement), Phase 7, scan glyph ⬜ (nothing
  sourced: `project/assets/audio/` does not exist). No constants gate. Owner: tech-art —
  this dispatch starts at the asset-audit half, not a straight code dispatch.
- **Row 2 — Camera & modes** — nominally step 4 (verify headless) per the tracker, Phase 1
  (would otherwise rank first). Held out of the ranked list proper by the §3 contradiction;
  worth a design-integrity look at `validation_status` before dispatching anything against
  it, since the disk suggests this row may already be a `human_gate`-only step away from ✅
  rather than genuinely needing a QA rewrite.

## 6. Blocked, needs a human

**Empty this pass.** No row's next incomplete step is 2 or 5, and no row carries `⛔`. This
is a genuine change from the 2026-08-08 pass (eight rows blocked there: 1, 2, 6, 7, 9, 10,
12, 13) — today's operator rulings (D-35 through D-39, plus the rows 1/6/7/9/10 `human_gate`
closures) cleared every one of them. The table shape is kept below, empty, so a downstream
mechanical reader does not mistake its absence for an unfinished report.

| Row | Next step | Specific blocker | Furthest step actually completed |
|---|---|---|---|
| *(none)* | — | — | — |

## 7. Your call

Row 12's brief (§4) is ready to fire as-is. Dispatch it now — a follow-up `Agent` call to
`ui-engineer` using that prompt verbatim — or hold it until the §2/§3 directory-claim and
row-2 contradiction notes are looked at first? That choice is yours to make, not mine.
