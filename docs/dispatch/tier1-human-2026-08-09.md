# Tier 1 Unblocker — human-triage pass, 2026-08-09 (rev. 2)

## 1. Source report

Input list: `docs/dispatch/tier1-next-2026-08-09.md`, §6 "Blocked, needs a human" — that
table is **empty**. Every row that report's own §1 classification names is either already
closed (rows 1, 3, 4, 5, 6, 7, 8, 9, 10, 11) or dispatchable at step 3/4 (rows 2, 12 at cache
time, 13, 14, 15). Per that report, no row's next incomplete step was 2 or 5 at the moment it
was written, and no row carried `⛔`.

**One deviation from a pure read of that cached list, called out explicitly rather than
silently folded in:** the report's own §4 brief dispatched Row 12 (Pointers) to `ui-engineer`
as its top pick; that dispatch has since landed and passed QA. This is not this report
re-deriving the blocked list — it is the one specific row this run was told had moved past
the cache, checked directly against `tier1-status.md`'s current Row 12 detail block (exactly
the "read the row's own fields" step this report's job already requires once a row is named).
No other row was re-audited beyond what the cached report already classified; the other
fourteen rows' state is trusted from the source report as-is.

**Current Row 12 state, read directly from `tier1-status.md` (2026-08-09):**
`implementation_location` — "Declared and built 2026-08-09 (ui-engineer)." `validation_status`
— "**pass** (2026-08-09, qa-engineer)," independently re-run: 58 suites/57 passed, the new
`test_news_report.gd` at 158/158, the one red (`test_placeable_schema`) confirmed
pre-existing and unrelated. `human_gate` — "not started." `status` — "🚧 — thin form built and
step 4 (qa-engineer) now closed ...; step 5 (human gate) is the only thing still open." This
is a genuine new blocked-on-a-human row the cached report predates, not a re-litigation of
anything it already ruled on.

## 2. Paperwork

**None this pass.** Row 12's `human_gate` is correctly "not started," not stale — a
genuinely un-signed gate on a genuinely just-finished build, not a fix that landed with no
tracker entry to match. Nothing else in the cached report's scope needs a field re-recorded
that hasn't already been applied (rows 1/6/7/9/10's `human_gate` closures and D-35–D-39 are
already in the tracker text, per the source report's own §1).

## 3. Decision

**None this pass.** Row 12's own `constants` field is `DECIDED 2026-08-09 (→ D-37)` for both
values it was carrying (nudge delay ~3 s, News Report cadence 90–150 s) — already ruled,
not re-opened here. The source report names no row at step 2 this pass.

## 4. Verify (1 row)

### Row 12 — Pointers

**Why Verify, not Paperwork or Decision:** the gating constants are already decided (D-37);
what's left is (a) a real playtest of feel/rhythm no headless suite can judge, and (b) two
small presentation constants the build's own code flags as `PROPOSED — human owns this,`
with no gdd.md/spec.md baseline to source a candidate against — the same shape as Row 10's
now-closed marker/font values in the prior pass: the fastest path to a ruling is looking at
the built widget, not guessing numbers in the abstract.

**Checklist — a real build, new save:**

1. Start a brand-new world and confirm the first-time nudge fires at ~3 s (D-37) and reads as
   an *invitation*, not an assignment — it should never name the player's own spot or what
   their land lacks (gdd.md Pillar 1, this row's own `invariants`).
2. Let the world run and confirm ambient News Report toasts land on a visibly non-lockstep
   90–150 s rhythm (D-37) — "a handful of reports in 10–15 minutes," per D-37's own framing,
   not a metronome.
3. **`NewsReportToast.DISPLAY_SECONDS = 7.0`** (`project/scripts/ui/news_report_toast.gd:38`,
   marked `PROPOSED — human owns this` in-file) — sized off `DisplacementNotice.MOMENT_SECONDS`
   (4.5 s) plus headroom for a longer line; does 7 s actually read comfortably for a fluent
   8-year-old reading once while still glancing at the world, or does it linger/vanish wrong?
4. **Toast vertical offset, 120 px vs. `DisplacementNotice`'s 28 px**
   (`project/scenes/ui/NewsReportToast.tscn`, header comment, marked "a JUDGMENT CALL, not a
   spec value") — if a Gentle Displacement warning and a News Report ever land on screen at
   the same time, does the 120 px stack read as two distinct messages, or as clutter/overlap?
5. Toggle Gameplay Hints off, confirm the nudge/toast layer is actually suppressed (not just
   a hidden control), and confirm toggling off *before* the first nudge fires retires it
   permanently even across a later re-enable (gdd.md: "disables it forever," stated twice).
6. Confirm the News Report banner reads as visually the same *voice* as `DisplacementNotice`
   (same panel treatment, per the toast's own header) while still being distinguishable from
   it and from a fact card at a glance.

**Already verified, so the remaining surface is genuinely just the six items above:**
58 suites / 57 passed, `test_news_report.gd` 158/158 independently re-run by qa-engineer, not
re-trusted from ui-engineer's own numbers. Structurally confirmed by direct source read (not
assumed): `NewsReportScheduler.advance()` gates on `hints_enabled` first and returns
`EVENT_NONE` unconditionally when off; `set_hints_enabled(false)` routes through
`retire_nudge()` so a pre-nudge toggle-off both retires the nudge and arms the ambient timer
correctly for a later re-enable; `NewsReportContent` touches only a grid read and an RNG, no
simulation writes (matching "Discovery reads, never drives"); `HABITAT_TAGS`/`BARE_TAGS`/
`ID_PATTERN` byte-unchanged in `animal_definition.gd`, confirmed by direct grep. The fox
pool's flagged-suspect ambient line was cross-checked directly against
`docs/content/fox-news-report-pool.md`'s own warning and confirmed excluded from `fox.tres`.
A leaked-`ObjectDB` warning at the suite tail was traced to `test_world_snapshot.gd` in
isolation — a pre-existing, unrelated leak, not something this row introduced.

**Named separately, because neither is this checklist's to resolve:**

- **Two `[COPY]`-marked placeholders remain** (the nudge's own line, the Settings Hints-toggle
  label) — a content-pipeline gap, not an unverified behaviour, same shape as Row 7's empty
  portrait frame in the prior pass. There is nothing to go look at yet for these two strings;
  closing them needs real copy, not a human playtest call.
- **The Field Guide's "hinted at" column is deliberately unbuilt.**
  `NewsReportPresenter.hinted_species_ids()` is exposed as a ready hook, but wiring it in was
  left out because it raises a persistence decision (does "hinted at" survive a reload, and
  where does it live?) that this row's thin-form dispatch brief didn't scope. This is a
  depth-axis question for a future pass, not a blocker on closing this row's human gate today.
- **`NewsReportContent.BASELINE_WEIGHT = 1.0`** and the choice to scan the whole grid once per
  90–150 s cycle rather than maintain an incremental counter are both flagged `PROPOSED` /
  "a judgment call" in-file (`project/scripts/ui/news_report_content.gd`), but both are
  engineering-judgment items ("worth a second look from whoever owns `WorldGrid` next"), not
  something a playtest checklist would surface — left named for whoever picks up `WorldGrid`
  next, not folded into the checklist above.

**Value beyond this row:** Row 15 (Settings & Credits) — currently dispatchable per the
source report's runner-up list — names Row 12 directly in its own `invariants` ("The Hints
toggle is a pillar invariant carried from row 12"), and its `thin_form` already calls Row 12's
new `SettingsOverlay` "the existing overlay" it expects to extend, not replace. Signing Row
12's human gate doesn't just close Row 12 — it's the surface Row 15's own dispatch brief is
already assuming exists.

## 5. Closing note

Everything above is a proposal, not a ruling — this report has no `Write`/`Edit` access to
`tier1-status.md` or `decisions.md`. Closing Row 12's `human_gate` after walking the checklist
above means the operator writes the sign-off text into that field directly, in the same shape
D-29's and D-34's playtest gates used; if the checklist changes either `DISPLAY_SECONDS` or the
120 px offset, that edit belongs in the two named files/lines directly (`news_report_toast.gd`,
`NewsReportToast.tscn`'s header), not in a tracker field. No new `D-NN` is proposed this pass —
both open items are in-code judgment calls, not gdd.md/spec.md constants, and D-37 already
closed everything this row's `constants` field was carrying.
