# Tier 1 — Next dispatch candidate, 2026-08-13

Scope: all fifteen Tier-1 rows in `game-design/tier1-status.md`, classified against
`game-design/systems-pipeline.md`'s five steps, ranked per this doc's own procedure, with a
disk spot-check on the top-ranked candidate (and, because that check failed, on the
candidate it fell through to).

## 1. Classification table

`Scan glyph` is copied verbatim from `tier1-status.md`'s own **Scan table** (top of the
file). Several rows' own detail sections have moved on since that table was last touched —
see the note under the table — so `Scan glyph` and `Next incomplete step` legitimately
disagree for a number of rows below. That disagreement is expected, not an error on my part:
the scan table is the coarse, slower-to-update signal this run's ranking uses for phase order
and the in-progress/specced/unstarted tie-break; `Next incomplete step` is read fresh off each
row's own `thin_form`/`invariants`/`constants`/`implementation_location`/`validation_status`/
`human_gate` fields, per the classification rules.

| # | System | Phase | Scan glyph | Next incomplete step | Dispatchable? |
|---|---|---|---|---|---|
| 1 | Start & persist | 1, 5 | 🚧 | none — done | Already done (human_gate full gate, 2026-08-09) |
| 2 | Camera & modes | 1 | 🚧 | 4, verify | Dispatchable **per the tracker** — spot-check found the tracker's step-4 blocker is stale (see §3) |
| 3 | Terraform | 1 | ✅ | none — done | Already done |
| 4 | Build | 2 | ✅ | none — done | Already done |
| 5 | Economy | 2 | 🚧 | none — done | Already done (scan glyph stale; row detail `status` is ✅) |
| 6 | Habitat & move-in | 3, 4 | 🚧 | 5, human gate | Blocked on a human — no recorded taste/eyeball pass, only constant rulings |
| 7 | Fact cards | 5 | 🚧 | none — done | Already done (scan glyph stale; row detail `status` is ✅) |
| 8 | The decided roster | 4 | 🚧 | none — done | Already done (owner_agent: content pipeline) |
| 9 | Minimal Avoids | 4 | 🚧 | none — done | Already done (scan glyph stale; row detail `status` is ✅) |
| 10 | Gentle displacement | 5 | 🚧 | none — done | Already done (human_gate full gate, 2026-08-09) |
| 11 | Species status | 6 | ✅ | none — done | Already done |
| 12 | Pointers | 6 | 🚧 | none — done | Already done (scan glyph stale; row detail `status` is ✅) |
| 13 | Mist | 6 | ⬜ | 4, verify | **Dispatchable — recommended, see §2** |
| 14 | A thin audio slice | 7 | ⬜ | 3, implement | Dispatchable |
| 15 | Settings & Credits | 7 | 🚧 | 3, implement | Dispatchable |

**Already done:** rows 1, 3, 4, 5, 7, 8, 9, 10, 11, 12 (10 of 15). All ten carry a recorded
`human_gate` in their own detail section even where the top-of-file scan table's glyph still
reads 🚧 or ⬜ — that table is dated against 2026-07-27 repo state per its own header note and
was not re-touched every time a row's detail section closed later (2026-08-01 through
2026-08-09 in most of these cases). This is a tracker-internal staleness (the scan table and
a row's own `status` field are supposed to always agree, per `tier1-status.md`'s own mechanics
note), not something I corrected — flagging it rather than re-auditing all fifteen rows, which
is design-integrity's job, not mine.

**Blocked, needs a human:** row 6 (see §5).

**Dispatchable gaps:** rows 2, 13, 14, 15 (row 2's dispatchability is what the tracker says;
§3 explains why I don't trust it as this run's top pick).

## 2. Identified next step

**Ranked, in tracker order, before the spot-check:**

1. **Row 2 — Camera & modes** (Phase 1, glyph 🚧, next step 4/verify)
2. **Row 13 — Mist** (Phase 6, glyph ⬜, next step 4/verify)
3. **Row 15 — Settings & Credits** (Phase 7, glyph 🚧, next step 3/implement)
4. **Row 14 — A thin audio slice** (Phase 7, glyph ⬜, next step 3/implement)

Reasoning: phase order puts row 2 first (Phase 1) and row 13 second (Phase 6); rows 14 and 15
tie on Phase 7, and the scan-table glyph tie-break (🚧 outranks ⬜) puts row 15 ahead of row 14.
No row names another of these four as a blocking prerequisite in its own `thin_form`/
`invariants`/prose (row 14's invariants name row 13's mist chime as the thing its SFX pairs
with, but row 13 already shipped the `mist_revealed` hook row 14 needs — the prerequisite step
is already reached, so it doesn't reorder anything here).

**Row 2 was disqualified by its own spot-check (§3) and the recommendation falls through to
row 13.** Row 2's `validation_status` names four suites as failing against the old camera API
(`test_camera_rails.gd`, `test_mode_tap_model.gd`, `test_resident_lookup.gd`,
`test_fact_card.gd`). Running each of them today shows all four green. Recent, undocumented
commits on this branch (`switch to a first-person view`, `Add measured wheel-notch check to
camera rails suite`, `Fix wave: close five review findings on row2-camera-modes-closeout`, and
a further wave of `project/scripts/ui/` work — a Minecraft-style inventory/hotbar rewrite and a
`MenuWindow` that folded `FieldGuide`/`SettingsOverlay` into tabs — have moved the actual UI
code well past what row 2's (and rows 11/12/15's) tracker entries describe. This is a genuine
tracker/disk disagreement, not a stale read on my part — I ran the four named suites directly.

**Recommended: Row 13 — Mist, next incomplete step 4 (verify headless).**

Ranking reasons, unaffected by the row-2 fallthrough: Phase 6 is the lowest phase among the
remaining candidates; the row is already built and self-measured by gameplay-engineer
(2026-08-09) at 58/59 green with the one red pre-existing and unrelated
(`test_placeable_schema`, a row 4/8 content-pipeline gap); what's missing is qa-engineer's own
**independent** re-run — `validation_status` literally reads "not started — qa-engineer's to
independently re-run" — and the human gate after that. I re-ran the suite myself as part of
the spot-check (§3): it holds up, unlike row 2's.

**Directory-claim caveat (does not change the ranking):** row 13's `implementation_location`
is `project/scripts/world/` (`mist_reveal.gd`, plus edits to `world_grid.gd`,
`terrain_view.gd`, `world_root.gd`). That directory is shared with row 6 (`gentle_displacement.gd`'s
directory neighbour, and `world_root.gd` itself is a file row 6's own dispatches have touched)
— and row 6 is currently 🚧/blocked-on-human, not actively being coded, so the practical
collision risk right now is low, but a future row-6 dispatch that reopens `world_root.gd`
alongside a row-13 QA/human-gate pass should be sequenced, per the claims table's own existing
note that this directory is no longer exclusive to any one row.

## 3. Spot-check

**Row 2 (originally top-ranked) — FAILED, tracker and disk disagree.**
- `implementation_location` paths all exist: `project/scripts/ui/camera_rig.gd`,
  `tap_router.gd`, `game_hud.gd`, `game_ui.gd`, `tap_cue.gd`, `ui_palette.gd` — confirmed on disk.
- `validation_status` names four suites as the step-4 blocker; all four exist under
  `project/tests/` and I ran each with `bash scripts/run-tests.sh <name>`:
  `test_camera_rails.gd` (56/56 pass), `test_mode_tap_model.gd` (99/99 pass),
  `test_resident_lookup.gd` (63/63 pass), `test_fact_card.gd` (60/60 pass). **All four pass
  today.** The tracker's row-2 entry (dated 2026-08-03/2026-08-09) is stale — real, uncommitted
  -to-the-tracker work has already closed this gap. Per instructions, I did not correct
  `tier1-status.md` and I am not recommending row 2 off this stale data; I fell through to the
  next-ranked candidate instead.
- Wider observation (not a re-audit, just what surfaced while spot-checking): the same branch
  has recently restructured `project/scripts/ui/` substantially — a `MenuWindow` now hosts
  `FieldGuide`/`SettingsOverlay` as tabs, and `CatalogPanel` was deleted in favour of a 5-slot
  hotbar — none of which is reflected in rows 2, 11, 12, or 15's tracker entries. Rows 11/12
  are already marked done above on the strength of their own recorded `human_gate`s, which
  predate this restructuring, so their *mechanism* may now differ from what's described even
  though their floor-level sign-off stands. Worth a design-integrity pass; out of scope here.

**Row 13 (recommended candidate) — PASSED.**
- `implementation_location` paths all exist: `project/scripts/world/mist_reveal.gd`,
  `world_grid.gd`, `terrain_view.gd`, `world_root.gd` — confirmed on disk.
- `validation_status` names `project/tests/test_mist_reveal.gd` (37 assertions) and the full
  suite (58/59 at last measurement). I ran `bash scripts/run-tests.sh mist_reveal`: **37/37
  pass**, matching the tracker exactly. I also ran the full suite: **66 total, 65 passed, 1
  failed** — the one failure is `test_placeable_schema`, the same pre-existing,
  row-4/8-owned, unrelated red the tracker already names (the suite count grew from 58→66
  between 2026-08-09 and today from unrelated work; the one red did not change).
- No contradiction found. Row 13 is a safe recommendation.

## 4. Runner-up list

- **Row 15 — Settings & Credits** (Phase 7, next step 3/implement). Note found in passing,
  not verified to the depth of a full spot-check: `settings_overlay.gd` and `SettingsOverlay.tscn`
  already exist on disk (built by row 12's 2026-08-09 dispatch, and further folded into the new
  `MenuWindow` by the recent hotbar work) — so this row's own `implementation_location` cell
  ("not started — reserved") undersells what's already there. The remaining floor work — an
  in-game Credits screen (a license/attribution obligation per `release-checklist.md`) and a
  Master Volume control — is genuinely still absent from disk, so the row is still a real gap,
  just not a from-scratch one.
- **Row 14 — A thin audio slice** (Phase 7, next step 3/implement). `project/assets/audio/`
  does not exist; nothing sourced or built. Owner is tech-art per the tracker, and this row's
  own step 2 is explicitly skipped by `systems-pipeline.md` ("no meaningful step 2").
- **Row 2 — Camera & modes**, listed for completeness of the ranked list per the ground-truth
  instructions, but not a live recommendation this run: its tracker-named step-4 blocker
  (four failing suites) no longer reproduces on disk (§3). Whoever owns `tier1-status.md`
  should re-run `bash scripts/run-tests.sh` against this row specifically and update
  `validation_status`/`status` before it's dispatched again on the strength of the tracker
  alone.

## 5. Blocked, needs a human

| Row | Next step | Specific blocker | Furthest step actually completed |
|---|---|---|---|
| 6 — Habitat & move-in | 5, human gate | No recorded taste/eyeball pass on waypoint wander, the home prop, or the qualitative neighbourhood preview. Every `human_gate` entry on this row (D-27, D-28, D-29) rules on **constants** (max_individuals, capacity_radius, arrival delay, wander speed/pause/radius, roamer budget, the capacity-overshoot fix) — none of them records the human actually watching a resident wander, looking at a den prop, or reading the hover-preview bands and approving the feel, the way rows 1/2/3/7/10/12 each have an explicit "the human played/looked/heard" entry. The row's own `status` field stays 🚧 and repeats language that is itself stale (it still says "Gentle Displacement (row 10) is unbuilt," which has been false since 2026-07-28) — consistent with the tracker's own rule that `status` can only read ✅ once `human_gate` is genuinely recorded, and it isn't yet for this row's taste call. | Step 4 (verify) — clean. 51/51 suites at the row's last dedicated measurement (2026-08-01, 1655 assertions), including a dedicated overshoot-fix reproduction (`test_gentle_displacement.gd`'s `_check_arrival_extends_to_a_competing_neighbour()`). Sits inside today's clean 65/66 full run (the one red is row 4/8's unrelated stale content pin). |

## 6. Ready-to-dispatch brief — Row 13, Mist

`thin_form` (verbatim, gdd.md via `tier1-status.md`):
> Build-proximity reveal per World Structure: band unfurls, chime plays. Instant, no
> sprouting — Tier-1-cheap.

`invariants` (verbatim, `tier1-status.md`):
> Nothing exists behind the mist — no terrain data, no animals, no simulation. Reveal is a
> deterministic function of `(world_seed, x, y)`. Revealed land is always tag-inert wild
> grass, so revealing never hands the player finished habitat. Animals are never
> pre-placed. Mist reveal is **not** a qualification trigger (D-22).

**Acceptance condition (step 4, verify headless):** `bash scripts/run-tests.sh` (with
`--import` first, per project convention) independently reproduces green on
`project/tests/test_mist_reveal.gd` (37 assertions) and shows no new regression across the
full suite beyond the one pre-existing, unrelated red (`test_placeable_schema`, row 4/8's).
Record the independent result in `validation_status`, replacing "not started — qa-engineer's
to independently re-run." I reproduced this myself during the spot-check (§3) and it held —
this should be a fast, low-risk dispatch.

**Owner:** `tier1-status.md` lists this row's `owner_agent` as gameplay-engineer, and they
already delivered step 3 (2026-08-09) — that part is done. The outstanding step is 4, verify
headless, whose owner per `systems-pipeline.md` is **qa-engineer**, not gameplay-engineer.
Dispatch this to qa-engineer, not to the row's own `owner_agent` field.

This is not a row whose `owner_agent` is "content pipeline," so it does route through a
direct dispatch (to qa-engineer, per the note above) rather than a Content Pipeline step-3
human decision.

## Closing question

Dispatch the row-13 verify-headless brief above now (a follow-up `Agent` call to qa-engineer,
using §6 verbatim), or hold it? Separately: row 2's tracker entry appears to be several
commits stale (§3) — worth a quick tracker refresh before it's dispatched on its own steam.
