# Tier 1 Planner — dispatch pass, 2026-08-08

Sources read this pass, in full: `game-design/gdd.md` (Phases of Work; Scope: the floor
and the depth — the 15-row table), `game-design/tier1-status.md` (all 15 row blocks, the
scan table, and the directory-claims table), `game-design/systems-pipeline.md`,
`game-design/next-steps.md` (dated 2026-07-27 — used only as context for build-order
deviation, per instructions). No `git` command was run. No tracker, GDD, or code file was
edited — this report is the only file written.

A same-dated report already exists at this path from an earlier pass today; this is an
independent re-read of all three source docs, not a copy of it. The two passes reach the
same conclusion, which is itself worth noting: nothing about tracker state moved between
them.

## 1. Classification of all fifteen rows

| # | Row | Phase(s) | Scan glyph | Next incomplete step | Dispatchable? |
|---|---|---|---|---|---|
| 1 | Start & persist | 1, 5 | 🚧 | 2 — constants | No |
| 2 | Camera & modes | 1 | 🚧 | 2 — constants | No |
| 3 | Terraform | 1 | ✅ | done | — |
| 4 | Build | 2 | ✅ | done | — |
| 5 | Economy | 2 | 🚧 *(scan table stale — row's own `status`/`human_gate` are ✅, see §5)* | done | — |
| 6 | Habitat & move-in | 3, 4 | 🚧 | 5 — human gate | No |
| 7 | Fact cards | 5 | 🚧 | 5 — human gate | No |
| 8 | The decided roster | 4 | 🚧 *(scan table stale — row's own `status`/`human_gate` are ✅, see §5)* | done | — |
| 9 | Minimal Avoids | 4 | 🚧 | 5 — human gate | No |
| 10 | Gentle displacement | 5 | 🚧 | 2 — constants | No |
| 11 | Species status | 6 | ✅ | done (D-34) | — |
| 12 | Pointers | 6 | 🚧 | 2 — constants | No |
| 13 | Mist | 6 | ⬜ | 2 — constants | No |
| 14 | A thin audio slice | 7 | ⬜ | 3 — implement | **Yes** |
| 15 | Settings & Credits | 7 | 🚧 | 3 — implement | **Yes** |

No row's next incomplete step is 1 — every row already carries a populated `thin_form` and
`invariants` quoted from gdd.md. No row carries `⛔` in the scan table.

**Why each step-2/5 call, briefly (full detail in §6):**

- **Row 1** — five constants (`Autosave.INTERVAL_SECONDS`, `WorldSnapshot.SAVE_VERSION`,
  the preset `display_name`, `DEFAULT_WORLD_NAME`, `MIN/MAX_SAVED_WORLD_TILES`) are all
  still bare `PROPOSED (2026-08-01)` with no `DECIDED`/`REVERSED`/approved/D-NN anywhere in
  their own entries. The 2026-08-02 human playtest ruled on a different (row-6) number, not
  these.
- **Row 2** — reopened 2026-08-03 for D-33's first-person rebuild. Every constant the
  rebuild needs (`EYE_HEIGHT`, `MOVE_UNITS_PER_SECOND`, `MOUSE_SENSITIVITY_DEG`,
  `PITCH_LIMIT_DEGREES`, `BODY_CAPSULE_RADIUS`/`_HEIGHT`, the `IMPASSABLE_TERRAIN_IDS`
  mechanism, `MOVEMENT_BLOCK_HEIGHT`, `ThumbnailViewport` size) is `PROPOSED (2026-08-03)`
  and unruled — the old camera's constants further down the same entry *are* `DECIDED` and
  fully gated, but D-33 superseded that camera outright, so those rulings don't transfer.
- **Row 6** — `human_gate`'s own last sentence is still conditional: "Row cannot fully
  close until [the D-29 #5 arrival-check fix] lands and is re-verified." The row's own
  later `validation_status` entry (same day) shows the fix landed and was independently
  re-verified — but `human_gate` was never re-recorded to close the loop, so the field
  itself still reads partial.
- **Row 7** — `human_gate` recorded explicitly as **partial**: nothing verifies a voice
  actually speaks on a real desktop (undecidable headless), and the card's portrait frame
  is still an empty rectangle for every species.
- **Row 9** — `human_gate` literally reads "not started — nothing built yet, so no
  row-level sign-off is possible" — stale wording (the row's own `implementation_location`
  and `validation_status` show it fully built and validated), but no sign-off is actually
  recorded regardless, so step 5 stands open.
- **Row 10** — most constants carry `DECIDED 2026-08-01`, but `human_gate` itself names
  the holdout: "the finer visual constants (colours, radii, fonts) were not individually
  reviewed and stay PROPOSED" (`MARKER_RADIUS`/`_RADIUS_END`/`_WIDTH`/`_DASH`,
  `NOTICE_FACE`, `MOVE_TRAIL`, three `FONT_*` sizes). Not ambiguous — the row's own text
  says so plainly.
- **Row 12** — `constants` field literally: "not started — nudge trigger delay, News
  Report cadence." Both expected, neither proposed.
- **Row 13** — `constants` field literally: "not started — reveal proximity (~2 tiles),
  band width, world cap (~128×128)." All three expected, none proposed.

## 2. Identified next step

**Row 15 — Settings & Credits → step 3 (implement thin).** Owner: `ui-engineer`.

Ranking reasons:

- **Phase.** Rows 14 and 15 are both Phase 7 — the only two dispatchable rows this pass,
  so phase does not separate them.
- **Finish-before-starting (decisive).** Row 15's scan glyph is 🚧 (in progress — the
  attribution backend, `project/attribution/`, already exists and is tested); Row 14's is
  ⬜ (nothing sourced at all: `project/scripts/audio/` and `project/assets/audio/` don't
  exist). 🚧 outranks ⬜, so Row 15 wins.
- **Named dependency (textual, not inferred).** Row 15's own `invariants` names Row 12
  once — "The Hints toggle is a pillar invariant carried from row 12" — and Row 12 is
  itself blocked at step 2. Flagged, but it does not reorder anything: Row 12 is not a
  dispatchable competitor for this slot, and Row 15's own thin form only asks for the
  toggle control to exist on the shared overlay, not for row 12's News Report machinery to
  be live behind it. Row 14's own `invariants` similarly name Row 13 ("Row 13's reveal
  chime and this row's confirmation SFX are one sound in the floor") — also blocked at
  step 2 — again noted, not ranking-determinative, since row 13 would be the one reusing
  row 14's asset, not the reverse.
- **Tiebreak 4 (row number).** Not reached — the glyph rule already separated them.

**Directory-claim caveat (does not change the ranking).** Row 15's
`implementation_location` is `project/scripts/ui/` + `project/scenes/ui/`, which the
claims table also assigns to rows 2, 7, 11, and 12. Row 11 is done and out of the way, but
**rows 2 and 7 are both still 🚧 with live work in that same directory** — row 2's D-33
camera rebuild touches `camera_rig.gd`/`tap_router.gd`/`game_hud.gd`, and row 7's fact-card
work touches `fact_card.gd`. A row-15 dispatch into `game_hud.gd`/`game_ui.gd`/
`ui_palette.gd` is not parallel-safe against either without sequencing (gdd.md Technical
Strategy #6). Row 14's directory, `project/scripts/audio/`, does not exist yet and is
disjoint from everything above.

## 3. Spot-check of the top candidate (Row 15)

- `project/attribution/generate_credits.gd`, `attribution_asset.gd`, `attribution_entry.gd`
  all exist on disk, matching the row's "Existing generator" note.
- `project/scripts/ui/` and `project/scenes/ui/` exist and contain exactly the files the
  tracker names for rows 2/7/11/12 (`camera_rig.gd`, `tap_router.gd`, `game_hud.gd`,
  `game_ui.gd`, `ui_palette.gd`, `fact_card.gd`, `read_aloud.gd`, `field_guide.gd`,
  `displacement_copy.gd`, `displacement_notice.gd`, `neighborhood_preview.gd`,
  `tap_cue.gd` / `GameUI.tscn`, `FieldGuide.tscn`, `FactCard.tscn`,
  `DisplacementNotice.tscn`) — no Settings or Credits screen file exists yet, confirming
  row 15's own "not started" claim for its `implementation_location`.
- `project/tests/test_attribution.gd` exists, matching `validation_status`'s named suite.
  Ran it scoped: `bash scripts/run-tests.sh attribution` → **1 suite, 1 passed, 30
  assertions** (source counts, the CC BY 3.0 verbatim credit-line check, and the
  Required-attributions/Acknowledgements filing checks all passed).

**No contradiction between tracker and disk. Row 15 stands as the top pick; no fall-through
to Row 14 is needed.**

## 4. Ready-to-dispatch brief (Row 15 — Settings & Credits)

```
thin_form: Credits ships whole — attribution is a license obligation, not polish. Master
volume + Hints toggle on the existing overlay.
invariants: Credits ships whole and does not thin. The Hints toggle is a pillar invariant
carried from row 12 — build the control itself now; wiring it to row 12's actual News
Report/nudge layer is that row's own later work once its constants are ruled, and does not
block this dispatch. Master volume replaces the separable Ambient/SFX sliders in the floor.
acceptance condition: a Settings overlay, reachable from a HUD icon shared between the
title screen and in-game (gdd.md -> GUI & screens), renders a master volume slider and a
Gameplay Hints toggle; opening it in-game does NOT pause the world (Pillar 1 -- nothing in
the game protects the player from a menu). A Credits screen reachable from the same surface
lists every third-party art/audio source and its required attribution so the license
condition is satisfied IN-GAME, not only in CREDITS.md -- concretely, the rabbit's CC BY
3.0 credit line must render on screen. project/attribution/generate_credits.gd and
CREDITS.md already exist and are validated (test_attribution.gd, 30/30 passing, confirmed
this pass) but satisfy no player-visible obligation on their own; that gap is what this
row closes. This row has no meaningful step 2 (systems-pipeline.md's own carve-out for
rows 13-15 -- no tuning constants here, only a slider and two toggles), so step 4 is the
attribution checks plus a headless instantiation check for the new screen, and step 5 is a
compliance confirmation, not a taste call.
owner_agent: ui-engineer
```

This block is written to be pasted as an `Agent` tool call's `prompt`, verbatim. Flag the
§2 directory-claim caveat to whoever dispatches it — it is a sequencing note against rows
2 and 7's live work, not a scope change.

## 5. Runner-up

- **Row 14 — A thin audio slice** — step 3, Phase 7, ⬜. Owner **tech-art**, not
  ui-engineer or gameplay-engineer — and not a straight code dispatch in row 15's shape:
  nothing is sourced yet (`project/assets/audio/` does not exist), so this dispatch starts
  at the asset-audit half before landing one ambient bed and one confirmation SFX. No
  constants gate here either (systems-pipeline.md's own carve-out).

That exhausts the dispatchable set — only two rows qualify this pass.

## 6. Blocked, needs a human

Every row below is identified, not dispatchable — its next step is 2 or 5. No row this
pass carries `⛔`. The "furthest step completed" column exists so a step-5 block sitting
behind dozens of passing suites doesn't read as if nothing happened.

| Row | Next step | Specific blocker | Furthest step actually completed |
|---|---|---|---|
| 1 — Start & persist | 2 (constants) | Five constants still bare `PROPOSED (2026-08-01)`: `Autosave.INTERVAL_SECONDS=90.0`, `WorldSnapshot.SAVE_VERSION=1`, preset `display_name`, `DEFAULT_WORLD_NAME`, `MIN/MAX_SAVED_WORLD_TILES=1/128` — none carry a ruling anywhere in their own entries | Step 4 done (57/57 green); a step-5 playtest was even run and recorded **partial**, on top of a step 2 that was never actually closed |
| 2 — Camera & modes | 2 (constants) | Reopened 2026-08-03 for D-33's first-person rebuild; every new constant (`EYE_HEIGHT`, `MOVE_UNITS_PER_SECOND`, `MOUSE_SENSITIVITY_DEG`, `PITCH_LIMIT_DEGREES`, `BODY_CAPSULE_RADIUS/HEIGHT`, `IMPASSABLE_TERRAIN_IDS`, `MOVEMENT_BLOCK_HEIGHT`, `ThumbnailViewport` size) is `PROPOSED (2026-08-03)`, unruled | Step 4 (53/57 green under the new camera; the 4 reds are named, explained, and awaiting a QA rewrite against the new camera, not defects) |
| 6 — Habitat & move-in | 5 (human gate) | `human_gate`'s own text still reads conditional on the D-29 #5 arrival-check fix "landing and being re-verified" — it already did, per this row's own later `validation_status` entry, but `human_gate` was never re-recorded | Step 4 (thin form met in full — capacity formula, wander/home prop, and the live neighborhood preview all built and validated) |
| 7 — Fact cards | 5 (human gate) | `human_gate` recorded explicitly as **partial**: nothing verifies a voice actually speaks on a real desktop (undecidable headless), and the card's portrait frame is still an empty rectangle for every species | Step 4 (51/51 green, the `AUTO_SPEAK` wiring assertion added this pass) |
| 9 — Minimal Avoids | 5 (human gate) | `human_gate` literally reads "not started — nothing built yet, so no row-level sign-off is possible" — stale wording; no sign-off is recorded regardless | Step 4 (53/53 green; a dedicated `test_avoids_distance_keeping.gd`, 29 assertions, proves the bias, the symmetric union, and that avoids never gates a move-in) |
| 10 — Gentle displacement | 2 (constants) | `human_gate` explicitly names the holdout: several `DisplacementNotice` presentation constants (`MARKER_RADIUS`/`_RADIUS_END`/`_WIDTH`/`_DASH`, `NOTICE_FACE`, `MOVE_TRAIL`, three `FONT_*` sizes) "were not individually reviewed and stay PROPOSED" | Step 4 for the mechanism half (147 assertions; the tap-reachability conflict and the D-29 #5 overshoot are both directly verified); the presentation widgets themselves have never been seen by a human |
| 12 — Pointers | 2 (constants) | `constants` field literally: "not started — nudge trigger delay, News Report cadence" | Step 1 only — three News Report pools (fox/rabbit/human) are written and checklist-passed, but `AnimalDefinition` has no field to hold them |
| 13 — Mist | 2 (constants) | `constants` field literally: "not started — reveal proximity (~2 tiles), band width, world cap (~128×128)" | Step 1 only; nothing built |

**Already done (✅), outside both lists:** Row 3 (Terraform), Row 4 (Build), Row 5
(Economy), Row 8 (The decided roster), Row 11 (Species status, closed 2026-08-08 per D-34).

**Tracker discrepancy worth naming, not corrected here:** the scan table at the top of
`tier1-status.md` still shows rows 5 and 8 as 🚧, while each row's own detail block — the
per-field record the "Fields" section names as authoritative — carries an unconditional,
dated `human_gate` approval and a `status` of ✅. Row 11's scan-table glyph *was*
synced to ✅ on 2026-08-08, so the sync mechanism works; rows 5 and 8 are simply still
waiting for the same pass. This report treats rows 5 and 8 as done on the strength of
their own `human_gate` entries, per the "Already done" rule, not on the scan table — and
flags the drift for a design-integrity pass rather than editing either row.

## 7. Against next-steps.md

`next-steps.md` (2026-07-27, before D-29's constant rulings and before D-33's camera
reversal) grouped rows 11, 12, 14, 15 together as "the shell" — a natural first
parallel-dispatch batch, unranked among themselves. This pass's classification agrees on
11 (now closed), 14, and 15, but **disagrees on 12**: it is not currently dispatchable.
Its `constants` field was never advanced past "not started," so — same shape as rows 1, 2,
and 10 — it needs a human ruling (nudge trigger delay, News Report cadence, plus the
`AnimalDefinition` schema gap that has left three already-written, checklist-passed News
Report pools with nowhere to live) before it can reach step 3. This isn't a new deviation
from gdd.md's own Phases of Work — next-steps.md's own "Step 3" already named the one
deliberate deviation on record (row 1 pulled earlier than phase 5) — it's a correction to
next-steps.md's now-stale shell grouping, which predates the step-2 gate being written down
per-row.

## 8. Your call

Row 15's brief (§4) is ready to fire as-is. Dispatch it now — a follow-up `Agent` call to
`ui-engineer` using that prompt verbatim — or hold it until the directory-claim caveat
against rows 2 and 7 is cleared some other way (sequencing, or confirming those two rows'
current work is done touching `project/scripts/ui/`)? That choice is yours to make, not
mine.
