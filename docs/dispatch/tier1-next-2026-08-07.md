# Tier 1 Planner — dispatch pass, 2026-08-07

Sources read: `game-design/gdd.md` (Phases of Work; Scope: the floor and the depth),
`game-design/tier1-status.md` (full, all 15 row blocks + scan table + directory claims),
`game-design/systems-pipeline.md`, `game-design/next-steps.md`. `spec.md` Open Questions
were not needed as a blocker citation for the winning row. No `git` commands run; no
tracker file edited.

## 1. Identified next step

**Row 11 — Species status → step 3 (implement thin).** Owner: `ui-engineer`.

Ranking reasons, in order:

- **Phase.** Row 11 is Phase 6 in the scan table — the lowest phase number among the three
  rows whose next incomplete step is 1, 3, or 4 (the only dispatchable set this pass turned
  up; see §3–4). Rows 14 and 15 are both Phase 7.
- **Scan-table glyph tiebreak (not needed here, phase alone decided it)** — for the record,
  among the Phase-7 pair, row 15 (🚧) would outrank row 14 (⬜).
- **Named dependency check (textual, not inferred).** Row 11's own `thin_form` names row 12
  once: *"the 'hinted at' column needs row 12's per-species News Reports."* That column is
  explicitly carved **out** of row 11's own thin form ("**Resident state only**"), so the
  named dependency does not gate the work row 11 actually has to build. No demotion applied.
- **Tiebreak 4 (row number)** — not reached; phase alone separated all three.

**Directory-claim caveat (does not change the ranking, per the rubric).** Row 11's
`implementation_location` is `project/scripts/ui/` + `project/scenes/ui/`, a directory the
claims table already shares with rows **2, 7, 12, 15** — and rows 2 and 7 are both currently
🚧 with live, uncommitted-feeling churn in exactly that directory (row 2's D-33 camera
rebuild is still touching `game_hud.gd`/`tap_router.gd`/`camera_rig.gd`; row 7's
`fact_card.gd` is mid-flight too). A row-11 dispatch is not parallel-safe against either
without sequencing, per Technical Strategy #6.

## 2. Ready-to-dispatch brief

```
thin_form: HUD counters + a flat Field Guide list. Resident state only; the "hinted at"
column needs row 12's per-species News Reports.
invariants: Counters are informational, never goals (Pillar 1 — no win/loss).
acceptance condition: the persistent HUD shows the four top-level counters (Resources,
Species Hosted, Currently Resident, Village Population) at all times in both first-person
and map-peek view; a Field Guide screen, reachable from the HUD icon, lists every
currently-resident species as a flat list — no percentage, no total-species target, no
completion/finish state anywhere on the screen (the indicator test, Pillar 1). The
"hinted at" column is explicitly out of scope for this dispatch (row 12 territory).
owner_agent: ui-engineer
```

This block is written to be pasted as an `Agent` tool call's `prompt` verbatim. Flag the
directory-claim caveat above to whoever dispatches it, since it is a sequencing note, not a
scope note.

## 3. Runner-up list (the rest of the ranked, dispatchable gaps)

1. **Row 11 — Species status** — step 3, Phase 6, ⬜. (above)
2. **Row 15 — Settings & Credits** — step 3, Phase 7, 🚧. Owner ui-engineer. No constants
   gate (systems-pipeline.md's own carve-out: "no meaningful step 2"). The attribution data
   layer (`project/attribution/`) is done and tested; the Settings/Credits *screen* itself
   is what's unbuilt. Also shares `project/scripts/ui/` with rows 2, 7, 11, 12 (🚧).
3. **Row 14 — A thin audio slice** — step 3, Phase 7, ⬜. Owner tech-art. Also no
   constants gate. Nothing sourced yet: `project/assets/audio/` does not exist, so this
   dispatch starts at the tech-art asset-audit half, not mid-build.

No row's next incomplete step is 1 (scope) — every one of the fifteen already has a
populated `thin_form` and `invariants`. No row is marked ⛔ in the scan table.

## 4. Blocked, needs a human

Every row below is identified, not dispatchable — its next step is 2 or 5, or (none, this
pass) ⛔. Furthest completed pipeline step is named alongside each, so a step-5 block with
heavy validation behind it doesn't read as if nothing happened.

| Row | Next step | Specific blocker | Furthest step actually completed |
|---|---|---|---|
| 1 — Start & persist | 2 (constants) | Five constants still bare `PROPOSED (2026-08-01)` with no `DECIDED`/`REVERSED`/D-NN nearby in their own entries: `Autosave.INTERVAL_SECONDS=90.0`, `WorldSnapshot.SAVE_VERSION=1`, preset `display_name`, `DEFAULT_WORLD_NAME`, `MIN/MAX_SAVED_WORLD_TILES=1/128`. (The row's 2026-08-02 human playtest ruled on an *unrelated* row-6 number, not on these.) | Step 5 (partial human_gate already recorded) — i.e. steps 3–4 are done and step 5 has started, but step 2 was never actually closed underneath it |
| 2 — Camera & modes | 2 (constants) | Reopened 2026-08-03 for D-33's first-person rebuild; every new constant (`EYE_HEIGHT`, `MOVE_UNITS_PER_SECOND`, `MOUSE_SENSITIVITY_DEG`, `PITCH_LIMIT_DEGREES`, `BODY_CAPSULE_RADIUS/HEIGHT`, the `IMPASSABLE_TERRAIN_IDS` mechanism, `MOVEMENT_BLOCK_HEIGHT`, `ThumbnailViewport` size) is `PROPOSED (2026-08-03)`, unruled | Step 4 (53/57 green under the new camera; 4 known-stale suites named and explained, awaiting QA rewrite) |
| 6 — Habitat & move-in | 5 (human gate) | `human_gate` is still literally conditional: *"Row cannot fully close until [the D-29 #5 arrival-check fix] lands and is re-verified."* The row's own later `validation_status` entry (same day) shows that fix **did** land and got a dedicated headless proof — but `human_gate` itself was never re-recorded to close the loop | Step 4 (thin form met in full per its own status text; all three clauses — capacity, wander/home prop, live preview — built and validated) |
| 7 — Fact cards | 5 (human gate) | `human_gate` recorded explicitly as **partial**: nothing has verified a voice actually speaks on a real desktop (undecidable headless), and the card's portrait frame is still an empty rectangle for every species | Step 4 (51/51 green, AUTO_SPEAK assertion added) |
| 9 — Minimal Avoids | 5 (human gate) | `human_gate` field literally reads *"not started — nothing built yet, so no row-level sign-off is possible"* — stale wording (the row is fully built and validated underneath it) but no sign-off is actually recorded | Step 4 (53/53 green; dedicated `test_avoids_distance_keeping.gd`, 29 assertions, confirms the bias, the symmetric union, and zero move-in gating) |
| 10 — Gentle displacement | 2 (constants) | `human_gate` explicitly confirms several `DisplacementNotice` presentation constants (`MARKER_RADIUS`/`_RADIUS_END`/`_WIDTH`/`_DASH`, `NOTICE_FACE`, `MOVE_TRAIL`, the three `FONT_*` sizes) "were not individually reviewed and stay PROPOSED" | Step 4 for the mechanism half (147 assertions, the reachability conflict and the D-29 #5 overshoot both directly verified); presentation widgets never seen by a human at all |
| 12 — Pointers | 2 (constants) | `constants` field literally: *"not started — nudge trigger delay, News Report cadence"* — both expected, neither proposed yet | Step 1 only (thin_form/invariants scoped); verified copy exists (three News Report pools, content-writer's) but has nowhere to live — `AnimalDefinition` has no News Report field |
| 13 — Mist | 2 (constants) | `constants` field literally: *"not started — reveal proximity (~2 tiles), band width, world cap (~128×128)"* — all three expected, none proposed | Step 1 only (thin_form/invariants scoped); nothing built |

**Also flagged, not a gap:** rows **3, 4, 5, 8** are already ✅ (full, non-partial
`human_gate` recorded in each row's own detail block) and sit outside both lists per the
task's own rule. **Discrepancy worth naming:** the scan table at the top of
`tier1-status.md` still shows rows 5 and 8 as 🚧, not ✅ — the row-level `status` field (the
authoritative "only ✅ once `human_gate` is recorded" field, per systems-pipeline.md) has
moved on without the scan table being re-synced. Rows 5 and 8 are treated as done here
because their own `human_gate` entries are unconditional approvals, not because the scan
table says so; the scan table itself looks stale and worth a Design Integrity pass.

## Against next-steps.md

`next-steps.md` (written 2026-07-27, before D-33's camera reversal and before D-29's
constant rulings) recommended a "Step 4 — the shell" batch of rows **11, 12, 14, 15**
together as the natural first parallel-dispatch experiment, without ranking among them.
This pass agrees on rows 11, 14, 15, but **disagrees on row 12**: it is no longer
dispatchable as-is — the constants gate (`constants: not started`) was never closed, so it
needs a human ruling (nudge trigger delay, News Report cadence, and the `AnimalDefinition`
News Report schema gap) before it can move to step 3, same shape as the block already named
for rows 1, 2, and 10.

## 5. Your call

Row 11's brief (§2) is ready to fire as-is. Do you want it dispatched now — a follow-up
`Agent` call to `ui-engineer` using that prompt verbatim — or held until the directory-claim
caveat against rows 2 and 7 is cleared some other way (sequencing, or confirming those two
rows' current dispatches are actually done touching `project/scripts/ui/`)?
