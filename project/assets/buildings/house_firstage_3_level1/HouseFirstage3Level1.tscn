; House look variant — FirstAge 3 Level 1 (content-variety pass Task 2, 2026-08-26).
; One of 9 new House look variants added alongside the original HousesFirstAge1Level1
; (house.tres index 0, unchanged) — see house.tres's header for the consolidated batch
; note and the human sign-off flag.
;
; Source: Quaternius, "Ultimate Fantasy RTS" pack (glTF), Houses_FirstAge_3_Level1.gltf —
; CC0 1.0 Universal, same pack as the existing House entry. See
; project/attribution/sources/quaternius_ultimate_fantasy_rts.tres.
;
; SCALE: raw AABB measured via manual local-transform composition (NOT global_transform —
; see project/data/terrain/cultivated_field.tres's header for why that silently returns
; identity on a freshly-instantiated, not-yet-tree-attached node): footprint 0.6765 x
; 0.6513 tile-units (x, z), height 0.5741 — already sits comfortably within a single 1x1
; tile with margin (smaller even than HousesFirstAge1Level1's own 0.8693 max-axis
; footprint), so NO RESCALE NEEDED, same as the original House variant's own case. Left at
; identity scale.
;
; FACING: left as authored (identity transform, no rotation) — unconfirmed by a human eye
; pass, same posture as every other placement judgment call in this project. FLAG FOR
; HUMAN SIGN-OFF.
[gd_scene load_steps=2 format=3]

[ext_resource type="PackedScene" uid="uid://bpuym6tuoydvi" path="res://assets/buildings/house_firstage_3_level1/HousesFirstAge3Level1.gltf" id="1_model"]

[node name="HouseFirstage3Level1" type="Node3D"]

[node name="Model" parent="." instance=ExtResource("1_model")]
