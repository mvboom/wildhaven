; House look variant — FirstAge 1 Level 2 (content-variety pass Task 2, 2026-08-26).
; One of 9 new House look variants added alongside the original HousesFirstAge1Level1
; (house.tres index 0, unchanged) — see house.tres's header for the consolidated batch
; note and the human sign-off flag.
;
; Source: Quaternius, "Ultimate Fantasy RTS" pack (glTF), Houses_FirstAge_1_Level2.gltf —
; CC0 1.0 Universal, same pack as the existing House entry. See
; project/attribution/sources/quaternius_ultimate_fantasy_rts.tres.
;
; SCALE: raw AABB measured via manual local-transform composition (NOT global_transform —
; see project/data/terrain/cultivated_field.tres's header for why that silently returns
; identity on a freshly-instantiated, not-yet-tree-attached node): footprint 0.8693 x
; 1.1724 tile-units (x, z), height 0.7645. Footprint's long axis (z=1.1724) exceeds the
; 1x1 tile, so uniformly scaled by 0.741466 to bring that axis down to 0.8693 — matching
; HousesFirstAge1Level1's own natural (unscaled) max-axis footprint, the same "small
; margin, not edge-to-edge" fit that variant already established. Scaled result: 0.6446 x
; 0.5669 x 0.8693 (x, y, z).
;
; FACING: left as authored (identity transform, no rotation) — unconfirmed by a human eye
; pass, same posture as every other placement judgment call in this project. FLAG FOR
; HUMAN SIGN-OFF.
[gd_scene load_steps=2 format=3]

[ext_resource type="PackedScene" uid="uid://dq7f7csbl0t08" path="res://assets/buildings/house_firstage_1_level2/HousesFirstAge1Level2.gltf" id="1_model"]

[node name="HouseFirstage1Level2" type="Node3D"]

[node name="Model" parent="." instance=ExtResource("1_model")]
transform = Transform3D(0.741466, 0, 0, 0, 0.741466, 0, 0, 0, 0.741466, 0, 0, 0)
