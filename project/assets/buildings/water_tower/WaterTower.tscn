; WaterTower — NEW farm-building placeable (content-variety pass Task 3, 2026-08-26), one
; of 8 brand-new PlaceableDefinition entries. See project/data/buildings/water_tower.tres
; for the full data-entry header (proposed cost/footprint, human sign-off flag).
;
; Source: Quaternius, "Farm Buildings" pack (FBX), WaterTower.fbx — CC0 1.0 Universal.
; License.txt confirmed CC0 1.0 Universal / Public Domain Dedication before import, per
; asset-import-pipeline.md's audit gate. Same NEW pack as Barn.tscn — see
; project/attribution/sources/quaternius_farm_buildings.tres.
;
; TEXTURE AUDIT (mandatory — DeadTree/PineTree2 precedent; see Barn.tscn's header for the
; full method): probed every surface on the raw, unmodified FBX. albedo_texture null on
; every surface, but vertex_color_use_as_albedo reads true with MEANINGFUL, DISTINCT
; per-surface colors (brown wood ~(0.5115, 0.3069, 0.2132), mid grey ~(0.6269, 0.6269,
; 0.6269), darker grey ~(0.4568, 0.4568, 0.4568)) — same pack's named flat-color
; materials baked into real per-vertex color, NOT the uniform near-(0.9063, 0.9063,
; 0.9063, 1.0) missing-texture tell. No texture fix needed; no material_override applied.
; Plain instance.
;
; SCALE: raw AABB measured via manual local-transform composition on the unmodified FBX
; (NOT global_transform): footprint 2.5323 x 2.4895 (x, z) tile-units, height 8.4286 — a
; naturally TALL, narrow-footprint structure (a raised water tank on legs). Target
; max-axis 0.869309 (House's own established 1x1 margin). Long axis (x) scaled by
; 0.343295. Scaled result: 0.8693 x 2.8935 x 0.8546 (x, y, z) — height deliberately NOT
; capped to match the other buildings, same reasoning as Silo.tscn's header.
;
; FACING: left as authored (identity transform, no rotation) — unconfirmed by a human eye
; pass. FLAG FOR HUMAN SIGN-OFF, including whether the ~2.89-unit height (the tallest of
; the 8 farm buildings) reads correctly at the fixed camera pitch.
[gd_scene load_steps=2 format=3]

[ext_resource type="PackedScene" uid="uid://d0oe4pt6egi6" path="res://assets/buildings/water_tower/WaterTower.fbx" id="1_model"]

[node name="WaterTower" type="Node3D"]

[node name="Model" parent="." instance=ExtResource("1_model")]
transform = Transform3D(0.343295, 0, 0, 0, 0.343295, 0, 0, 0, 0.343295, 0, 0, 0)
